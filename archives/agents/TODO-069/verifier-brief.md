# TODO-069 verifier への依頼

## 対象

未コミットの差分（`git diff`）。仕様は `TODO.md` の TODO-069 の節: 本編でドラッグ中に、スマホは 2 本目の指のタップ、PC は右クリック・
ホイール下で次の向き、ホイール上で 1 つ前の向き。回す軸はつかんでいるマス。ドラッグ中は HUD のボタンを受けない（利用者が決めた）。
implementer がレビュー（`reviewer-report.md`）の指摘を直した。報告は `implementer-report.md`（「レビュー対応（2 回目）」以降が最新）。

## 確かめること（手段も指定）

implementer の実測をなぞらず、別の手順で確かめる。Playwright（npx の置き場から借りる。`tools/capture.mjs` と同じ）、新しいコンテキスト。

1. `tests.html` の全件（件数・落ちた件数・コンソールエラー）
2. 1280×720・マウス: トレイのピースをつかんで盤の上まで動かし、右クリック 1 回・ホイール下 1 回・ホイール上 1 回のそれぞれの前後で、
   `piece.cells` と、マウスの下にあるマス（ピースの `container` の位置とつかんだ点から計算）が同じかを記録する。最後に離して、
   その向きで盤に置かれ、Undo（一手戻す）1 回で置く前に戻ること
3. 844×390・CDP タッチ（`hasTouch`）: 1 本目でつかんで動かし、2 本目でタップして離す。向きが変わり、ドラッグが続き、
   ピースと 1 本目の指の位置関係（マス 1 個ぶん左にずらした分）が回す前と同じこと。2 本目を HUD の「一手戻す」の上で押しても一手戻らないこと
4. ドラッグしていないときの右クリック・ホイールで何も起きないこと。デモ画面で右クリック・ホイールで何も起きないこと
5. `docs/UsersGuide.md` の操作の表が今の動きと合うか

各手順は 1 回でよい（同じコードを通るものを繰り返さない）。値は記録して報告に載せる。

## 見なくてよいもの

見た目、全解データ（`--check` は不要。`logic.js` の全解に関わる関数は変わっていない）、テストの強さ（implementer が壊して確かめた）。

## 報告

`archives/agents/TODO-069/verifier-report.md`。一致したものは 1 行、食い違いだけ詳しく。測った値を必ず載せる。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
