# TODO-069 reviewer への依頼

## 対象

`git diff`（未コミット）。仕様は `TODO.md` の TODO-069 の節: 本編でドラッグ中に、スマホは 2 本目の指のタップ、PC は右クリック・
ホイール下で次の向き、ホイール上で 1 つ前の向き。回す軸はつかんでいるマス。報告: `archives/agents/TODO-069/implementer-report.md`。
依頼: `implementer-brief.md`（「気をつけること」に挙げた点も見る）。

## 見てほしいこと

- 2 本目の指の `pointerdown` / `pointermove` / `pointerup` でドラッグが外れたり動いたりしないか。最初の指を離したとき 2 本目がまだ
  押されているとき、2 本目が先に離れたとき、2 本目の指がピースの上に触れたとき（`onPiecePointerDown` が走らないか）
- 右クリックでドラッグ・タップが始まらないか。右ボタンを押したまま動かしたとき。ドラッグ中でないときの右クリック
- ホイールの間引き（一定時間は無視）の作り。`this.time.now` などシーンの時間を使っているか
- 回す軸の計算（`turnPivot` など）: 向きを変えたあとも、つかんでいるマスが同じ画面位置に残るか。タッチでマス 1 個ぶんずらした分、
  トレイの縮小表示から広がった分（`scale`）の扱い。対称なピース（T・U・V・W・Z など）で、つかんだマスがどこへ移るかの決め方
  （implementer は対称なケースを個別に測っていないと報告）
- 1 つ前の向き（逆順）が、次の向きとちょうど逆になっているか。X など向きが 1 つのピース
- `DemoScene` で何も起きないか。本編の今の動き（タップ・ドラッグ・スワイプ・Undo）を変えていないか
- 規約（`setTimeout` 無し、値は `config.js`、状態はシーンのプロパティ）。テストが壊すと落ちるか（足したテストがあれば Node で確かめる）

## 見なくてよいもの

画面の見た目、全解データ、間引きの時間の値の良し悪し。

## 報告

`archives/agents/TODO-069/reviewer-report.md` に、指摘を重い順に（場所・問題・どうすればよいか）。問題の無い観点は 1 行ずつ。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
