# TODO-063 tests 報告

## 完了条件

`python3 -m http.server 8765` 経由の `tests.html`（Playwright、新しいコンテキスト）で
**339 件すべて通った**（元の 331 件 + 新規 8 件）。コンソールエラーなし。

## 1. 落ちた既存 2 件を直した

「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が残っていれば、
次は必ず place になる」（8×8・6×10）。崩し（TODO-063）が行き詰まりの一続きの末尾に
割り込んで remove を続けることがあるのが原因だった。

`tests.html` 側で、置いた直後のその場外し（TODO-060/066〜068）と「行き詰まりの
一続き」を、既存の「置ける手が尽きて 1 手ずつ外す並び…」テストと同じ分け方で
見分けたうえ、行き詰まりが終わるたびに深さ（`stack.length` 相当）ごとの回数を
`maybeCollapse()` と同じ規則で数え直し、`DEMO.randomCollapseAfter` 回目なら
続く `DEMO.randomCollapseMoves` 手を崩しとして読み飛ばしてから「次は place」を
確かめるようにした。タイトルも「崩しも起きなければ」を明記する形に変えた
（元の狙いは弱めていない）。

## 2. 足したテスト（4 件 × 8×8/6×10 = 8 件）

- 「崩し（TODO-063）が起き、1 回で外す手数が randomCollapseMoves（盤に残る手が
  それより少なければその数）になる」— `regionsFitPieces` では浅い深さでの崩しが
  ほぼ起きないため、実際の解と照合する `hasSolution`（既存テストと同じ考え方）を
  使い、シード 1〜5・手数 8000 で確かめた
- 「同じ深さの行き詰まりが randomCollapseAfter 回目のときだけ崩す」
- 「崩した手も控えに入り、崩した先の盤面で同じ手をもう一度選ばない」

3 つとも、テストの中だけ `DEMO.randomCollapseAfter` を 3 に下げて崩しを起きやすく
し、`finally` で元に戻す。共通の前処理は `collectCollapses()` にまとめた
（solveStepsRandom の出力を、行き詰まりの一続き／その場外し／崩しに分類し直す
もの。1 の直したテストと同じ分け方を関数として切り出した）。

## 3. 壊すと落ちるか（Node で確認・元に戻した。`src/` は変更していない）

一時的に `src/logic.js` を書き換えて Playwright（新しいコンテキスト）で確認し、
`\cp` で元の内容に戻した（`md5sum` で復元を確認済み）。

- (a) `yield* maybeCollapse();` を止めて崩しを起こさない
  → 新規 3 件が「崩しが一度も起きなかった」で失敗（1 で直した既存テストも失敗）
- (b) その場外し（TODO-060/066〜068）の直後にも `maybeCollapse()` を呼び、
  詰まりに数える
  → 1 で直した既存テストを含む 8 件が失敗（既存の「置ける手が尽きて…」系も含む）
- (c) 崩しの remove で控え（`failedOf`）に入れない
  → 既存の「『解なし』だった置き方は盤面ごとに覚え…」（8×8・6×10）が失敗。
  ただし、専用に足した「崩した手も控えに入り…」テストは、たまたま同じ
  （盤面, 置き方）へ再度たどり着く場面が今回のシード・手数では起きず、
  このテスト単体では検知できなかった（別の既存テストが同じ壊れ方を
  確実に検知しているので、全体としては (c) も壊すと落ちる状態になっている。
  境界線上の判断のため報告のみ）

## 4. シードの組を 10 通りずらして確認（Node）

`collectCollapses()` と同じロジックを Node に複製し、`FAST_SEED` の代わりに
シード 1〜10（テスト2・3相当、手数 20000）、シード群 1-5・6-10・…・46-50 の
10 通り（テスト1相当、手数 8000・`hasSolution`）を試した。

- 8×8: 崩し 1305 件・手数不一致 0・回数不一致 0・控えの違反 0・
  クリップ（浅い深さでの崩し）が出た組 10/10
- 6×10: 崩し 1213 件・手数不一致 0・回数不一致 0・控えの違反 0・
  クリップが出た組 10/10

10 通りとも成り立った（`ALL OK`）。手数は `tests.html` 側と揃えたまま増やして
いない。この確認スクリプト自体は Node の一括実行で 1 盤あたり数十秒かかった
（`hasSolution` が 6×10 で 9356 解と毎回照合するため）が、`tests.html` 側の
実行時間（ブラウザで全 339 件が数秒以内）には影響しない。

## 保った点

- `src/` は最終的に無変更（`git diff` で確認、変更は implementer の元の
  diff のみ）
- 既存テストの書き方（`failedByBoard`/`activeStack` を使う分け方、
  `hasUnfitClosedRegion` 等の共有ヘルパー）に合わせた

## 追記（reviewer の要修正への対応）

reviewer 報告（`archives/agents/TODO-063/reviewer-report.md`）の要修正 1・検討 2 に対応した。

### 直したこと

- 足した 3 本（`collectCollapses()` 含む、旧 `tests.html:1520-1674`）を削除した。
  崩しの確かめは、1 で直した既存テスト（「外す連なりが ok:true で止まったとき…」）
  に一本化した
- その既存テストの崩し消化ループを直した:
  - `i >= steps.length` で打ち切るのは「手数の上限で打ち切った」場合だけとし、
    それ以外で `steps[i]` が `remove` でなければ（＝崩しの remove 数が足りない）
    `assert` で失敗させる（前は `ok=false` のまま握りつぶしていた）
  - 崩しがちょうど期待した手数で終わったとき（打ち切りでないとき）、置ける手が
    残っていれば「次は必ず place」を追加で確かめるようにした（外しすぎ＝
    `moves+1` を捕まえるため）

### 壊すと落ちるか（Node で確認・`src/` は元に戻した。`\cp` 後 `md5sum` で復元を確認済み）

5 つの壊し方（reviewer の表と同じ）を `src/logic.js` に一時的に入れ、Playwright
（毎回 `fetch('/src/logic.js')` で実際に配信されている中身にその壊し方が入って
いることを確かめたうえで）`tests.html` を実行した。

| 壊し方 | 8×8 | 6×10 |
|---|---|---|
| 崩しを 1 回遅らせる（`count <= after` で return） | 落ちた | 落ちた |
| 崩しを 1 回早める（`count < after - 1` で return） | 落ちた | 落ちた |
| 崩す手数を 1 手減らす（`moves - 1`） | 落ちた | 落ちた |
| 崩す手数を 1 手増やす（`moves + 1`） | **落ちた（直す前は未検知）** | 落ちた |
| 崩した後に回数（`collapseCounts`）を消さない | 落ちた | 落ちた |
| 崩した手を控え（`failedOf`）に入れない | 落ちた（既存の「『解なし』だった
  置き方は盤面ごとに覚え…」で） | 落ちた（同左） |

いずれも両方の盤で落ちることを確認した。「崩す手数を 1 手増やす」は直す前は
6×10 だけしか落ちなかったが、上の「次は必ず place」の追加確認で 8×8 も
落ちるようになった。「崩した手を控えに入れない」は既存の別テストが両方の盤で
確実に捕まえるため、専用テストは足していない（reviewer の検討 2 のとおり）。

### シードの組を 10 通りずらして確認（Node、手数は 4000 のまま増やしていない）

直した既存テストと同じロジックを Node に複製し、`FAST_SEED` の代わりにシード
1〜10 を順に試した。10 通りとも 8×8・6×10 の両方で成り立った
（`checkedRunEnds` は毎回 75〜173、1 件あたり 0.6〜1.3 秒）。

### 完了条件（直した後）

`python3 -m http.server 8765` 経由の `tests.html`（Playwright、新しいコンテキスト）で
**333 件すべて通った**（元の 331 件 + 1 で直した既存テストの分。追加の 3 本は
削除したので純増はゼロ）。コンソールエラーなし。`src/` は最終的に無変更
（`git diff --stat src/logic.js` は implementer の元の diff のみ）。
