# TODO-063 reviewer への依頼

## 対象

`git diff`（未コミット）。仕様: デモのランダム（`solveStepsRandom()`）で、行き詰まり（置ける手が尽きて戻った一続き）が終わったときの
`stack.length` ごとに回数を数え、`DEMO.randomCollapseAfter`（仮 5）に達したら `DEMO.randomCollapseMoves`（仮 3）手を続けて外す。
崩した手は全部控え `failed` に入れる。置いた直後にその場で外す手（TODO-060/068・066・067）は数えない。崩したら崩した後の深さ以上の回数を消す。
利用者は途中で 2 回決め直した（経緯は `archives/agents/TODO-063/implementer-report.md` の各追記）。報告: `implementer-report.md`（「追記 2」が最新）・`tests-report.md`。

## 見てほしいこと

- 仕様どおりか。回数の数え方・消し方、崩しの手数（盤に残る手が少ないとき）、空の盤まで崩したとき、崩しの途中で解が見えるなどの端
- TODO-059・061・062・066〜068 の動きを壊していないか。乱数は `random` だけか。決め直しの名残（使われない関数・古いコメント）が無いか
- `src/scenes/demo.js` を直さなくてよいか（崩しの remove の連なりは待たずに戻す作りに乗るか）
- テスト: 直した 2 件・足した 8 件が狙いを弱めていないか。tests の報告にある「(c) 崩した手を控えに入れない、を専用テストが捕まえない」
  の扱い（既存テストで捕まるなら専用テストが要るか）

## 見なくてよいもの

画面の見た目、全解データ、値の良し悪し。

## 報告

`archives/agents/TODO-063/reviewer-report.md` に、指摘を重い順に（場所・問題・どうすればよいか）。問題の無い観点は 1 行ずつ。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
