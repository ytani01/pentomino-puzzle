# TODO-063 implementer 報告

## 追記 2（「詰まり」を行き詰まりだけに数える決め直しへの対応）

利用者がさらに決め直した:「詰まり」は**行き詰まり（置ける手が尽きて戻った
一続き）だけ**を数える。置いた直後にその場で外す手（TODO-060/068・066・067）
は数えない。深さごとに数える・崩した手は全部控えに入れる、はそのまま。
以下は最新の内容で、この節より下（「追記」節・元の報告）は経緯として残す。

### 変更したファイル（この追記の分）

- `src/logic.js`
  - `yield* maybeCollapse();` の呼び出しを、行き詰まりの `while` ループ
    （置ける手が尽きたとき）の直後 1 か所だけに残し、TODO-066（穴に合う手が
    解なしにしたときの 2 手外し）・TODO-060/068（5 の倍数でない空き）・
    TODO-067（置き済みと同じ形の空き）の 3 か所からは外した（コメントで
    「置いた直後のその場外しは『詰まり』に数えない」と明記）。
  - `solveStepsRandom()` の JSDoc・`collapseCounts` / `maybeCollapse()` の
    コメントを、「詰まり＝行き詰まりの一続きだけを数える」に書き換え。
- `src/config.js:762-767` — `randomCollapseAfter` の説明に、詰まりに数える
  のは行き詰まりだけで、その場で外す手は数えないことを追記。
- `docs/UsersGuide.md:123-125` — 「詰まり」が行き詰まりだけを指すことに
  合わせて文面を修正。
- `docs/developer.md:237` — 同じ趣旨で「詰まり」の説明を修正。

### 直したあとの再検証

- `node tools/gen-solutions.mjs --check` — 終了コード 0（8×8: 520 解・
  4.7 秒、6×10: 9356 解・148.8 秒。一致）。
- `tests.html`（Playwright, 新しいコンテキスト）— **331 件通過、2 件失敗**
  （直す前の 8 件から、TODO-060/066/067/068 のその場外しが崩しの対象から
  外れたことで、それに依存していた 6 件が通るようになった）。残る 2 件:
  1. 「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が
     残っていれば、次は必ず place になる」（8×8）— N の remove の直後が
     place でない
  2. 同上（6×10）— N の remove の直後が place でない
  （原因の見立てはしていません。行き詰まりの一続きの末尾で崩しが割り込んで
  remove を続ける、という新しい正当な挙動によるものに見える点は変わって
  いません。）

### 実測（依頼どおりの表）

`hasSolution` を渡し、`mulberry32` シードで空の盤から解くまで回す
（RUNS=15、`randomCollapseMoves=3` 固定、`MAX_STEPS=400000`。全設定・
全盤で 15/15 解けた）。計測には `src/logic.js` の
`solveStepsRandom()`（公開済みの export だけを使う）を忠実に複製した
計測専用スクリプトを使い、複製がリポジトリの実装と同じ手順を返すことを
シード 5 通りで突き合わせて確認済み。スクリプトはスクラッチパッドのみで
リポジトリには残していない。

| 盤 | 設定 | 解けた/実行 | 最初の解までの place 数 平均 | 崩しなし比 | 1 回の解あたりの崩した回数 平均 |
|---|---|---|---|---|---|
| 8×8 | 崩しなし | 15/15 | 403.2 | 1.00 | 0.00 |
| 8×8 | after=2 | 15/15 | 2502.9 | 6.21 | 24.53 |
| 8×8 | after=3 | 15/15 | 963.9 | 2.39 | 5.47 |
| 8×8 | after=5 | 15/15 | 804.4 | **1.99** | 2.20 |
| 6×10 | 崩しなし | 15/15 | 303.7 | 1.00 | 0.00 |
| 6×10 | after=2 | 15/15 | 1621.1 | 5.34 | 19.33 |
| 6×10 | after=3 | 15/15 | 1026.5 | 3.38 | 6.93 |
| 6×10 | after=5 | 15/15 | 692.0 | 2.28 | 1.93 |

### 値の判断

**`randomCollapseAfter = 5`（`randomCollapseMoves = 3` のまま）を選んだ**
（`src/config.js` は元々この値だったので変更なし）。

- `after=2` は崩しすぎ（1 回の解で 20 回前後崩し、手数が崩しなしの 5〜6 倍）
  で、依頼の条件から外れる。
- `after=3` は「1 回の解で数回崩す」（5.47・6.93 回）には合うが、手数が
  崩しなしの 2.4〜3.4 倍で「2 倍を超えない」から外れる。
- `after=5` は 8×8 で崩しなし比 1.99 倍（2 倍をわずかに下回る）、6×10 で
  2.28 倍（2 倍をわずかに超える）。1 回の解あたりの崩した回数は 8×8 が
  2.20 回・6×10 が 1.93 回で、どちらも「数回」の範囲。**2 つの盤の一方
  （6×10）は「2 倍を超えない」をわずかに超えているが、3 つの候補の中では
  もっとも両方の条件に近く、`randomCollapseMoves` は今回動かしていないので
  これも含めて追加で振るかは判断が要る**（下の「判断が要る点」に追記）。

## 追記（利用者の決め直しへの対応）

依頼ファイルの「控えに入れるのは最後の 1 手だけ」を、利用者が
「まとめて崩した手は**全部**控えに入れる（それぞれ外した後の盤面の控え）」
に決め直したので、以下のとおり直した。以下は追記時点の最新の内容で、
この節より下は最初の報告のまま残してある（判断の経緯が追えるように）。

- `src/logic.js` — `maybeCollapse()` 内の崩しの remove を、控えに入れない
  `undoLastPlain()` ではなく、通常どおり `undoLast()`（外した後の盤面の
  控えへ入れる）で行うように変更。使われなくなった `undoLastPlain()` と、
  それだけのために切り出していた `popLast()` を削除し、`undoLast()` を
  元の 1 関数の形に戻した。JSDoc（`solveStepsRandom()` の崩しの説明）も
  「崩した手も通常の 1 手外しと同じくそれぞれ控えに入る」に書き換えた。
- `docs/developer.md` の探し方の表の行 — 「控えに入れるのはこの崩しの
  最後の 1 手だけ」→「崩した手も 1 手ずつ通常どおり控えに入る」に修正。
- `src/config.js`・`docs/UsersGuide.md` — 元々「最後の 1 手だけ」に触れて
  いなかったので変更なし。

### 直したあとの再検証

- `node tools/gen-solutions.mjs --check` — 終了コード 0（8×8: 520 解・
  5.1 秒、6×10: 9356 解・201.9 秒。ともに `src/data/*.js` と一致）。
- `tests.html`（Playwright, 新しいコンテキスト）— **325 件通過、8 件失敗**
  （直す前の 10 件から、「『解なし』だった置き方は盤面ごとに覚え…」の
  2 件（8×8・6×10）が通るようになった。崩した手も全部控えるようになり、
  控えの前提と実装が揃ったため）。残る 8 件:
  1. 「置ける手が尽きて 1 手ずつ外す並び（remove の連なり）は、最後の 1 手
     だけが ok:true か盤が空になっており、途中の remove は ok:false」
     （既定, 8×8）— V の remove の直後が remove でない
  2. 同上（常に偽, 8×8）— V の remove の直後が remove でない
  3. 「常に偽を返す判定だと、置ける手が尽きるたびに、盤のピースがすべて
     外れるまで remove が続く」（8×8）— 期待 0 / 実際 4
  4. 「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が
     残っていれば、次は必ず place になる」（8×8）— X の remove の直後が
     place でない
  5. 同 1.（既定, 6×10）— P の remove
  6. 同 1.（常に偽, 6×10）— N の remove
  7. 同 3.（6×10）— 期待 0 / 実際 7
  8. 同 4.（6×10）— F の remove
  （原因の見立てはしていません。いずれも「外す一続きの終わりに崩しが
  割り込んで remove を続ける」という新しい正当な挙動によるものに見える点は
  直す前と同じです。）

- 実測（`hasSolution` を渡し、`mulberry32` シードで空の盤から解くまで
  回す。RUNS=15, MAX_STEPS=200000。スクリプトはスクラッチパッドのみで
  リポジトリには残していない）:

  崩しあり（`randomCollapseAfter=5`, `randomCollapseMoves=3`。直した実装）

  | 盤 | runs | 崩し回数（3 手以上連続 remove）平均/run | 1 回に外した手数 min/avg/max | 最初の解までの place 数 平均 |
  |---|---|---|---|---|
  | 8×8 | 15 | 660.3 | 3 / 4.38 / 11 | 7251.3 |
  | 6×10 | 15 | 150.9 | 3 / 4.48 / 11 | 1588.3 |

  崩しなし（`DEMO.randomCollapseAfter = Infinity` で比較用に測定）

  | 盤 | runs | 3 手以上連続 remove 平均/run | 最初の解までの place 数 平均 |
  |---|---|---|---|
  | 8×8 | 15 | 12.7 | 403.2 |
  | 6×10 | 15 | 11.3 | 303.7 |

  直す前（最後の 1 手だけ控える版）の実測は下の「実測」節に残してある
  （8×8: 4670.1、6×10: 1260.3）。**全部控える版は、8×8 で約 1.5 倍
  （4670→7251）、6×10 で約 1.3 倍（1260→1588）さらに手数が増えた。**
  崩した手を全部控えると、同じ失敗をより避けるようになった分、探索が
  別の手を試そうとして時間がかかるようになったように見える
  （原因の切り分けはしていません）。

## 変更したファイル

- `src/config.js:762-766, 780-781` — `DEMO` の JSDoc に `randomCollapseAfter` /
  `randomCollapseMoves` の説明を追加し、値を追加（仮の値: `randomCollapseAfter: 5`,
  `randomCollapseMoves: 3`）。
- `src/logic.js`（**「追記」節のとおり、この後さらに直している**。ここは
  最新の状態）
  - `:590-600` 付近（JSDoc）— 崩しの仕組みと理由を `solveStepsRandom()` の
    説明に追記（崩した手も全部控えに入る、と書いてある）。
  - `undoLast()` の直後 — 深さごとの回数を数える `collapseCounts`（`Map`）と、
    外す一続きが終わるたびに呼ぶ `function* maybeCollapse()` を追加
    （閾値に達したら `randomCollapseMoves` 手をまとめて `undoLast()`（毎回
    控えに入る）で外す。崩した後は、崩した後の深さ以上の `collapseCounts`
    を消す）。
  - 4 か所の「外す一続き」の直後（行き詰まりの `while` ループ、TODO-066 の
    2 手外し、TODO-060/068 の即時外し、TODO-067 の即時外し）に
    `yield* maybeCollapse();` を追加。
- `docs/UsersGuide.md:123-125` — ランダムの説明に、同じ深さへ何度も戻ると
  数手まとめて崩す旨を追記。
- `docs/developer.md:237` — 探し方の表の行に、崩しの説明（`randomCollapseAfter`
  ・`randomCollapseMoves`・崩した手も 1 手ずつ通常どおり控えに入る）を追記。

`src/scenes/demo.js` は変更していません（下記「確認したが直さなかったこと」）。

## 確認したが直さなかったこと

- `demo.js` の `advance()` は、今の手と 1 手先読みした手が両方 `remove` なら
  待たずに続けて動かす仕組み（TODO-060）になっており、これは 1 手ずつの
  先読みなので、崩しで生まれる 3 手以上の `remove` の連なりにもそのまま
  効きます（`type: 'remove'` を返す点は `undoLastPlain()` も `undoLast()` も
  同じため）。**`demo.js` を直す必要はありませんでした。**

## 検証（最初の実装。控えは最後の 1 手だけの版。今は「追記」節のほうが最新）

### `node tools/gen-solutions.mjs --check`

バックグラウンドで実行。終了コード 0、`src/data/8x8.js`・`src/data/6x10.js`
とも一致（8×8: 520 解・4.7 秒、6×10: 9356 解・154.2 秒）。

### `tests.html`（Playwright, 新しいコンテキスト、`python3 -m http.server 8765` 経由）

323 件通過、10 件失敗。失敗した既存テスト（原因の見立てはしていません。
すべて「崩し」で挙動が変わったために既存テストの前提が崩れた箇所に見えます
——判断は主担当へ）:

1. 「置ける手が尽きて 1 手ずつ外す並び（remove の連なり）は、最後の 1 手
   だけが ok:true か盤が空になっており、途中の remove は ok:false」
   （既定, 8×8）— V の remove（ok:false、盤に残りあり）の直後が remove でない
2. 同上（常に偽, 8×8）— V の remove（ok:false、盤に残りあり）の直後が
   remove でない
3. 「常に偽を返す判定だと、置ける手が尽きるたびに、盤のピースがすべて
   外れるまで remove が続く」（8×8）— 期待 0 / 実際 4
4. 「『解なし』だった置き方は盤面ごとに覚え、同じ盤面で同じ置き方をもう
   一度 place しない」（8×8）— T を、以前 remove した同じ盤面・同じ置き方で
   もう一度 place した
5. 「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が
   残っていれば、次は必ず place になる」（8×8）— X の remove で ok:true に
   なり、まだ置ける手が残っているのに次が place でない
6. 同 1.（既定, 6×10）— P の remove
7. 同 1.（常に偽, 6×10）— N の remove
8. 同 3.（6×10）— 期待 0 / 実際 7
9. 同 4.（6×10）— V を、以前 remove した同じ盤面・同じ置き方でもう一度
   place した
10. 同 5.（6×10）— F の remove

### 実測（`hasSolution` を `canContinue` に渡し、`mulberry32` シードで空の盤から
解くまで回す。スクリプトはセッションのスクラッチパッドに置いたのみ、
リポジトリには残していません）

崩しあり（`randomCollapseAfter=5`, `randomCollapseMoves=3`、実装した値のまま）:

| 盤 | runs | 崩し回数（3 手以上連続 remove）平均/run | 1 回に外した手数 min/avg/max | 最初の解までの place 数 平均 |
|---|---|---|---|---|
| 8×8 | 15 | 422.2 | 3 / 4.40 / 11 | 4670.1 |
| 6×10 | 15 | 120.3 | 3 / 4.48 / 11 | 1260.3 |

崩しなし（`DEMO.randomCollapseAfter = Infinity` にして比較用に測定。変更前相当）:

| 盤 | runs | 3 手以上連続 remove 平均/run | 最初の解までの place 数 平均 |
|---|---|---|---|
| 8×8 | 30 | 12.4 | 428.6 |
| 6×10 | 30 | 10.9 | 295.0 |

## 判断が要る点（最新は「追記 2」節）

- 「詰まり」を行き詰まりだけに絞ったことで、崩しが起きる頻度そのものが
  大きく減りました（「追記 2」の表を参照）。**`randomCollapseAfter = 5` を
  選びましたが、6×10 だけは「崩しなしの 2 倍を超えない」からわずかに
  はみ出ています**（2.28 倍）。`randomCollapseMoves`（今回は 3 で固定）を
  下げれば 6×10 も 2 倍以内に収まる可能性がありますが、依頼された組み合わせ
  （`randomCollapseAfter` のみ 2・3・5 を振る）には無かったので試していません。
  必要なら `randomCollapseMoves` も振って追試してください。
- `tests.html` の失敗は 10 件→8 件→**2 件**になりました（「追記 2」節に
  最新の一覧があります）。残る 2 件はどちらも「外す連なりが ok:true で
  止まったとき、まだ置ける手が残っていれば次は必ず place になる」で、
  崩し機能が行き詰まりの一続きの末尾に割り込んで remove を続ける、という
  新しい正当な挙動によるものに見えます。既存テストをどう直す（またはどこ
  まで新しい前提として書き直す）かは主担当の判断です。

（このあとの「判断が要る点」の古い版は、直す前の状態の記録として残して
あります。）

### 判断が要る点（1 つ前。控えを全部にした直後の版）

- **崩しあり（仮の値 5/3、崩した手を全部控える版）は、最初の解までの
  place 数が崩しなしの約 18〜5.2 倍に増えます**（8×8: 403→7251、
  6×10: 304→1588）。「追記」節の実測のとおり、控えを最後の 1 手だけから
  全部に変えたことで、8×8 はさらに 1.5 倍（4670→7251）、6×10 は
  1.3 倍（1260→1588）増えました。値を仮のまま公開すると、デモの
  「最初の解までの時間」がかなり延びる可能性があります。
  `randomCollapseAfter` / `randomCollapseMoves` の値を画面で見て決めるとき、
  この実測も踏まえて判断してください。
- `tests.html` の失敗は 10 件→8 件になりました（「追記」節に最新の一覧が
  あります）。残る 8 件は、崩し機能が「外す一続きの終わり」に割り込んで
  追加の remove を続ける、という新しい正当な挙動によるものに見えます。
  既存テストをどう直す（またはどこまでを新しい前提として書き直す）かは
  主担当の判断です。
