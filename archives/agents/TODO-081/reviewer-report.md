# TODO-081 reviewer 報告

対象: `git diff`（src/logic.js・src/config.js・tests.html・docs/UsersGuide.md・docs/developer.md）。
コードは直していない。

## 実測したこと

- `tests.html` を今の差分のまま Playwright（headless Chromium、npx の playwright 1.63.0）で実行: **405 件すべて通った**（約 33 秒）
- `src/` と `tests.html` を scratchpad に写し、`src/logic.js` だけを壊して `tests.html` を流した（作業ツリーは触っていない）

| 壊し方 | 結果 |
|---|---|
| M1 `undoLast()` で `deadMoves` を 0 に戻さない | 590 秒で終わらない（どこかのテストが解に着かず回り続ける）。アサーションでは落ちない |
| M2 `deadMoves >= limit` を `>` にする | 足したテスト 2 件が落ちる |
| M3 上限で戻ったあとの `maybeCollapse()` を消す | **405 件すべて通る** |
| M4 `deadMoves` を増やさない（上限が効かない） | 足したテスト 2 件が落ちる |
| M5 その場で外した手を数えない | 足したテスト 2 件が落ちる |
| M6 上限で `undoUntilOk()` でなく 1 手だけ外す | 10 件が落ちる |

- `canContinue = () => false`、8×8、シード 14 で 3000 手を Node で回した: 置いた回数 1500、最大の深さ 7。
  置いた手の深さは `1,2,3,3,4,5,6,6,7` のあと**すべて 1**（最初の上限で空まで戻ったあとは、毎手すぐ戻る）

## 要修正

なし。

## 検討

### 1. 上限で戻ったあと「詰まり」に数えることを確かめるテストが無い

- `src/logic.js:865`（`yield* maybeCollapse();`）
- 何が問題か: この 1 行を消しても（M3）405 件すべて通る。JSDoc（`src/logic.js:698-700`）と
  `docs/developer.md`・`docs/UsersGuide.md` の 3 か所で「上限で戻ったのも詰まりに数える」と
  書いているのに、テストが押さえていない
- 根拠: 実測（上の表の M3）
- 判断が要る点: 足すかどうか。TODO.md の見込みのチェック項目（「上限を超えない」テスト）には入っていない

### 2. 空の盤まで戻っても解のある盤面にならないと、`deadMoves` が 0 に戻らず、以後は毎手すぐ戻る

- `src/logic.js:745`（0 に戻すのは `undoLast()` で `boardOk` が真になったときだけ）、`src/logic.js:862`
- 何が問題か: `undoUntilOk()` が空の盤まで戻っても `canContinue` が偽なら、`boardOk` は偽のまま、
  `deadMoves` も上限以上のまま残る。以後は空の盤に 1 手置くたびに上限の分岐に入り、すぐ外す
  （深さ 0 の `maybeCollapse()` の回数も毎手増える）。コメント（`src/logic.js:734-735`）は
  「空の盤は真」と書いているが、それは最初の値だけで、戻ったあとの空の盤には当てはまらない
- 根拠: 実測（常に偽で 3000 手、最初の 9 手のあとは深さ 1 のまま）
- 実害は未確認。デモが渡す `hasSolution` は空の盤で必ず真なので、デモでは起きないはず。
  起きるのはテストで常に偽を渡したときだけ
- 関連: `tests.html:1471-1473` のコメント「常に偽だと DEMO.randomDeadLimit 手ごとに盤が空まで戻り」は
  実測と違う（最初の 1 回のあとは毎手戻る）。既存テストの上限を `Infinity` にした（try/finally の 8 行）のは
  この振る舞いのため

### 3. 既存テストのタイトルが「置ける手が尽きるたびに」のまま

- `tests.html:1566`（差分の外。上限の 8 のまま常に偽で走る）
- 何が問題か: 常に偽では、戻るきっかけのほとんどが上限になった（上の 2）。テストは通るが、
  タイトルとコメントの「置ける手が尽きるたびに」が確かめている中身と合わない
- 根拠: 実測（上の 2）と読んだコード

## 好みの範囲

- `src/logic.js:842`: 置いた手で `boardOk` が真になっても `deadMoves` を 0 に戻さない。
  コメント（`src/logic.js:735`）は「解のある盤面に戻ったら 0 に戻す」。`hasSolution` は解の無い盤面へ
  足しても解が戻らないので、デモでは起きない。ずれるのは、置くと真に戻りうる判定を渡したときだけ（実害は未確認）
- `docs/UsersGuide.md:177`・`183`: 「「解なし」のまま 8 手置いた」は、札が「解なし」に変わる最初の手を
  数えない（数えるのは解の無い盤面の上に置いた手）。札を数える人には 9 回目に見える。
  あわせて、この 2 行だけ前後より長い（前後はおよそ 40 字で折り返している）

## 問題の無かった観点

- 経路ごとの `boardOk` / `deadMoves`: 通常の place（:839-842）、forced の 2 手外し、その場外し 3 種、
  尽きたときの戻り、`maybeCollapse()` の崩しは、どれも `undoLast()` を通るので漏れは無い。空の盤で控えを消す
  経路（:798-801）は `hasSolution` なら空の盤で `boardOk` が真のため問題なし（常に偽のときは上の 2）
- 止まるか: 常に偽でも毎手 yield するので、何も返さずに回り続けるループにはならない（実測で 3000 手出た）。
  `undoUntilOk()` は `stack.length > 0` で守られているので、空のスタックから外さない（M6 で 1 手外しにすると落ちる）
- TODO-063 との整合: 上限で戻るのは「尽きる前に気づいた行き詰まり」として数える、と JSDoc に理由つきで書いてある。
  その場外しは数えないという元の意図とは食い違わない
- 足したテストの強さ: M2・M4・M5・M6 で落ちる。M1 はハングで気づく（アサーションでは落ちない）
- 既存テストの上限を `Infinity` にした変更: 常に偽のときだけ元の振る舞いに戻して、その場外しの確認を続けている。
  上限で戻る並びは、`hasSolution`（既定）の分と `tests.html:1566` の常に偽のテストが今も上限 8 で見ているので、
  確かめる中身は弱まっていない。`DEMO` を書き換えて try/finally で戻すやり方は既存（:2204、:2394）と同じ
- 数値の置き場所: `randomDeadLimit` は `DEMO` に置き、コメントに理由（なぜ要るか）と誰が決めたかがある
- JSDoc: 理由（TODO-081。百手以上重ねていたため）が書いてある。developer.md の 2 か所・UsersGuide の 2 か所と実装は一致
- 範囲: 指示に無い変更は無い
- 規約: `setTimeout`・トップレベルの `let`・色の直書きは無い（`boardOk` / `deadMoves` は関数の中）

## 作り込みすぎ

- `tests.html:2431,2439,2457,2460`: delete: 足したテストの `board` は作って更新しているが、どのアサーションも読まない。4 行を消してよい
- `tests.html:2431-2437`: shrink: `board`・`boardOk`・`deadRun`・`awaitingOk`・`solved`・`taken` を外で宣言し、シードのループの頭で入れ直している。
  ループの中で `let` 宣言すれば、外の 6 行が要らない（外に要るのは `sawLimitHit` だけ）
- `tests.html:1471-1481`: 上の「検討 2」で空の盤に戻ったときに `deadMoves` を 0 に戻すことにするなら、
  この try/finally（8 行）は要らなくなる見込み（未確認）

net: -10 lines possible.
