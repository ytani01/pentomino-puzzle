# TODO-044 reviewer の報告

対象: `git diff src/`（`src/logic.js`・`src/scenes/game.js`。未コミット）。

## 要修正

### 1. ヒント表示が入だと、周りを囲まれたピースを盤から外せない（外すと即座に埋め戻される）

- 場所: `src/scenes/game.js:796-803`（`dropDrag()` の「外す」）→ `:1165`（`refreshHud()` の先頭の `fillForced()`）
- 起きる手順: ヒント表示が入・解ける盤面で、上下左右を他のピースか盤の縁で囲まれたピース X を
  トレイへ運んで離す。
  1. `history.push(snapshot)`（X が盤にある状態）、`X.location = 'tray'`、`audio.lift()`
  2. `refreshHud()` → `fillForced()`。X の抜けた跡は切り離された 5 マスで X と同じ形、
     盤面は元の解の一部なので `hasSolution` も真 → X を元の場所へ置き直す（`audio.auto()`・「X を置いた」）
  3. 結果、盤は外す前とまったく同じ。**履歴には外す前（＝今と同じ状態）が 1 件積まれる**ので、
     次の「一手戻す」を押しても見た目が何も変わらない（1 回ぶん空振りする）
- 実測（Node、ロジックだけ。画面では未確認）: 8×8 の代表形 65 件から、隣り合う 2 つを
  トレイに残した盤（10 マスの空き 1 つ。ヒント入でも埋まらずに止まる盤）1272 件で、
  盤の他のピースを 1 つずつ外した 12720 通りのうち **7796 通り（61%）で、外したピースが
  そのまま元の位置に戻る**（戻したあとの `boardKey` が外す前と一致: 7796 / 7796）。
  スクリプトは scratchpad の `bounce.mjs`（プロジェクトには残していない）
- なぜ問題か: 終盤ほど囲まれたピースが多く、ヒント表示を入にしたまま組み替えようとすると
  外せない。利用者から見ると「外したのに戻ってくる」「一手戻すが効かない」になる。
  依頼の「戻した直後にまた埋まって戻せなくなる筋」と同じ種類の問題で、「外す」の側で起きる
- 判断が要る点: 論理的には「その空きはそのピースしか入らない」ので、仕様として受け入れる
  選択もある。受け入れる場合でも、空振りする履歴 1 件は残る
- 同じ種類: ヒント表示が切のときに X を外し（履歴に X が盤にある状態）、そのあと入にすると
  X が埋め戻され、次の一手戻すが空振りする

## 検討

### 2. 一手戻したら、そのまま「完成」へ進むことがある（実害は未確認）

- 場所: `src/scenes/game.js:916-920` → `:901-913` → `:1165`・`:1175`
- 起きる手順: ヒント表示が切のまま、残り 1 個（その空きは切り離されていてそのピースの形）の
  盤 S1 から、盤の別のピース B を外して隣り合う 10 マスの空きにする（S2）。ヒント表示を入にする
  （S2 では埋まらない）。一手戻す → S1 に戻り、`fillForced()` が最後の 1 個を埋めて `checkSolved()`
  → そのまま Clear へ移る
- 依頼の設計（「戻した盤面に当たる空きがあればまた埋まる。止まらない。これでよい」）の範囲内の
  挙動で、ヒント表示が入のあいだに積んだ履歴だけなら起きない（入のあいだに撮った控えは、
  すでに埋め終えた盤なので戻しても埋まらない。コードの読み）。「戻す」を押して完成になる
  のを許すかどうかだけ

### 3. おまかせの直後に埋まると文言が上書きされ音が 2 回鳴る件（把握済みのもの）

直さなくてよい。どちらのピースも滑って入るので置かれたことは見て分かり、音もほぼ同時で 1 回に聞こえる。

## 問題なしの観点

- 再入: `fillForced()` は `refreshHud()` を呼ばず、`checkSolved()` → `refreshHud()` の入れ子では
  `playing` が偽なので埋めない。ループしない
- 二重のクリア遷移: `checkSolved()` の先頭 `if (!this.playing) return;`（`:1190`）で、`dropDrag()`・
  `useAuto()` が後から呼ぶ 2 回目は止まる。`restoreState()`・`turnPiece()`・`toggleHint()` は
  `checkSolved()` を呼ばないので、埋めて完成したときは `refreshHud()` 末尾の 1 回だけ
- `persist()` の順: 埋めたあとの盤を控えてから `checkSolved()` が `clearProgress()` するので、
  完成した盤が遊びかけに残ることはない。入れ子の `refreshHud()` の `persist()` は `playing` 偽で何もしない
- ボタンの状態: 完成時は入れ子の `refreshHud()` が最後に走るので、一手戻す・おまかせは無効で終わる
- ドラッグ中・`pending` 中: 盤を変える入口（ボタン）はポインタ 1 本では押せず、`main.js` は
  `activePointers` を設定していない（Phaser の既定はタッチ 1 本）。全解のデータが届く
  `refreshHud()`（`:107`）はヒント表示が押せる前なので `hinting` が偽で埋めない
- `restoreState()`（ドロップ失敗で戻す）: 控えはドラッグを始めた時点の盤で、ヒント表示が入なら
  すでに埋め終えた盤なので、戻しても埋まらない
- 一手戻すのまとめ方: 置く（`:782`）・外す（`:798`）・盤の上で向きを変える（`:872`）・おまかせ（`:1011`）は
  履歴を積んでから `refreshHud()` で埋めるので、1 回で両方戻る。トレイの上で向きを変えるのは盤が
  変わらず埋まらない。ヒント表示を入にしたときは直前の手とまとめて戻る（設計どおり）。
  入のあいだに撮った控えへ戻っても再び埋まらないので、戻せなくなる筋は無い（上の 1 を除く）
- 続きから始めた直後: `hinting` は保存しておらず偽から始まるので、組み立て中には埋めない。
  入にして埋めた手は履歴が空なので戻せない（設計どおり）
- デモ: `demo.js:186` が `refreshHud()` を上書きし、`checkSolved`・`toggleHint`・`undo`・`useAuto`・
  `restoreState` も呼ばない（`rg` で確認）。`fillForced()` には届かない
- `forcedPlacements()`: `normalize()` は並べ替えまでするので `sameShape()` で比べてよく、返す `row`/`col`
  は `normalize()` が引いた最小の行・列と同じなので `place()` で元の領域に戻る。穴（`#`）は
  埋まったマスとして扱われる。同じピースが 2 領域に当たると 2 つ目は `find` が何も返さず飛ばす。
  実装者の `check-forced.mjs`（a/b/c とも NG 0）とも合う
- 規約: `setTimeout` なし、モジュールのトップレベルの `let` なし、`logic.js` に Phaser/DOM なし、
  盤面は `place()` で作り直し、新しい数値・色なし。変更は `logic.js`・`game.js` だけ
- JSDoc: `emptyRegions`・`forcedPlacements`・`fillForced`・`refreshHud` とも「なぜ」が書いてある
- `emptyRegionSizes()` が領域ごとに配列を作るようになった点: 既定の `regionsFitPieces` を通るのは
  `solveSteps()` を既定で呼ぶところだけで、本編・デモ・`tools/` は通らない。速さの影響は無視してよい（未計測）

## 作り込みすぎ

- `src/scenes/game.js:1143-1151` と `:1011-1019`（`useAuto()`）: shrink（好みの範囲）。
  「ピースを引く・`cells`/`location`/`row`/`col` を入れる・`place()`・`refreshPiece()`・`settlePiece(piece, true)`」の
  8 行が同じ。`putOnBoard(name, cells, row, col)` のような 1 つにまとめれば両方から 1 行で呼べる
- 他は無し。`forcedPlacements()` の `shapes` の前計算と重複の判定は必要な分だけ

net: -7 lines possible.

## 見直し（2 回目）

対象: main が直した (a) `refreshHud()` で控えを捨てる、(b) `undo()` の繰り返し、(c) `slideIn()`。
コードを読んで確かめた（画面・Node では測っていない）。

### 要修正

なし。

### 検討

- `src/scenes/game.js:922-927`（(b)）: 挙動がヒント表示と関係なく変わる。**ヒント表示が切でも、
  盤の上のピースを掴んで同じ場所へ置き直した手**（`dropDrag()` は置けた時点で `history.push` するので、
  盤の変わらない控えが積まれる。前からある挙動）を、一手戻すが飛ばして 1 つ前の手までまとめて戻す。
  前は 1 回空振りしていたので良くなる方向だが、TODO-044 の外の変化で、JSDoc には「ヒント表示が切のときに
  打った手へ戻すと」とだけ書いてある。受け入れるなら JSDoc にこの場合も入る旨を足すかどうか（実害は未確認）

### 問題なしの観点

- (b) 止まらないループ: 1 周ごとに `history.pop()` で必ず 1 件減り、中で呼ばれる `restoreState()` →
  `refreshHud()` → `fillForced()` → `checkSolved()` はどれも履歴を積まない（(a) はさらに減らすだけ）。必ず止まる
- (b) クリア遷移: 戻した先で埋まって完成すると盤が `before` と変わるのでループはそこで抜け、
  `checkSolved()` は `playing` を偽にするので 2 回目は無い。指摘 2 を「許す」とした挙動のまま
- (b) 戻しすぎ: 盤が変わった時点で止まるので、見た目が変わる手はいつも 1 つだけ戻る
- (a) 捨てる条件: 埋めた（`filled`）ときだけ見る。置く・盤の上で向きを変える・おまかせは、履歴の末尾より
  ピースが増えるか向きが変わるので一致しない。一致するのは「外したピースが埋め戻された」
  （指摘 1。切のときに外してから入にした場合も含む）と、一手戻した先で埋まって 1 つ前の控えと同じ盤に
  なった場合だけ。どちらも今の盤と同じ控えなので、捨てても戻せる手は減らない。埋め戻したピースの向きは
  `normalize()` した正規形で、控えの向き（正規形）と同じ
- (a) 二重に捨てる: 入れ子の `refreshHud()`（`checkSolved()` から）は `playing` 偽で `filled` が偽なので捨てない
- (a) 一手戻すボタン: 捨てたあとに `setEnabled` を判定しているので、空になれば押せなくなる
- (c) `slideIn()`: `useAuto()` の履歴を積む→置く→`usedAuto` の順は前と同じ。`fillForced()` の中身も同じ動き

### 作り込みすぎ

なし。(b) だけでも指摘 1 の空振りは飛ばせるが、(a) が無いと「外して埋め戻された」控えだけが残ったとき、
一手戻すが押せるのに何も起きないので、(a) も要る。`slideIn()` の JSDoc は「何を」寄りだが、
2 か所で共通に使うことが「なぜ」になっているので、このままでよい。

net: -0 lines possible.
