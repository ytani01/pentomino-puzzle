# TODO-047 2 段目 wording 報告

対象: `README.md`・`docs/developer.md`・`CLAUDE.md`。3 つとも最後まで読んだ。

## 直した箇所

- `README.md:34` ・ 「近くの置ける升目へ吸い付く」→「近くの置けるマスへ吸い付く」・
  同じ「置けるマス」を指すのに、この 1 箇所だけ「升目」という別の語を使っていた。
  同じ README の 23 行目・39 行目や `docs/developer.md`・`CLAUDE.md` はどこも
  「マス」「マス目」で、「升目」はプロジェクト内に他の用例が無い（表記のゆれ）
- `README.md:60-61` ・ 「マウスを載せると（タッチ端末では押すと）名前が出る」→
  「…何のボタンかが出る」・ 同じ機能（HUD ボタンの説明表示）を、本編の説明
  （32 行目「何のボタンかが出る」）と別の言い方（「名前が出る」）で書いていた。
  `docs/developer.md:45` も「何のボタンかは説明に出す」と書いており、そちらに揃えた
  （表記のゆれ）

## 見送った箇所（直さなかったもの）

- `docs/developer.md:52` の「1 頁ぶんずつ**頁送り**する」・「頁」はカタカナの
  「ページ」ではなく漢字だが、`src/scenes/records.js` のコメント側で
  「頁送り」「1 頁」「頁を送る」を一貫して使っており、このリポジトリで
  定着した語だった。ユーザー全体の CLAUDE.md は「そのリポジトリで既に
  使われている語で書く」としているため、そのまま残した
- README の「音の ON・OFF」と「ヒント表示…入にすると」・トグルの呼び方が
  2 種類あるように見えたが、`src/icons.js`・`src/ui.js`・`src/scenes/game.js`
  のコメントで「音 ON／音 OFF」と「入／切」がそれぞれ別物として明示的に
  区別されていた（game.js:1037 のコメント参照）。実装に合わせた表記なので
  揺れではなく、直さなかった
- 「盤」（画面の用語表の見出し語）と「盤面」（盤の状態・データを指す語。
  `盤面の更新` `遊びかけの盤面` など）・ 前者は UI の枠、後者は中身の状態を
  指しており、使い分けが一貫していたので直さなかった

## 確認が要る点

特に無し。3 文書とも読み終え、事実（数値・関数名・ファイル名・コマンド・
見出し文言）は変更していない。

---

## 2 回目（基準を上げての再確認）

基準: 「読めなくはない」でなく「初めて読む人が一度で読めるか」。
`README.md:92`（icons.js の行）は main が直した箇所なので触っていない。

### 直した箇所

- `README.md:32` ・「マウスを載せると、タッチ端末では押したときに、何のボタン
  かが出る」→「マウスを載せると（タッチ端末では押すと）何のボタンかが出る」・
  読点で 3 つの事柄（マウス／タッチ端末／出る内容）が並び、マウスとタッチ端末が
  「どちらか一方の操作」だと一度で読み取れなかった。60 行目は既に
  「マウスを載せると（タッチ端末では押すと）」という括弧の形で書けていたので、
  そちらに揃えた（長すぎる 1 文・指す先の分かりにくさ）
- `README.md:60-66`（デモの速さ・次の解を探すの段落）・ 63 行目だけ 1 行が
  他行（33〜40 字程度）の倍近い長さ（56 字）になっており、折り返しが
  揃っていなかった。文の内容は変えず、段落全体を他の行と同じ幅で
  折り返し直した（形式のみ。事実・文言は変更していない）
- `docs/developer.md:14-16`（画面の用語の冒頭）・「`makeLayout()` が返す配置
  （以下 `LAYOUT` と呼ぶ。実際に export してあるのは…`LAYOUTS`）のキーと、
  画面の部位の対応。」→「`makeLayout()` は、画面の部位ごとの配置を返す
  （以下 `LAYOUT` と呼ぶ。実際に export してあるのは…`LAYOUTS`）。その
  `LAYOUT` のキーと画面の部位の対応を、以下にまとめる。」・ 主語（配置）と
  述語（のキーと画面の部位の対応）の間に、`LAYOUT`/`LAYOUTS` の説明という
  長い挿入句が割り込み、述語まで読んだときに主語を見失う作りだった。
  2 文に分け、挿入句を主語のすぐ後ろに寄せた（事実は変えていない。
  `makeLayout()`・`LAYOUT`・`LAYOUTS`・export の説明はすべてそのまま）

### 見送った箇所（考えたが直さなかったもの）

- `CLAUDE.md:46-51`（confirm/alert/prompt を使わない理由）・「ネイティブ
  ダイアログは…浮くうえ、`confirm()` は同期的にブロッキングするため、…
  止まってしまう」で、主語が「ネイティブダイアログ」から `confirm()` に
  途中で変わる。読みにくさはあるが、`confirm()`/`alert()`/`prompt()` の
  代表として `confirm()` を挙げているだけで、一度読めば趣旨は取れると
  判断し、直さなかった
- `docs/developer.md:52`（一覧の頁送りの説明。「日時と時間は行の左端から、
  印は右端から」で後半の動詞が省略されている）・ 直前に一覧・行の説明が
  あり、レイアウトの話だと分かる文脈なので、直さなかった

### 確認が要る点

特に無し。
