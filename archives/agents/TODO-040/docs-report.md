# TODO-040 文書の整合（docs 担当の報告）

コードは読むだけで変更していない。`src/scenes/demo.js`、`src/logic.js` の
`solveSteps()`、`src/config.js` の `DEMO`、`src/scenes/title.js`・
`src/scenes/game.js`（`createHudButtons()`）を読んで確認した。

## 直した箇所

### README.md
- 新しい節「## デモ」を「## テスト」の前に追加。タイトルの `記録` の横の
  `デモ` ボタン、速さ 3 段階（ゆっくり／速い／最速）、`次の解を探す`、
  記録・遊びかけ・見つけた解には残らないことを書いた。TODO 番号は書いていない
  （利用者向け文書のため）
- 「## 構成」のツリー: `ui.js ボタンと枠（4 つのシーンで共通）` →
  `（5 つのシーンで共通）`。`scenes/` の一覧に `demo.js` の行を追加

### docs/developer.md
- 「画面の用語」に「デモの画面（`src/scenes/demo.js`。TODO-040）で使う
  呼び名」の表を追加（記録の画面の表のあと）。**速さ**（ゆっくり・速い・
  最速）、**次の解を探す**、試した手／見つけた解 の 3 行

### CLAUDE.md
- 「ファイル構成」表: `src/logic.js` の説明の末尾に
  「デモ用に 1 手ずつ返す探索 `solveSteps()`（TODO-040）」を追加
- 同表: `src/ui.js` の説明「4 つのシーンが同じ見た目を使うため」→
  「5 つのシーンが同じ見た目を使うため」
- 同表に `src/scenes/demo.js` の行を `records.js` の下に追加
  （`GameScene` を継承すること、記録・遊びかけ・見つけた解には残さないこと）

## 直さなかった食い違い（コード側。今回の範囲外）

- `src/ui.js` 冒頭のコメント「タイトル・本編・クリア・記録の 4 シーンが
  同じ見た目のボタンを使うので」が、今は `demo.js` も使っており実際は
  5 シーン。`src/**` は読むだけの指示のため直していない
  （`CLAUDE.md` 側の同じ数字は 5 に直した）

## 確かめて食い違っていなかったもの

- `docs/developer.md` の HUD の表（本編の HUD、ボタン 6 個）はデモ画面と
  別物で、デモの追加とは無関係。変更していない
- CLAUDE.md・README.md の内部解像度（960×640）、盤の解の数（65／2339）など、
  依頼にあった「数」の項目はデモ機能と直接関係する記述が無く、既存の値も
  コードと合っていた
