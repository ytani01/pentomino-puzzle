# TODO-089 レビュー報告（reviewer）

対象: `git diff`（未コミット、7 ファイル）。コードは直していない。

配置の数値は `makeLayout()` を Node から呼んで出した
（`tools/window-shim.mjs` を先に読み込み、変更前の値は一時的に `git stash` して
同じスクリプトで出し、すぐ `git stash pop` で戻した。作業ツリーは元どおり）。

| 画面 | 盤 | 本編 cell（前→後） | デモ cell（前→後） | 盤の枠の y（本編 / デモ、後） |
|---|---|---|---|---|
| 横 | 8×8 | 52 → 49 | 52 → **46** | 185 / 210 |
| 横 | 6×10 | 64 → 64 | 64 → **62** | 189 / 208 |
| 縦 | 8×8 | 74 → 74 | 74 → 74 | 182 / 208 |
| 縦 | 6×10 | 59 → 59 | 59 → 59 | 182 / 208 |

## 要修正

### 1. 横画面のデモで、マス目のテクスチャが作られていない大きさを使う

- 場所: `src/config.js:486-490`（`DEMO_LAYOUTS` に `note: true`）と
  `src/scenes/boot.js:58-63`（`LAYOUTS` だけを回して焼く）
- 内容: `note` で HUD が 26 px 下がると、横画面では盤に使える高さが減り、
  デモのマスが本編より小さくなる（上の表: 8×8 は 49 と 46、6×10 は 64 と 62）。
  Boot が焼くのは `LAYOUTS`（本編）の大きさだけなので、デモが引く
  `TEX.boardCell(46)`・`TEX.ghost(46)`・`TEX.piece(…, 46)`（6×10 は 62）は
  存在しない。`game.js:164/186/216` はこのキーで `add.image` するので、
  横画面のデモの盤とピースが Phaser の欠けたテクスチャで出るはず
- 根拠: 実測（上の表）と、`boot.js:58`・`game.js:164,186,216`・`boot.js:27-31` を読んだ結果。
  変更前は `DEMO_LAYOUTS` の JSDoc に「マスの大きさは本編と同じになる」とあり、
  Boot はその前提で本編の大きさだけを焼いていた（今回その一文が消えている）
- 実画面での見え方は未確認（撮影の担当の範囲）。縦画面は cell が変わらないので起きない

## 検討

### 2. 文書と JSDoc の「1 行ぶん下がる」は、横画面では盤が小さくもなる

- 場所: `docs/developer.md:236`（「そのぶん盤とトレイが本編より 1 行下がる」）、
  `src/config.js:481-484`（`DEMO_LAYOUTS` の JSDoc）
- 内容: 横画面では下がるだけでなく、マスも小さくなる（上の表）。
  要修正 1 の直し方次第で記述が変わるので、そちらと合わせて見直す
- 同じく `src/config.js:339-340` のコメント「本編だけ段が少ないと、デモの盤と
  トレイだけが下へずれるため」は、今回デモが意図してずれるようになったので、
  理由の書き方が今の作りと合わなくなりかけている（段数を揃える理由としてはまだ有効）

### 3. 本編の横画面 8×8 で、盤のマスが 52 から 49 に縮む

- 場所: `src/config.js:242`（`TITLE_BAND = 34`。旧 `HUD_TOP = 10` から +24）
- 内容: TODO-089 の本文にある「スマホ横画面で盤・トレイが縦に詰まらないか」に
  当たる変化。6×10 と縦画面は変わらない。挙動の変化として把握しておくだけで、
  良し悪しの判断は撮影の結果を見て管理者がする（境界線上の判断なので報告だけ）

### 4. 記録の画面の横画面で、タイトル行の当たり判定が見出し「記録」の上端に被る

- 場所: `src/scenes/records.js:88-89`（`titleY: 16`・`headingY: 48`）、`src/ui.js:39`（`padding: { x: 24, y: 6 }`）
- 内容: タイトル行は FONT.small 20 px に上下 6 px の padding で、おおよそ y = -2〜34 が
  当たり判定。見出し「記録」（36 px、中央 48）の文字の枠はおおよそ 26〜70 で、どちらも
  画面の中央に置くので横も重なる。見出しの上端付近を押すとタイトルへ戻る。
  見出しは押せる部品ではないので実害は小さいと思われるが、未確認
- 縦画面は titleY 22（判定 4〜40）・headingY 64（文字の枠 42〜86）で重ならない
- 左上の「タイトルへ」ボタン（x 中心 32）とは横で離れているので重ならない

### 5. 記録の画面の横画面で、一覧の最後の行と下段のボタンの間が 6 px

- 場所: `src/scenes/records.js:93-95,99`（`listTop: 176`・`rowsPerPage: 8`・`footY: 600`）
- 内容: 一覧の下端は 176 + 8 × 50 − 6 = 570、下段のボタン（高さ 48）の上端は 576。
  変更前は 564 と 576 で 12 px。重なりはしないが詰まって見える可能性がある。実害は未確認
- そのほかの縦の並び（見出し 26〜70、盤の選択 79〜125、全部選ぶ 124〜156、
  完成形の枠 152〜426、detailY 455、この回を続ける 486〜534）は重ならない
  （全部選ぶと盤の選択は 1 px 重なるが、変更前も同じ間隔）

### 6. `tools/capture.mjs` の決め打ちの数値と、`docs/images/` の画像が古くなる

- 場所: `tools/capture.mjs:241`（完成形を `detailText.y - 309`・高さ 280 で指す）
- 内容: `boardBox` を y 146→152、高さ 280→274 に変えたので、吹き出し E の枠が
  完成形から数 px ずれる可能性がある（ponytail コメントに「配置を変えたら合わせ直す」とある）。
  本編の A・B（`side: 'top'`）も HUD が 24 px 下がったので、吹き出しがタイトル行に
  重なる可能性がある。いずれも未確認
- `docs/images/` の game.png・records.png・デモの画像はタイトル行が無いまま。
  撮り直すかどうかは TODO-089 の 3 項目に含まれていないので、範囲の判断が要る

### 7. UsersGuide のデモの節に、タイトル行と探し方の一文の記述が無い

- 場所: `docs/UsersGuide.md:150-225`（デモの節）
- 内容: 「HUD のボタン」の節（52-53 行）に「記録・デモの画面でも同じ」とまとめて
  書いてあるので足りるとも読めるが、記録の節（138-139 行）には書き足してあり、
  デモの節だけ書いていない。探し方の一文も文書に出てこない
- あわせて 52-53 行の「記録・デモの画面でも同じで、そちらは確認なしで戻る」は、
  「同じ」と「確認なし」が並んでいて読みにくい（好みの範囲に近い）

## 問題無い点

- TODO-089 の 3 項目: 3 画面にタイトル行、本編は `confirmToTitle()`・HUD の「タイトルへ」は残る、デモに探し方の一文。いずれも差分に入っている
- 押下の扱い: `pointerdown` でドラッグ中なら押したことにせず、`pointerup` でも `scene.drag` を見る。ピースを持って離した位置がタイトル行でも、押し始めがタイトル行でないので動かない。2 本目の指も同じ。`createButton()` と同じ考え方
- 確認の枠が出ている間: 本編の背景の帯は depth 40（`DEPTH.confirm`）、タイトル行は 30、記録は帯 10・タイトル行 0。どちらも `topOnly` で帯が受けるので抜けない
- クリア表示中: Game は `scene.pause()`（`game.js:1347`）されるので入力を受けない
- 本編 → `confirmToTitle()`、デモ → 上書きした `goToTitle()`（URL の `demo`・`board` を消す）、記録 → 新設の `goToTitle()`（左上のボタンも同じ関数に寄せた）を通る。デモの `createHud()` は `super` を呼ばないのでタイトル行が 2 重にならない
- 規約: `setTimeout` なし。色は `TEXT_COLORS` から。状態は `createTitleBar()` の呼び出しごとのクロージャでモジュールには持たない。JSDoc は「なぜ」を書いている
- `clear.js` は `SCREEN` だけを読み、LAYOUTS を読まないのでずれない。`records.js` の `CONFIRM` は寸法だけで位置に依らない
- 範囲外の変更は無い

## 作り込みすぎ

作り込みすぎ: なし（`createTitleBar()` は 3 画面で使い、押下の扱いも最小限。
`layout.title`・`layout.note` は数値 1 つを包むだけだが、既存の `message: { x, y }` と揃っているので残してよい）
