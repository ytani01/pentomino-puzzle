# TODO-050 screens への依頼

デモの HUD に探し方の切り替えボタン（7 つ目）が足された。はみ出し・重なりが無いかを撮って確かめる。

条件: http://localhost:8765/（サーバが無ければ `python3 -m http.server 8765` を立てる）。
画面サイズは 1280×720（PC 横）、844×390（スマホ横）、390×844（スマホ縦）の 3 つ。盤は 8×8 と 6×10 のうち 6×10 だけでよい。
撮るもの: 各サイズで (a) デモ画面 1 枚、(b) 切り替えボタンを 1 回押したあとの 1 枚（アイコンが変わり、ピースがトレイに戻って探し直していること）、
(c) 本編（ゲーム画面）1 枚（HUD のボタンが 6 つで以前どおり並んでいること）。
ボタンのツールチップは 1 つ（切り替えボタン）だけ出して見る。ほかのボタンは押さない・載せない。
見ること: ボタンが HUD の枠からはみ出さないか、重ならないか、アイコンが欠けていないか、盤・トレイと重ならないか。デザインの良し悪しは評価しない。
画像は ~/tmp/playwright-mcp/ に置く。報告は archives/agents/TODO-050/screens-report.md（画像のパスと、問題の有無。問題なしは 1 行）。返事は 5 行以内。
