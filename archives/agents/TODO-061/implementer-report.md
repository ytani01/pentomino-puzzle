# TODO-061 implementer 報告

## 変更したファイル

- `src/logic.js`
  - `countCellMoves(choices)`（新規 export、370 行台）: 空きマスごとに、それを
    覆う手の数を数える純関数。
  - `moveCoversCell()`・`pickTightCell()`（新規、非 export のヘルパー）: 「狭い所」
    （置ける手が一番少ないマス。数が同じなら `random` で 1 つ選ぶ）を求める。
  - `solveStepsRandom()` 内のピース抽選（旧 `pickOne(choices)` の分岐）を、
    狭い所を覆える手を持つピースへ `DEMO.randomTightWeight` を掛けた重み付き
    抽選に変更。選んだピースが狭い所を覆えるなら、置き方をその手だけに絞って
    から従来の `touchWeight` / `moveDistance` の抽選へ渡す（`pickWeighted` の
    利用箇所）。
  - `solveStepsRandom()` の JSDoc を今の動きに合わせて書き直し（狭い所の説明を
    追加し、「ピースの選び方には掛けない」の一文を削除）。
- `src/config.js`
  - `DEMO.randomTightWeight = 4`（仮値）を追加し、JSDoc の箇条書きに説明を追加。
- `docs/UsersGuide.md`
  - ランダムの説明を「狭い所を覆えるピースが選ばれやすい」「選んだら置き場所も
    狭い所に絞る」に書き換え。「ピースは近さに関係なく選ぶので、遠くへ置くことも
    ある」の一文は今の動きと合わなくなったため削除。
- `docs/developer.md`
  - 探し方の表のランダムの説明に、ピース選択の重み付け（`countCellMoves()`・
    `DEMO.randomTightWeight`）を追記。

## 検証

- `node tools/gen-solutions.mjs --check` → 成功（終了コード 0）。
- `tests.html` を Playwright の新しいコンテキストで
  `python3 -m http.server 8765` 経由で開いた結果: **325 件通過、2 件失敗**。
  - NG: 「残りのピースで埋められる 5 マスの閉じた空きがあれば、次の place は
    必ずそれになる（TODO-066, 8×8）」 / why: 「選んだシード・手数で、合う手の
    ある 5 マスの穴が一度も出なかった」
  - NG: 「5 マスの穴に合う手を埋めた直後に解なしになると、その手と直前の手を
    まとめて外す（TODO-066, 8×8）」 / why: 「選んだシード・手数で forced な
    解なしの二重 remove が一度も起きなかった」
  - 原因の見立ては書かない（依頼どおり）。ピース選択の前に `countCellMoves` /
    `pickTightCell` が `random()` を追加で消費するようになったため、以降の
    乱数列全体がずれ、固定シードのテストが前提にしていた手順（forced な穴が
    出る手数）と食い違っている可能性はあるが、確かめていない。
- 実測（固定シード、`hasSolution` を `canContinue` に渡し、8×8・6×10 それぞれ
  約 3000 手超まで seed を変えながら周回。forced な手は抽選を通らないので集計
  から除外）:
  - 8×8: 3505 手中 30 手が forced、残り 3475 手のうち 30.9% が「狭い所」を覆う
    手を選んだ。1 手あたりの数える処理の平均は約 0.31ms。
  - 6×10: 3248 手中 16 手が forced、残り 3232 手のうち 33.9% が「狭い所」を覆う
    手を選んだ。1 手あたり約 0.26ms。
  - デモは最速で 1 フレーム 1 手（目安 16ms）なので、数える処理はその 1/50
    程度で収まっている。
  - 測定に使ったスクリプトはスクラッチパッド（セッション限りの一時ディレクトリ）
    に置いたのみで、リポジトリには残していない。

## 判断が要る点・気づいたこと

- `tests.html` の 2 件の失敗は、依頼どおり原因を直さず・見立ても書かずに
  一覧だけ報告した。固定シードに依存したテストなので、抽選の乱数消費が
  増えたことが影響している可能性はあるが未確認。
- `randomTightWeight` の値は依頼どおり仮で `4`（`randomTightWeight` は
  `touchWeight` 等と単位が異なる「ピース抽選の重み」なので、値そのものは
  画面で見て利用者が決める前提）。
