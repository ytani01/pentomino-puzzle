# TODO-061 reviewer 報告

対象: `git diff`（`src/logic.js`・`src/config.js`・`tests.html`・`docs/UsersGuide.md`・`docs/developer.md`）。
測定用スクリプトは scratchpad（`perf.mjs`・`seeds.mjs`・`forced.mjs`）に置いただけで、リポジトリには残していない。

要修正: 0 件。

## 検討

### 1. `tests.html:1578`・`tests.html:1629` — 直した TODO-066 の 2 件が、最初の 1 件を確かめたところで止まる

- 問題: 前は 1 シード 4000 手（3000 手）の中で起きた場面を**全部**確かめていたが、今は最初の 1 件で `break` し、後の場面は見ない。
  テストの名前は「次の place は**必ず**それになる」なのに、確かめるのは 1 回だけになった。
- 根拠（実測。seeds 1〜5 × 1200 手、`regionsFitPieces`）: 5 マスの穴に合う手がある場面は 8×8 で 13 回、6×10 で 1 回あった。
  13 回とも違反は無い（テストが今捕まえるのは 1 回目だけ）。
  6×10 は 5 シードで 1 回しか出ないので、`SEED_COUNT = 5` だと余裕が小さい（tests の報告どおり 10 組では出ている）。
- どうすればよいか: 場面を見つけても `break` せず、そのシードの残り（とシードの残り）も見続ける。`sawForced` は
  「1 度でも見たか」を記録するだけにする。手間はほとんど増えない。

### 2. `src/logic.js:679` — 狭い所を数えるとき、控え（`failed`）で除いた手の分だけ狭く見える（実害は未確認）

- 問題: `countCellMoves(choices)` の `choices` は `failed` を除いた後の手なので、外した手が覆っていたマスの数が減り、そこが「狭い所」に選ばれやすい。
  外した手のすぐ近くに、別のピースで戻ってくる動きになる。
- 根拠（実測。seeds 1〜5 × 1200 手、forced を除いた place）: 8×8 では 3008 手のうち 2843 手で今の盤面の控えが空でなく、
  そのうち 2656 手（93%）で、控えを除く/除かないで狭い所（同数を含めた最小の組）が変わった。6×10 も 3020 手中 2813 手・2567 手で同じ傾向。
  **影響はほぼ毎手ある。** ただ、控えにある手は実際に置けないので、「置ける手の少ないマス」（利用者が決めた測り方）としてはこちらが字義どおり。
  「同じ隙間に何度も挑む」ように見えるのが良いか悪いかは画面を見ないと分からない。
- どうすればよいか: 画面で見てから利用者が決める。控えを数に入れないなら、`countCellMoves()` に渡すものを控えを除く前の手にする。

### 3. `src/logic.js:446-457` — 覆える手が 0 のマス（もう埋まらないマス）は「狭い所」にならない（実害は未確認）

- 問題: `counts` は覆われたマスしか持たないので、どのピースも入らない空きマスがあっても、それを飛ばして 1 以上で一番少ないマスを狙う。
  人が「ここにはもう何も入らない」と気づく場所は狙われない。
- 根拠: コードを読んで確認（JSDoc にも「数が 1 以上」と書いてある）。そういうマスがあるとき盤面はすでに解なしだが、
  TODO-059 の決めごと（置ける手が尽きるまで置き続ける）どおりで、今回の変更で悪くなったわけではない。
- どうすればよいか: 仕様どおりなら何もしない。気になるなら TODO-063（詰まったらまとめて崩す）の調べのときに合わせて見る。

## 好みの範囲

### 4. `tests.html:1945`・`tests.html:1900 付近` — 手を控えつつたどるループが 1 つ増えた

`failedByBoard` / `failedOf` / `activeStack` で控えを再現しながら手をたどるループは、既存のテストに何か所もあり
（`tests.html:1355` 付近、`1436`、`1517`）、今回 `sumTightCoverage()` と「唯一絞れる」テストで 2 つ増えた。
既存の書き方に合わせたもので規約違反ではない。まとめるなら別の項目にする。

## 問題の無かった観点

- 仕様どおりか: 狭い所 = 置ける手の数が最小のマス（同数は `random` で 1 つ）、覆える手を持つピースに `DEMO.randomTightWeight`、選んだら覆う手に絞る。利用者が決めたとおり。
- `randomTightWeight = 1` でも絞り込みは残る（重みだけが消える）。比べるテストはこの前提で組んであり、意味は合っている。
- TODO-059・062・066〜068: forced の分岐（`pickOne(forced)`）は変わっていない。外す判定（`undoLast` の 3 分岐）も変わっていない。近さの重み（TODO-062）は絞った後の置き方にそのまま掛かる。seeds 1〜5 × 1200 手で forced の手の違反は 0 件（上の 1）。
- 乱数: 新しく増えた消費は `pickTightCell()` と `pickWeighted()` の `random()` だけ。`Math.random` は使っていない。
- 規約: `setTimeout` 無し、トップレベルの `let` 無し、値は `config.js`、Phaser/DOM の持ち込み無し、盤面は書き換えていない（`countCellMoves` は `choices` を読むだけ）。
- JSDoc・文書: `solveStepsRandom()` の JSDoc から「ピースの選び方には掛けない」を消し、`UsersGuide.md` からも「ピースは近さに関係なく選ぶ」を消してある。`rg "近さに関係なく"` は 0 件。`config.js`・`developer.md` の説明も今の動きと合う。コメントは「なぜ」（人は隙間に入るピースを探す）を書いている。
- テスト（新しい 6 件）: シードの組をずらしても通る。「唯一絞れる」テストを seeds 100〜119 × 1500 手で再現すると、8×8 で 6254 件・6×10 で 6735 件確かめて違反 0、確かめる場面が 0 件のシードも無い。比べるテストを seeds 100〜104・200〜204…1000〜1004 の 10 組で再現すると、全組で重みありの方が大きい（8×8 は 0.718〜0.800 対 0.439〜0.592、6×10 は 0.748〜0.849 対 0.503〜0.560）。差が大きいので不安定にはなりにくい。
- 壊すと落ちるか: tests の報告（重みを 1 にする → 比べるテストが落ちる、絞り込みを外す → 唯一絞れるテストが落ちる）で、2 つの変更のどちらも捕まえられる。
- 1 手あたりの重さ（実測。seeds 1〜5 × 2000 手、place と remove を合わせた 1 手の平均）: 変更前 8×8 0.197ms・6×10 0.172ms、変更後 0.140ms・0.105ms。数える処理が増えても 1 手の時間は伸びていない（盤の埋まり方が変わって、1 手ごとに作る手の数が減ったためと思われるが未確認）。最速の 1 フレーム（16ms）に比べて十分小さい。
- 範囲: 指示に無い変更は無い。

## 作り込みすぎ

- `src/logic.js:457`・`681`・`686`: delete: `choices` が空でないときしか通らないので `counts` も空にならず、`tightCell` は null にならない（コメントにも「ここには来ない」とある）。`keys.length > 0 ? … : null` と `tightCell !== null` の 2 か所を消し、コメントの該当の文も消せる。重大度: 好みの範囲。
- `src/logic.js:680-689`: shrink: 同じピースの手を `some(moveCoversCell)` と `filter(moveCoversCell)` で 2 回なめている。`const covering = choices.map((ms) => ms.filter((m) => moveCoversCell(m, tightCell)))` を先に作り、重みは `covering[i].length > 0 ? W : 1`、置き方は `covering[i]` が空でなければそれ、にすると 1 回で済む（`pickWeighted` は添字の配列に掛ける）。行数はほぼ同じなので、好みの範囲。
- `tests.html:1987`: `let withWeight` / `let withoutWeight` は代入し直さないので `const` でよい。好みの範囲。

net: -5 lines possible.
