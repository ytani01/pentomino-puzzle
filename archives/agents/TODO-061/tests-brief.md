# TODO-061 tests への依頼

## 背景

`solveStepsRandom()` のピースの選び方を変えた（`git diff src/logic.js src/config.js`）。空きマスごとに覆える手の数を数え
（`countCellMoves()`）、一番少ないマス（狭い所）を覆えるピースを `DEMO.randomTightWeight` で選ばれやすくし、選んだら狭い所を
覆う置き方に絞る。実装担当の報告: `archives/agents/TODO-061/implementer-report.md`（落ちた既存 2 件、実測の割合）。

## やること

1. **落ちた既存 2 件を直す**（TODO-066 の 8×8 の 2 件。乱数の使い方が変わり、固定シードでその場面が出なくなった）。
   シードを 1 つ選び直して通すのではなく、複数のシードを回して場面が出るまで集める形など、手順が変わっても場面が出る形にする。
   直したあと、シードの組を 10 通りずらしても場面が出ることを Node で確かめる。1 件あたり 1 秒以内
2. **足す**:
   - `countCellMoves()`: 小さな盤面で、手で数えた値と一致する
   - 狭い所が効いていること: 抽選の place（forced を除く）のうち狭い所を覆った割合が、`DEMO.randomTightWeight` を 1 にしたときより
     大きい（`DEMO` は finally で戻す）。8×8・6×10 とも、シードの組を 10 通りずらして 10/10 で通ることを Node で確かめる。1 盤 1 秒以内。
     「狭い所」をテスト側で数え直す必要があるなら、`countCellMoves()` を使ってよい
3. 壊すと落ちるか: (a) ピースの重みを常に 1 にする、(b) 狭い所を覆う手への絞り込みを外す、のそれぞれで、2 の比べるテストか
   別のテストが落ちることを確かめて戻す。(b) で落ちるテストが無ければ、絞り込みを確かめるテストを足す
   （狭い所を覆えるピースを選んだ place は、必ず狭い所を覆う、など）
4. 手数は増やさない

## 保つもの

- `src/` は触らない。既存テストの狙いを弱めない。既存の書き方に合わせる
- ブラウザで確かめるときは Playwright の新しいコンテキストで開く（モジュールのキャッシュを持ち越さない）

## 完了条件

`python3 -m http.server 8765` 経由で `tests.html` の全件が通る（件数を報告）。

## 報告

`archives/agents/TODO-061/tests-report.md`（10 組の結果も載せる）。返事は 5 行以内。
