# TODO-068 implementer 報告

## 変更したファイル

- `src/logic.js:652-656`（`solveStepsRandom()`）: TODO-060 の分岐の条件を
  `emptyRegionSizes(board).some((size) => size < PIECE_SIZE)` から
  `!regionsFitPieces(board)` に変更。5 の倍数でない閉じた空き全般（7・12 マスなど）
  で置いた直後にその場で 1 手だけ外すようにした。分岐の並び（TODO-066 の
  `forced && !ok` が先、TODO-067 が後）は変えていない。
- `src/logic.js:526-535`（`solveStepsRandom()` の JSDoc）: 「7 や 12 マスのような
  大きい空きは今までどおり行き詰まってから戻す」の一文を削除し、条件を
  「5 の倍数でない大きさの閉じた空き」に書き換え。
- `src/logic.js:569`: コメント末尾に `TODO-068` を追記。
- `docs/UsersGuide.md:117-118`: 「ピースより小さい閉じた空き」→
  「5 の倍数でない大きさの閉じた空き」。
- `docs/developer.md:239`: 同様に「ピースより小さい閉じた空き」→
  「5 の倍数でない大きさの閉じた空き」、TODO 参照に `TODO-068` を追記。
- `src/scenes/demo.js:19-20`: 同様の言い換えと TODO-068 の追記。

`solveSteps()`（深さ優先）・乱数の使い方・TODO-059/062/066/067 の動きは
変更していない。

## 検証

- `node tools/gen-solutions.mjs --check`: 通った（終了コード 0）。
- `python3 -m http.server 8765` 経由で `tests.html` を Playwright（npx が
  持ってきたものをパスで借用、`tools/capture.mjs` と同じ手法）で開いて確認。
  **変更前後で同じ 7 件が失敗**（320 件は通過。今回の変更による新規の失敗は無い）。
  失敗した項目（すべて「常に偽を返す判定」や「即座には外さない」を確かめる
  既存のテストで、`小さな空きで即座に外れる 1 手は除く` という注記付きの
  ものが多い。TODO-068 でこの「即座に外れる」対象が広がったため、注記の
  除外条件の想定と一致しなくなっている可能性がある。原因の切り分けはしていない）:
  1. remove の連なり: 途中の remove が続かない（8×8、P）
  2. remove の連なりの終わりで盤にピースが残っている（期待 0 / 実際 3、8×8）
  3. ok:false の place の直後に別の place が一度も出なかった（8×8）
  4. remove の連なり: 途中の remove が続かない（6×10、X）
  5. remove の連なりの終わりで盤にピースが残っている（期待 0 / 実際 2、6×10）
  6. ok:false の place の直後に別の place が一度も出なかった（6×10）
  7. randomNearPower を効かせた平均距離が、効かせないときより小さくなっていない（6×10）
- 実測（固定シード `mulberry32(12345)`、`regionsFitPieces` を `canContinue` に渡して
  8000 手ずつ回した）: この分岐（`!regionsFitPieces(board)` によるその場外し）が
  発火した回数のうち、外れた空きの大きさが 5 マスより大きかった回数は
  8×8 で 3486 回中 1051 回、6×10 で 3472 回中 2910 回。7・12・27・39 マスなど
  5 の倍数でない大きい空きでも発火していることを確認した。計測用のログは
  検証後に元へ戻し、`git diff` で差分が残っていないことを確認済み。

## 判断が要る点・懸念

- `tests.html` の失敗 7 件は変更前から失敗しており、今回の変更が原因ではない
  （変更前後で同一）。ただし 1〜6 番目は「小さな空きで即座に外れる 1 手は除く」
  という既存の除外条件に関わるテストで、TODO-068 により「即座に外れる」対象が
  広がったことがテストの前提とずれている可能性がある。原因の切り分けは
  行っていない（依頼の範囲外）。
