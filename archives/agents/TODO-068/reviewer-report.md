# TODO-068 reviewer 報告

対象: `git diff`（未コミット、5 ファイル）。実測は Node（`tools/window-shim.mjs` を先に読み、
`src/` を直接 import）で、`tests.html` と同じ `mulberry32`・`FAST_SEED` を使った。
計測スクリプトは scratchpad に置いた（リポジトリには残していない）。

## 要修正

### 1. `tests.html:1739-1752` 近さのテスト（6×10）は、`randomNearPower` 12 をシード 1〜5 に合わせて通しているだけ

- 問題: シードの組を変えると 6×10 で逆転する。値を 8→12 に上げても、狙う性質を
  安定して捉えていない。コメントの「差をはっきりさせ」は 6×10 では成り立たない
- 根拠（実測。テストと同じ 5 シード × 300 手を、シードの組を 10 通りずらして平均距離を比べた）:

  | 6×10 | p0 より小さい組の数（10 組中） |
  |---|---|
  | 変更後、power 8 | 6 |
  | 変更後、power 12 | 7（シード 6-10・21-25・41-45 で逆転） |
  | 変更前（HEAD の `logic.js`）、power 8 | 9 |
  | 変更前、power 12 | 10 |

  テストが使うシード 1-5 でも、変更後の差は 2.508 と 2.769 で小さい。8×8 は変更後でも
  10 組すべてで p8・p12 とも p0 より小さかった。20 シード × 1000 手に増やしても、6×10 の
  差は 2.545 と 2.732、3.037 と 3.133 にとどまった
- どうすればよいか: 値を上げて通すのではなく、別のシードの組でも通るかを確かめてから
  決める（シード数か手数を増やす、測る対象を絞る、など。どれにするかは管理者の判断）。
  シードは固定なので flaky にはならないが、手順が少し変わるたびに今回のように
  逆転する
- 補足（実害は未確認）: 変更前後で 6×10 の近さの効き方が弱まっている
  （上の表の 9→6）。TODO-062 の効き方が変わったかどうかは値の良し悪しの話なので、
  評価はしていない

## 検討

### 2. 既定の `canContinue`（`regionsFitPieces`）では、TODO-066 の 2 手外しと「ok が真になるまで外す」流れが起きなくなった

- 問題: 既定の判定では `ok:false` の place がすべて置いた直後に外れる。スタックに残る
  盤面は必ず `regionsFitPieces` が真なので、forced な手を埋めても偽にならず、
  外した後の `ok` も常に真になる
- 根拠（実測、8000 手、`FAST_SEED`）:

  | | ok:false の place | うち forced（TODO-066） | ok:false の remove | ok:false の直後の place |
  |---|---|---|---|---|
  | 8×8 既定 | 3639 | 0 | 0 | 0 |
  | 6×10 既定 | 3566 | 0 | 0 | 0 |
  | 8×8 `hasSolution` | 1011 | 16 | 940 | 91 |
  | 6×10 `hasSolution` | 2879 | 33 | 2750 | 284 |

  デモは `hasSolution` を渡す（`src/scenes/demo.js:297`）ので、製品のコードでは
  どちらの分岐も生きている。既定値を使っている呼び出し元は無い
- 影響するもの:
  - `src/logic.js:519-523` の JSDoc「`canContinue(board)` が偽でも**その場では外さない**」は、
    既定の判定ではもう当てはまらない。既定では偽の place は必ずその場で外れる、と
    1 行足すかどうか
  - `tests.html:1188` の「remove の連なり」テストの `既定` の分は、連なりの途中が
    ok:false になる場面が無くなった（途中の remove を確かめる部分が空回りする）。
    TODO-068 の前は、7 マスの空きなどでこの部分を通っていた。連なりが
    「ok:false を何手か外したあと、ok:true で止まる」ことを確かめるテストは、
    これで無くなった（`常に偽` は止まらない側しか見ない）。`既定` の分を
    `hasSolution` に差し替えるかどうか（実害は未確認）

### 3. `tests.html:1459`・`1504` TODO-067 のテストの絞り込みが、古い条件（1〜4 マスだけ）のまま

- 問題: `hasExactOnly` は `!sizes.some((size) => size < PIECE_SIZE)` で、5 マスの空きと
  7 マスなどの空きが同時にある盤面も拾う。そうした盤面で直後に外れるのは TODO-068 の分岐で、
  TODO-067 の分岐ではない。コメント（1459 行）も「即座に外すのは PIECE_SIZE 未満だけだった」
- 根拠（実測、6000 手。`hasUnfailedMove` の絞り込みは掛けずに数えた）: 拾う盤面のうち
  5 の倍数でない空きも含むものは、8×8 で 71 件中 0 件、6×10 で 164 件中 30 件
- 残りが 134 件あるので、TODO-067 の分岐を消せばまだ落ちるはず（未確認）。条件を
  `regionsFitPieces(board)` に揃え、コメントも今の条件に直すかどうか

### 4. `tests.html:1331-1333` のコメントが、今の既定の判定での挙動と合わない（差分の外）

- 「置いた手は ok の真偽によらずいったん stack に積まれ（即座には外さない）」とあるが、
  このテストは `regionsFitPieces` を渡しており、上の 2 のとおり ok:false の place は
  すべてその場で外れる。TODO-060 の時点ですでにずれていたが、TODO-068 で
  「ok:false なら必ずその場で外す」になった

### 5. `src/scenes/demo.js:19` 「5 の倍数でない…ときだけは」

- TODO-066・067 でもその場で外すので、「だけ」は正しくない。変更前の「ピースより
  小さい…ときだけは」から残っているずれで、今回の書き換えで直す機会だった。
  `logic.js` の JSDoc と `docs/` はすでに 3 つを並べている

## 好みの範囲

### 6. `src/logic.js:569` `undoLast` のコメントに `TODO-068` を足した

- `undoLast` は TODO-068 で変わっていない。参照を足す理由が無い

## 作り込みすぎ

- `tests.html:1093-1096` shrink（好みの範囲）: `hasUnfitClosedRegion(board)` は
  `!regionsFitPieces(board)` を包むだけの 1 行の関数（呼び出しは 2 か所）。
  直接 `!regionsFitPieces(board)` と書けば消せる。隣の `hasPlacedFormClosedRegion` と
  名前を揃える意味はあるので、残すのも可
- net: -4 lines possible.

## 問題の無い観点

- 分岐の並び（066 → 068 → 067）: 仕様どおり。066 が先なので、forced な解なしが 1 手外しに
  すり替わることはない。`!regionsFitPieces` が偽なら `hasSolution` も必ず偽なので、
  068 の分岐が ok:true の手を外すことはない
- TODO-059・062・066・067 の分岐そのもの: `hasSolution` のもとでは、ok:false の直後の place、
  forced な解なしの 2 手外し、ok:false の remove の連なりがいずれも起きている（上の表）
- `docs/UsersGuide.md`・`docs/developer.md`・`logic.js` の JSDoc: 古い言い方（ピースより
  小さい空きだけ、大きい空きは行き詰まってから戻す）は残っていない（`rg` で確認。
  残りは上の 5 と `TODO.md` の背景だけ）
- 「ok:false の place の直後に別の place」のテストを `hasSolution` に差し替えた件: 妥当。
  デモと同じ判定で、ok:false の直後の place は 91 件・284 件起きている。
  その場で外すようにすれば 0 件になって落ちる
- 常に偽のテストと remove の連なりのテストで、除外条件を `!regionsFitPieces` にした件:
  本体の条件と一致している
- 規約（`setTimeout`・トップレベルの `let`・色の直書き・`logic.js` への Phaser 持ち込み）:
  違反なし
- 範囲: 指示に無い変更は無い
