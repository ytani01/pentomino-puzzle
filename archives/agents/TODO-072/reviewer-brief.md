# TODO-072 reviewer への依頼

## 対象

`git diff`（未コミット）。仕様は `TODO.md` の TODO-072 の節（チェックボックスがすべて）。報告: `implementer-report.md`（決めたこと・違えた点）、
`tests-report.md`、`docs-report.md`。実測スクリプト: `archives/agents/TODO-072/check.mjs`。

## 見てほしいこと

- 記録の更新（`storage.js` の `recordClear()` など）: 新しい解／成績がよい／よくない の分け方、成績の比べ方（印の無い方を優先。おまかせとヒントは
  区別しない、と implementer が決めた）、同じ解の履歴を 1 件にまとめる場所と保存し直す時、見つけた解・最短時間・おまかせの番号との関係。
  前の形で保存したもの（同じ解が複数ある履歴、完成した盤面を含まない遊びかけ）を読めるか
- 時計の止め方と動かし方（完成で止まり、「続ける」で動き、止めていた間を経過時間に数えない）。盤を変えずに「続ける」を押しても完成と見なさないか。
  ピースを 1 つ外して同じ所に戻したとき（同じ解を作り直す）の扱い
- クリア表示を本編に重ねる作り: 表示中に本編の入力（ドラッグ・HUD のボタン・TODO-069 のドラッグ中の向きの変更）が効いてしまわないか。
  「もう一度」「記録」「タイトルへ」から本編を抜けたとき、止めたシーンや残った表示が無いか。`DemoScene` の解の表示を壊していないか
- 遊びかけの保存: 完成した盤面を保存するようにした結果、つづきからで開いた直後に完成と見なされて表示が出てしまわないか
- 規約: `clear.js` の幕の色 `0x000000` がコードに直接書かれている（docs 担当の指摘）。`addHistory()` の JSDoc が古い（同）。`setTimeout`・`confirm()` を使っていないか
- テスト: 足したテストが、判定の各分岐を壊すと落ちるか（tests の報告を読み、足りなければ Node で確かめる）
- 文書（docs の直し）が今の動きと合うか

## 見なくてよいもの

見た目の良し悪し、全解データ、記録画面の配置（TODO-071 で決めた）。

## 報告

`archives/agents/TODO-072/reviewer-report.md` に、指摘を重い順に（場所・問題・どうすればよいか）。問題の無い観点は 1 行ずつ。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
