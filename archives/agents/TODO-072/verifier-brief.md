# TODO-072 verifier への依頼

## 対象

未コミットの差分（`git diff`）。仕様は `TODO.md` の TODO-072 の節と、レビューのあとで利用者が決めたこと
（ヒント表示の自動配置は、ピースを置いたときだけ走らせる。ヒント表示を入にした瞬間の自動配置は今のまま）。
報告: `implementer-report.md`（末尾が最新）・`tests-report.md`・`docs-report.md`・`reviewer-report.md`。
実測スクリプト `archives/agents/TODO-072/check.mjs` がある。なぞるだけにせず、下の手順を自分で確かめる（使い回してよい）。

## 確かめること（Playwright。npx の置き場から借りる。新しいコンテキスト。844×390。各手順 1 回）

1. `tests.html` の全件（件数・落ちた件数・コンソールエラー）
2. 8×8 を「おまかせ」で完成させる: 時計が止まり、クリア表示が本編の上に重なる（画面は移らない）。表示中はドラッグ・HUD のボタンが効かない
   （1 つ押して盤と `history` が変わらないこと）。HUD に「新しい解（N 番）」。`localStorage` の履歴にその解が 1 件
3. 「続ける」: 時計が止まっていた間を数えずに動き出す（押す前後の経過時間を記録）。ピースを 1 つ外して同じ場所へ戻す→「記録済み」、履歴の件数は変わらない
4. 印の比べ方: `localStorage` に「印なしで 10:00」の履歴を入れた解を、おまかせを使って（印あり）完成させる→上書きされない（「記録済み」）。
   逆に「印ありで 00:05」の履歴の解を、印なしで作ると上書きされる（「記録を更新」）。印なしで完成させるのが難しければ、
   `recordClear` の単体の呼び出し（`evaluate` で import）で代えてよい
5. 完成したまま「タイトルへ」→「つづきから」で開くと、完成した盤面のまま開き、クリア表示は出ない。完成させた解の番号が戻っている
6. 同じ解が 3 件ある履歴を `localStorage` に入れて記録画面を開くと 1 件にまとまっている（成績が一番よいもの）
7. ヒント表示を入にして完成→「続ける」→ピースを外すと、埋め戻されない
8. クリア表示の画面を撮って `~/tmp/playwright-mcp/todo072-overlay.png` に置き、はみ出し・重なり・欠けが無いか見る。デモで解が出たとき、今までどおりの表示か（1 回）

## 見なくてよいもの

見た目の良し悪し、テストの強さ（tests・implementer が壊して確かめた）、全解データ、記録画面の配置。

## 報告

`archives/agents/TODO-072/verifier-report.md`。一致したものは 1 行、食い違いだけ詳しく。測った値を必ず載せる。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
