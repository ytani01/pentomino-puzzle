# TODO-059 tests 報告

## 書き直したテスト（`src/logic.js — ランダムの探索`）

- 「常に偽を返す判定だと、すべての place の直後が同じピースの remove」
  → 「常に偽を返す判定だと、置ける手が尽きるたびに、盤のピースがすべて外れる
  まで remove が続く」に書き直した。新しい動きでは place の直後にすぐ外さない
  ため、旧仕様の前提そのものが崩れている。
- 「『解なし』だった置き方は盤面ごとに覚え、同じ盤面で同じ置き方をもう一度
  place しない」→ 手元の控え（failedByBoard・activeStack）の再現を、
  「置いた手は ok の真偽によらずいったん stack に積み、外すときにだけ
  外した後の盤面へ控える」新しい動きに合わせて簡単にした
  （旧テストにあった「ok:false の place 直後の即時 undo」の特別扱いが不要になった）。
- 「ok は渡した判定の結果と一致する」に remove の ok の確認も足し、
  タイトルへ明記した（依頼の「remove にも ok が付く」を確かめる分。重複を
  避けるため新しいテストにせず既存のループへ足した）。

## 足したテスト

- `src/logic.js — touchingEdges（TODO-059）`（新設の group）
  - 空の盤の角（X を (0,0)）→ 2
  - 盤の外にも穴にも接しない形（8×8 は I を (1,1)、6×10 は F を (1,1)）→ 0
  - 置き済みのピースの隣（I を (0,0) に置いてから同じ I を (0,1)）→ 6
  - 期待値は手で数え、`orientations`/`canPlace` を使ったスクリプトで裏取りした
    （config.js のピースの形・盤の穴の位置を直接見て計算）
- `src/logic.js — 置き方の重み付け（TODO-059）`（新設の group）
  - シード 1〜30 で、最初の place の touchingEdges の平均が、同じピースの
    全置き方の一様平均より大きいことを確認
- `src/logic.js — ランダムの探索` group への追加
  - 「置ける手が尽きて 1 手ずつ外す並び（remove の連なり）は、最後の 1 手だけが
    ok:true か盤が空になっており、途中の remove は ok:false」（既定・常に偽の
    両方）
  - 「ok:false の place の直後に、別の place が続くことがある（即座には外さない）」
  - 「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が
    残っていれば、次は必ず place になる」
    - これは依頼の壊すテスト（`if (ok) break;` を消す）を確かめる過程で追加した。
      「次が place かどうか」だけを見る素朴な判定は、ok:true で止まった直後に
      置ける手が本当に無くて次の連なりがすぐ始まる場面（実際の FAST_SEED で
      普通に起きる）と区別できず、break を消しても検知できなかった。
      「まだ控えていない置ける手が残っている場面」に絞ることで、break を
      消すと確実に落ちるようにした。

## 全件の件数と結果

314 → **316 件すべて通った**（Playwright + `python3 -m http.server 8765`）。

## 壊すと落ちるかの確認（`src/logic.js` を一時的に書き換え、確認後に復元）

1. `touchWeight` を常に `1` を返すようにする
   → 「最初の place は、一様に選ぶより touchingEdges が大きい置き方に偏る
   （8×8 / 6×10）」が落ちる（想定どおり）。
2. place の直後に `ok:false` なら即座に外す（旧の動き）に戻す
   → 「ok:false の place の直後に、別の place が続くことがある」が落ちる
   （想定どおり）。
3. 戻りのループの `if (ok) break;` を消す
   → 最初、素朴な「次が place かどうか」だけの判定では**落ちなかった**
   （314 件すべて通ってしまった）。原因は上に書いたとおり。
   「外す連なりが ok:true で止まったとき、まだ置ける手が残っていれば、次は
   必ず place になる」を追加して確認し直したところ、8×8・6×10 とも
   想定どおり落ちた。

いずれも確認後に `\cp` でバックアップから復元し、`git diff --stat src/logic.js`
が実装の差分（71 insertions, 24 deletions）と一致することを確認した。
