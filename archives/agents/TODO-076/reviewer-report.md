# TODO-076 reviewer 報告

対象: `git diff`（src/config.js, src/scenes/{game,demo,title,clear,records}.js,
docs/UsersGuide.md, docs/developer.md, tools/capture.mjs）。docs/images/ は見ていない。
寸法の値そのものの良し悪しは評価していない。

実測に使ったもの（scratchpad。リポジトリには置いていない）:

- `makeLayout()` を HEAD 版と作業ツリー版の両方で Node から呼び、横・縦 × 8×8・6×10 × ボタン 6・7 個の配置を出した
- ラベルの幅は、headless Chromium（npx の置き場の Playwright）の canvas `measureText` で、
  `FONT.family` と同じフォント指定で測った

## 要修正

### 1. `src/scenes/clear.js:52` クリア表示の「タイトルへ」のラベルがボタン幅より広い（実害は未確認）

- `BUTTONS.width` が 140 から 124 になった。ボタンのラベルは `fontSize: FONT.hud`（26）で描く（`clear.js:198`）
- 実測: `'タイトルへ'` @26px = **130px**（`'もう一度'` 104、`'続ける'` 78）。ボタン幅 124 より 6 広く、
  左右に 3px ずつ枠からはみ出す計算になる。`createButton()` はラベルを縮めない（`ui.js:212-`）
- 前の 140 では 5px ずつ余っていた
- 値は仮と聞いているが、今の値で撮るとこの状態のキャプチャになる。クリア表示は
  `tools/capture.mjs` の撮影対象に無いので、撮り直しの画像では気づけない。
  実際に文字が枠に掛かって見えるかは未撮影（実害は未確認）

## 検討

### 2. `src/config.js:343-350` 本編とデモで HUD の段数が揃うことが、値しだいで崩れる（今の値では揃う。実害は未確認）

- 前の式は縦画面で `Math.ceil(buttons / 2)` として、本編 6 個・デモ 7 個をどちらも 2 段にしていた
  （コメントに「デモの 7 個も 2 段に収め、盤とトレイを本編と同じ位置に保つ」とあった）
- 新しい式は `floor((room + HUD_GAP) / (HUD_BUTTON_MAX + HUD_GAP))`。縦画面は `room` = 592 なので、
  7 個が 1 段に入るのは `HUD_BUTTON_MAX` ≤ 77、6 個なら ≤ 92
- **今の 76 は境目の 1 つ手前。** 利用者が 78〜92 に決めると、本編は 1 段・デモは 6+1 の 2 段になり、
  デモの盤とトレイだけ 68 下がる
- `config.js:487-489` の `DEMO_LAYOUTS` の JSDoc「ボタンの段数は本編と同じなので、盤とトレイの位置・
  マスの大きさは本編と同じになる」が、値を変えるだけで偽になる。値を詰めるのはこれからなので、
  値を決める前に気づけるようにしておくか（テストか、コメントに境目を書くか）は判断が要る

計算結果（作業ツリー。`HUD_BUTTON_MAX` = 76）:

| 向き | 盤 | ボタン | 1 段の個数 | 段数（文字の段込み） | ボタン幅 |
|---|---|---|---|---|---|
| 横 | 8×8 / 6×10 | 6 | 6 | 2 | 76 |
| 横 | 8×8 / 6×10 | 7 | 7 | 2 | 76 |
| 縦 | 8×8 / 6×10 | 6 | 6 | 2 | 76 |
| 縦 | 8×8 / 6×10 | 7 | 7 | 2 | 76 |

1 段に並べた幅は 7 個で 76×7 + 8×6 = 580 ≤ 592（`room`）。はみ出しは無い。

### 3. `src/config.js:343-345` コメント「HUD が低くなった分だけ盤とトレイが広がる」が実測と合わない

HEAD と作業ツリーの比較（マスの大きさ `cell`、HUD の高さ）:

| 向き | 盤 | HUD の高さ | 盤の cell | 盤の枠の y | トレイの枠の高さ |
|---|---|---|---|---|---|
| 横 | 8×8 | 112 → **136** | 55 → **52** | 137 → 161 | 466 → 442 |
| 横 | 6×10 | 112 → 136 | 64 → 64 | 165 → 177 | 466 → 442 |
| 縦 | 8×8 | 168 → 136 | 74 → 74 | 190 → 158 | 282 → 314 |
| 縦 | 6×10 | 168 → 136 | 59 → 59 | 190 → 158 | 520 → 552 |

- 縦画面は盤が幅で頭打ちなので、盤は広がらず上へ 32 寄り、広がるのはトレイだけ
- 横画面は HUD が 24 高くなり、8×8 の盤が 55 → 52 に縮む（TODO.md の「高くした分だけ盤とトレイが
  縮まないかを見る」の答えにあたる）。値の良し悪しは評価しないが、利用者に伝える材料として挙げる
- また、このコメントは「7 個まで 1 段に収まり」と今の値に依る事実を書いている（上の 2 と同じ理由で、値を変えると偽になる）

### 4. 古いまま残ったコメント（`rg -n "3 個ずつ|2 段|footSize|タイトルへ" src docs` と周辺）

- `src/scenes/records.js:16-17` ファイル冒頭の JSDoc「前へ・次へ・ゴミ箱・**タイトルへ**は画面下の 1 段のアイコンに
  まとめてあり」。タイトルへは左上へ移ったので食い違う（`L` の JSDoc と `createFoot()` の JSDoc は直してある）
- `src/scenes/game.js:275-279` `createHud()` の JSDoc「縦画面では横幅が足りず、時間の表示とボタン 6 個が
  1 段に並ばないので折り返す」。今は縦画面でもボタンは 1 段で、時間の表示とは常に段が分かれる
- `src/scenes/records.js:203` 「下段のアイコンの説明」。説明の枠は左上のタイトルへでも使う（軽微）

### 5. `src/config.js:249` `HUD_ROW` と `HUD_BUTTON_HEIGHT` を対で直す必要がある

- コメントで「ボタンの高さに上下 6 ずつ足した分」と書いてあるが、値は別々の定数。
  利用者が `HUD_BUTTON_HEIGHT` だけ変えると、ボタンが段からはみ出すか、段が余る
- 値はこれから利用者が詰める前提なので、片方だけ直す事故が起きやすい。
  `HUD_ROW` を `HUD_BUTTON_HEIGHT + 12` にする場合は、`const` の宣言順（`HUD_BUTTON_HEIGHT` を先に）を入れ替える必要がある

### 6. テスト: HUD のボタンの並べ方に `tests.html` のテストが無い

- `makeLayout()` は既に `tests.html:176-` からトレイのスロットで確かめられている。
  今回の変更で `buttonsPerRow` が「固定の割り算」から「幅で決まる計算」になり、分岐（折り返すか）を持った
- 「各向き・各盤で、ボタンの並びが `hud.width - hud.padding` に収まる」「`DEMO_LAYOUTS` と `LAYOUTS` で `hud.rows` が同じ」
  の 2 つがあれば、上の 2 の境目を値の調整中に捕まえられる。足すかどうかは判断が要る

## 好みの範囲

### 7. `src/scenes/title.js:29` JSDoc の行頭の ` * ` が抜けている

`+（TODO-076 でボタンを高くした分は、…）。` の行だけ ` * ` が無い。コメントとしては壊れないが、書式が揃っていない。

### 8. `src/scenes/records.js:64` JSDoc の 1 行が長い

「ゴミ箱は画面下の 1 段のアイコンにまとめてあり（`footY`・`foot`）、その分空いた縦の余白を一覧の行数（`rowsPerPage`）へ回してある」を
1 行に繋げたまま。前後は 2 行に折ってあるので、折り直しが抜けたと見られる。

## 確かめて食い違いが無かったもの（1 行ずつ）

- 添字: `game.js:322-325`（undo 1・auto 2・hint 3・mute 5）、`demo.js:313-316`（速さ 1〜3・next 4・strategy 5・mute 6）は、並べた順と一致。`this.buttons` を他で添字参照している箇所は `capture.mjs:214・283` だけで、`n-1` の順は文書の番号表と一致
- 文書の番号表: `UsersGuide.md` 本編 1〜6・デモ 1〜7・記録 1〜4（4 = タイトルへ、`capture.mjs:259` の `n: 4`）は、どれもコードの並びと一致
- `docs/developer.md:231` の HUD の説明は新しい挙動と合う
- 記録画面のタイトルへ: 横 x 14〜50・y 16〜64、縦 x 14〜56・y 32〜88。見出し「記録」（中央）や盤の選択の行（`chooseY` 96 / 126）とは重ならない。説明はボタンの下に出る（`ui.js:384-387`。下に出しても画面に収まる）
- 記録画面の下段: 縦 y 1072〜1128（画面 1136）、横 y 576〜624（画面 640）で収まる。幅は 42×3 + 64 + 14×3 = 232（縦）で中央。`total` の `FOOT_GAP * 3` は並ぶ 4 つの間の数と合う
- 「この回を続ける」: 190×48。ラベル @20px = 140 で収まる。縦 984〜1032 は完成形の枠の下端 980 と重ならない、横 486〜534 も同様
- タイトルの `STACK`: 合計 632（コメントどおり）。`stackTops` は余り 8 を上 0.7・下 0.3 で配るので、最後の行の下端は 637.6 で画面 640 に収まる。`START` 224（'つづきから' @26 = 130）・`SUB` 160（'記録'・'デモ' @24 = 48）はラベルが収まる。2 つ並べた幅 468 / 340 も縦画面の 612 に収まる
- クリア表示: 4 つ並べた幅 124×4 + 12×3 = 532 はコメントの数と合い、外枠 580 ≤ 612。縦の合計 464 + 48 ≤ 640
- 確認の枠（`confirm`）: HUD の段数には依らない固定値なので、縦画面の HUD の変化の影響は無い。ボタン 130×56 は枠 460×200 の y+132〜188 に収まり、本文（本編は 3 行 @24、記録は 2 行）の下端 y+94 前後とも重ならない。'いいえ' @20 = 60 で収まる。記録画面の `CONFIRM` は `LAYOUTS['8x8'].confirm` を読むだけで同じ値
- 規約: `setTimeout` / モジュールのトップレベルの `let` / 色の直書き / ネイティブダイアログ / 新しい依存は差分に無い。寸法は `config.js` か各シーンの既存の定数表（`STACK`・`START`・`SUB`・`BUTTONS`・`L`・`CONTINUE_BUTTON`）に置いてあり、これまでの置き場所と同じ
- 範囲: 差分はすべて TODO-076 の 4 項目の範囲内

## 作り込みすぎ

作り込みすぎ: なし（`makeLayout()` の `Math.max(1, …)` は、`HUD_BUTTON_MAX` を大きくしたとき 0 除算になるのを防ぐ 1 語なので残してよい）
