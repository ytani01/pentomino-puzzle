# TODO-060 の分担

- implementer（Sonnet 5）: `logic.js` の即座に外す手、着手後に足した `demo.js` の先読み、`config.js`・文書の手直し。複数ファイルにまたがるため分けた
- tests（Sonnet 5）: `tests.html` の追加・既存テストの前提の直し、壊すと落ちるかの確認
- reviewer（Opus 5.5 に上書き）: 探索とデモの待ち方の分岐が変わるのでレビューを入れた。直したあと再レビュー
- verifier（Sonnet 5）: テストの実行とデモの動きの実測
- main: 依頼の組み立て、利用者への確認、`pickWaitScale()` の JSDoc 1 か所の手直し

依頼は `*-brief.md`、報告は `*-report.md`（2 回目以降の依頼・報告は同じファイルに追記）。
