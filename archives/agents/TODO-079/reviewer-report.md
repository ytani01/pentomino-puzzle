# reviewer の報告（TODO-079）

対象: `git diff`（`src/`・`tools/` のコメント、`storage.js` の `clearBest()` の削除）。
行番号は編集後のもの。コード（コメント以外）の変化は見ていない（verifier の担当）。

## 要修正（1 件）

### 1. `src/scenes/demo.js:123` 意味が逆になった

- 問題: 「`finishStep()` のあとから数えれば、回した時間は次の待ちに**含まれる**」。
  元は「回した時間は自然に次の待ちへ**足される**」。
- 根拠（コードを読んだ）: `update()` は `this.turning` の間 `waited` を増やさず、`advance()` の直前に
  `waited = 0` にしている。回し終えてから 0 から数えるので、手と手の間は「回した時間 + 間隔」になる。
  「含まれる」だと「回した時間も間隔の一部として数える」と読め、コードと逆。
- 直す案: 「`finishStep()` のあとから数えれば、回した時間は次の待ちに足される。」（元の動詞に戻す）

## 検討（9 件）

### 2. `src/scenes/game.js:297-299` 呼ぶ関数の役目が不正確になった

- 問題: 「（`refreshHud()` が切り替える）ので重ならない」。元は「（`refreshHud()` で消す）」。
- 根拠（コードを読んだ）: `recordText` を出すのは `showRecordStatus()`（game.js:1375）で、
  `refreshHud()` は `left > 0` のときに空にする（game.js:1300）だけ。「切り替える」は言い過ぎ。
- 直す案: 「（`refreshHud()` が消す）ので重ならない」

### 3. `src/scenes/game.js:1284-1286` 不自然な日本語

- 問題: 「形の決まった空きを埋める（TODO-044）・解の有無を調べ直す・遊びかけを控える、をここにまとめてある」。
  「〜する、を」が日本語として不自然。
- 直す案: 「……遊びかけを控える処理を、ここにまとめてある。」

### 4. `src/scenes/demo.js:336-338` 理由の文の主語が抜けた

- 問題: 「探し方を切り替えて空の盤から探し直す。2 つの探し方は盤面の辿り方が違い、今の盤面を引き継げないため。」
  元の「途中から続けないのは、」が消えて、「ため。」で終わる文が何の理由なのか分からない。
- 直す案: 「……探し直す。途中から続けないのは、2 つの探し方で盤面の辿り方が違い、今の盤面を引き継げないため。」

### 5. `src/logic.js:282-283` 「理由は」が 2 通りに読める

- 問題: 「置けない理由を返す。理由は、盤の外へはみ出したのか他のピースと重なったのかで画面の警告を
  出し分けるため。」の「理由は」が「返す値の中身」とも「返す理由」とも読める。元は「理由を返すのは、」。
- 直す案: 「置けるかどうかと、置けない理由を返す。理由も返すのは、……出し分けるため。」

### 6. `src/logic.js:590-592` 理由が文の途中に挟まって読みにくい

- 問題: 「違うのは、毎回違う試し方と解を見せるため、始める前に 1 回だけ……入れ替えることだけ。」
  「ため、」のあとに本題が続き、どこまでが理由か追いにくい。
- 直す案: 「違うのは、始める前に 1 回だけピースの並びと各ピースの向きの並びを `random` で入れ替えることだけ
  （毎回違う試し方と解を見せるため）。」

### 7. `src/solutions.js:142` 「なぜ」の結論が消えて、文が浮いた

- 問題: 「選ぶ解が毎回違えば、そこから決まる手も変わる。」元はこのあとに「ので、探索順を混ぜて引き直す
  仕掛け（TODO-017 の `shuffle` と `HINT_RETRIES`）は要らなくなった」が続き、それが言いたいことだった。
  経緯として消した結果、何のための文か分からなくなった。あわせて、このファイルから TODO-017 の参照が
  無くなった（implementer-1 も判断を求めている）。`src/storage.js` でも同じ理由で TODO-021 が消えている
  （旧「TODO-021 の `canonicalCellsKey()` はここでは要らなくなった」）。
- 直す案: 「解を無作為に選ぶので、同じ盤面でも出る手は毎回変わる（手を混ぜる仕掛けは要らない。TODO-017）。」
  TODO-021 は、無くなったものの話なので消したままでよいと考える（境界線上なので判断は管理者に任せる）。

### 8. `src/config.js:255` 言い過ぎになった

- 問題: `TRAY_SLOT_PAD` の「狭いほど指を動かす距離が縮み、掴みやすさは落ちない（TODO-053）」。
  「狭いほど……落ちない」は、いくら詰めてもよいと読める。元は「12 から詰めた。……間が狭くても
  掴みやすさは落ちない」で、6 まで詰めた範囲の話。
- 直す案: 「指を動かす距離を縮めるため詰めてある。この程度の間なら掴みやすさは落ちない（TODO-053）」

### 9. `src/config.js:489-491`・`:608-609` `edgeDarken` の説明（main が直した箇所）

値との対応は合っている。`TILE.edgeDarken` は `0.0`、`darken()`（boot.js:42）は係数を掛けるので 0 で黒、
`TILE.border` は 1、`OUTLINE.width` は 2。`ICON` の「ボタンの高さ（56）」は `HUD_BUTTON_HEIGHT` = 56、
`HINT_BADGE` の「`HUD_ROW` = 68」は 56 + 12 = 68 と合う。気になるのは次の 2 点。

- `edgeDarken` を使うのは 12 色の組のマス（`makeTile` の beveled）だけで、ガラスは `GLASS.gridColor`、ネオンは
  `NEON.gridAlpha` を使う。TILE の JSDoc の「格子は太さ 1 の黒い線」は 12 色の組の話だと書いたほうが正確
- `OUTLINE` の箇条書きは「触るもの 3 つ」の説明だが、`TILE.edgeDarken` の項の後半「格子は外周より細くする」は
  太さの話で、太さを決めるのは `edgeDarken` ではなく `TILE.border`
- 直す案: TILE は「12 色の組で、ピース内側の格子の色に掛ける係数（0 で黒）」とする。OUTLINE の項は
  「格子の色の係数（0 で黒）。格子の太さは `TILE.border`（1）で、外周より細くしてある（……。TODO-018）」とする

### 10. 3 人の表記揺れ: 「ぶん」と「分」

- 問題: implementer-2 だけが「ぶん」を「分」に書き換えている（game.js・demo.js・records.js で「ぶん」が 0 件になった。
  例: game.js「ピース 1 個分」「1 マス分」、records.js「1 頁分」「60 マス分」）。implementer-1・3 の担当と
  書き換えていないコメントは「ぶん」のまま（config.js 9 件、storage.js 3 件、title.js 3 件など）。
  同じ `60 マスぶん` が storage.js では「ぶん」、records.js:642 では「分」になっている。
- 直す案: どちらかに揃える。元のリポジトリは「ぶん」が多いので、implementer-2 の 3 ファイルを「ぶん」に戻すのが
  差分として小さい。直さずに残してもよい（好みの範囲に近い）。

## 好みの範囲（3 件）

- `src/scenes/game.js:6` 「盤面を Undo の履歴や**求解**へ」。遊ぶ側に探索はもう無い（照合だけ）。implementer-1 は
  logic.js の「求解」を「探索」に直しており、ここだけ残っている。案: 「Undo の履歴や全解のデータとの照合へ」
- `src/scenes/records.js:537`・`:544` `reload()` の JSDoc から「盤を切り替えたときに」が消え、
  「チェックは消す。別の盤の番号を持ち越しても意味が無いため」の理由が何の話か分かりにくい
  （呼ぶのは `create()` と `selectBoard()` だけ）。案: 「チェック（`this.checked`）は盤を切り替えたら消す（TODO-071）。」
- `src/solutions.js:27-28` 「パスを組み立てずに決め打ちで並べるのは、置き場所を変えたときに動かす前に
  気づけるようにするため」。「パスが文字列のまま見える（検索で見つかる）」という仕組みが抜けて、
  なぜ気づけるのかが書かれていない。案: 「……並べるのは、パスが文字列のまま見え、置き場所を変えたときに
  動かす前に気づけるため。」

## 範囲外で気づいたこと（今回の差分で生じたものではない）

- `src/scenes/game.js:450` 「外接矩形の真ん中は U や X のように空いていることがある」。X は 3×3 の真ん中が
  埋まっている（`PIECES` の X は `[1,1]` を持つ）。implementer-2 の報告と同じ見立て。X は印を出さない
  （`turnMarkKind()` が null）ので実害は無いと思われるが、未確認。

## main が直した 2 点

- `config.js` の 3 か所: 上の 9 のとおり、数値はコードと合っている。
- `clearBest()` の削除: `rg -n clearBest --glob '!archives/**'` を実行した結果は `TODO.md` の 2 行だけ
  （`src/`・`tools/`・`tests.html`・`docs/` には残っていない）。`clearHistory()` の JSDoc から `clearBest()` の
  名前も外れている（storage.js:368「最短時間は消さない」）。

## implementer-2 がコードに合わせた 2 か所

- `src/scenes/demo.js:315` `refreshStatus()`「1 手ごとに呼ぶ」: 呼ぶのは `finishStep()`（demo.js:250。1 手ごと）と
  `refreshHud()`（demo.js:310。状態が変わったとき）。「毎フレーム」より正確で、コードと合っている。
- `src/scenes/records.js:595` `refresh()`「1 件も無いときだけ頁の数を消す」: コードは `pageText` を空にし、
  前へ・次へは `setEnabled(false)` で隠さない。合っている。

## 問題の無いファイル

- `src/audio.js`・`src/main.js`・`src/icons.js`・`src/ui.js`・`src/scenes/boot.js`・`src/scenes/title.js`・
  `src/scenes/clear.js`・`tools/capture.mjs`・`tools/enumerate.mjs`: 問題なし
- `tools/gen-solutions.mjs:12`: 「依存は増やさない。」を消している。今後に向けた決めごとで、経緯ではない。
  ただし `CLAUDE.md` に「新しいライブラリを追加しない」があるので、実害は無い
- `src/storage.js`: 上の 7（TODO-021）以外は問題なし
- `src/logic.js`・`src/config.js`・`src/scenes/game.js`・`src/scenes/demo.js`・`src/scenes/records.js`: 上に挙げた箇所以外は問題なし。
  「なぜ」が消えた箇所は、ほかに見つからなかった（消えたのは経緯と、コードを読めば分かる「何を」だけ）

## 作り込みすぎ

作り込みすぎ: なし（コメントだけの差分。`clearBest()` の削除は減らす方向）。
