# TODO-094 画面確認報告

対象: 未コミットの working tree（TODO-093 と合わせた状態）。`python3 -m http.server 8765` で確認。

## 撮った画像（すべて `~/tmp/playwright-mcp/`）

- `game-960x640-8x8-landscape.png` — 本編・8×8・横 960×640（ピース2個配置）
- `game-640x1136-6x10-portrait.png` — 本編・6×10・縦 640×1136（ピース2個配置。`window.game.scale.width === 640` を確認）
- `demo-960x640.png` — デモ画面・横 960×640
- `game-960x640-8x8-restart-confirm.png` — やり直しの確認モーダル（ピースあり）
- `game-844x390-touch-restart-tooltip.png` — タッチでやり直し→はい 直後（840x390、CDP でタッチ有効化）

## 1. 操作の概要（本編）

一致。横 960×640・8×8、縦 640×1136・6×10 とも、下端に概要が出ていてトレイ・メッセージの行と重ならず、画面からはみ出していない。横は 1 行「ドラッグで置く・タップで向きを変える・盤の外で離すと外す」、縦は 3 行（ドラッグ/タップ/盤から外す、の各説明）。ガラス面（半透明＋斜めの反射）が盤の空きマスに見える（中央のアクリル板とは見分けがつく）。最上段のタイトル行と HUD の重なりも無い。

## 2. デモ画面に概要が無いこと

一致。`?demo=1` を横 960×640 で開き、`game.scene.getScene('Demo').layout.help.lines === 0` を確認。画像でも概要の帯は出ていない。

## 3. やり直しの確認

一致。8×8・横でピース2個を置いた状態で「やり直し」を押すと確認モーダルが出る（`confirmParts[0].visible === true`）。「いいえ」を押すと閉じて盤は2個のまま（`onBoard: 2`）。盤が空の状態で `confirmRestart()` を呼ぶと確認なしで即やり直し（`confirmVisible: false, onBoard: 0`）。

## 4. 盤が空のホーム

一致。8×8 でピース1個を置いてタイトルへ戻り（確認あり・「はい」）、`localStorage['pentomino-puzzle/progress/8x8']` の盤上ピース数は 1。続けて「はじめる」相当（`resume: false`）で空の盤を開き、何も置かずに HUD の一番左（ホーム）を押すと、確認なしで即 Title へ遷移し（`scene.getScenes(true)` が `['Title']`）、遊びかけの値は変わらず 1 のまま。タイトル行と同じ経路（`confirmToTitle()`）であることもコードの呼び出し経路の実測で確認。

## 5. タッチのツールチップ

一致。CDP でタッチを有効化した 844×390 のコンテキストで、ピースを置いてから「やり直し」を tap → 確認モーダルが出る（`confirmState: true`）→「はい」を tap → 直後（+300ms）に `tooltip.visible: true`、`text: 'やり直し'`、`onBoard: 0`（やり直し済み）。実装報告にある原因切り分け（`scene.restart()` がキュー処理のため旧シーンの tooltip に flash が出て捨てられる）と整合する挙動。

## 6. tests.html

454 件すべて通った（`#summary` を確認）。

## 7. コンソールのエラー

本編・デモ・確認モーダル操作中に 0 件。`tests.html` で 1 件出たが `favicon.ico` の 404 のみで、機能とは無関係。

## 境界線上の判断（実害は未確認。報告のみ）

- タッチ後のスクリーンショット（`game-844x390-touch-restart-tooltip.png`）には吹き出しの文字が視認できなかった。ただし `evaluate` での実測では `visible: true, text: 'やり直し'` を確認しており、スクリーンショットの撮影タイミング（`page.screenshot` 呼び出しまでの遅延）でフェード演出が進んでいた可能性がある。数値的な確認は取れているため、表示自体は機能していると判断した。
- 実装報告にある「モーダルを開いたときの『やり直し』の説明が幕の下に暗く見える」（`DEPTH.tooltip` < `DEPTH.confirm`）は見た目の優先順位の話であり、今回の依頼の完了条件には含まれないため見ていない。
