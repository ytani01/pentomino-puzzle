# TODO-073 reviewer への依頼

## 対象

`git diff`（未コミット。docs 担当が並行して文書を直している）。仕様は `TODO.md` の TODO-073 の節と、利用者が決めたこと（ボタンは完成形の下の
文字のボタン「この回を続ける」）。報告: `archives/agents/TODO-073/implementer-report.md`。

## 見てほしいこと

- 履歴の 1 件から遊びかけの形を作る計算: 盤面の復元、経過時間、印、完成させた解の一覧（始めた直後に完成と見なさない）。6×10・8×8 の両方
- 遊びかけがあるときの確認: 画面内で出るか、やめたときに遊びかけが残るか、置き換えたときに前の遊びかけが確実に消えるか
- 始めたあと: 別の解を作ると `recordClear()` で足され、同じ解を作り直すと「記録済み」、元の回の履歴が意図せず書き換わらないか
  （同じ回の続きで同じ解を早く作ったら上書きになるか、など。仕様どおりか）
- implementer が決めたこと: (1) ボタンの寸法を `config.js` ではなく `records.js` に置いた（規約「数値と色は `config.js`」に照らしてどうか）、
  (2) 続けると registry の盤を、記録画面で見ている盤に切り替える
- `tools/capture.mjs` の直し（`recordClear` に替えた）が正しいか
- TODO-071・072 の動きを壊していないか。足したテストが、計算を壊すと落ちるか

## 見なくてよいもの

見た目の良し悪し、全解データ、文書の中身（docs 担当が直している途中）。

## 報告

`archives/agents/TODO-073/reviewer-report.md` に、指摘を重い順に（場所・問題・どうすればよいか）。問題の無い観点は 1 行ずつ。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
