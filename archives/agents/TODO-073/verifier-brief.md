# TODO-073 verifier への依頼

## 対象

未コミットの差分（`git diff`）。仕様は `TODO.md` の TODO-073 の節と、利用者が決めたこと（完成形の下の文字のボタン「この回を続ける」）。
レビューのあとで、「はじめる」が前の続きを引き継ぐ不具合（TODO-030 からあった分も）を直した。報告: `implementer-report.md`（末尾が最新）・`reviewer-report.md`。

## 確かめること（Playwright。npx の置き場から借りる。新しいコンテキスト。各手順 1 回。implementer のスクリプトをなぞるだけにしない）

1. `tests.html` の全件（件数・落ちた件数・コンソールエラー）
2. 844×390: 記録画面を撮り（`~/tmp/playwright-mcp/todo073-records-844x390.png`）、「この回を続ける」が完成形の下にあり、達成度・下段と重ならない。
   390×844 も同じく撮る（`...-390x844.png`）
3. 遊びかけが無い状態で、印の付いた 6×10 の記録の回（`localStorage` に入れる）を、記録画面で 6×10 を選んで「この回を続ける」→本編が 6×10 の
   完成した盤面・その回の経過時間から始まり、クリア表示は出ない。HUD の印（ヒント・おまかせ）がその回のもの
4. ピースを 1 つ外して同じ所に戻す→「記録済み」、履歴の件数は変わらない
5. 遊びかけがある状態で「この回を続ける」→確認が出る→やめる→記録画面のまま、`localStorage` の遊びかけは変わらない。もう一度→置き換える→本編がその回で始まる
6. 本編→タイトル→「はじめる」がまっさらな盤（12 個ともトレイ・時計 0）で始まる。「つづきから」→タイトル→「はじめる」も同じ
7. `docs/UsersGuide.md` の記録の節の吹き出しの番号（Ⓐ〜Ⓖ・①〜）と `docs/images/records.png` が合うか

## 見なくてよいもの

見た目の良し悪し、テストの強さ（implementer・reviewer が確かめた）、全解データ。

## 報告

`archives/agents/TODO-073/verifier-report.md`。一致したものは 1 行、食い違いだけ詳しく。測った値を必ず載せる。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
