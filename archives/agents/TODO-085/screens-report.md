# TODO-085 画面確認

## 見たもの

- サーバ: http://localhost:8765/（既存起動を利用）。ビューポート 1280x720 の 1 条件。
- タイトルから `window.game.scene.getScene('Title').scene.start('Demo')` でデモ画面（盤 8x8 の既定）を開いた。既定の探し方はランダムで、HUD の探し方ボタン（`strategyButton`）にランダムのアイコン（節 5 個の一筆書き形。今回の変更対象）が出ていた。
- ボタン周辺（画面座標 800,96 起点、幅 120×高さ 90）を `page.screenshot` の `clip` で拡大して撮影。
- ボタンを 1 回クリック（`page.mouse.click(829, 126)`。1 回で成功、返り待ちの詰まりなし）し、`scene.getScene('Demo').strategy` が `'depth'` になったことを確認してから、同じ範囲を撮影。
- 画面全体（ランダム時）も 1 枚撮影。

## 画像

- `/home/ytani/tmp/playwright-mcp/todo085-full-random.png` — 画面全体（ランダム、盤 8x8）
- `/home/ytani/tmp/playwright-mcp/todo085-zoom-random.png` — 探し方ボタン拡大（ランダム）
- `/home/ytani/tmp/playwright-mcp/todo085-zoom-depthfirst.png` — 探し方ボタン拡大（深さ優先、切り替え後）

## 気づいたこと

- ランダムのアイコンと深さ優先（木の形）のアイコンは、拡大画像で見比べると形がはっきり異なり、見分けられる。ランダムは節が線に埋もれて見づらいというより、全体が細い交差線の塊に見える（節の丸みは判別できるが目立たない）。28px 表示でも判別できるかは境界線上で、実際の 28px 相当の見え方までは今回の画像からは確言できない（縮小して見比べる作業はしていない）。
- 画面全体のスクリーンショットでは、盤・トレイ・HUD のボタン列とも枠内に収まっており、はみ出し・重なり・欠けは見当たらない。
- コンソール: エラー 0 件。警告 4 件（すべて `GL_CLOSE_PATH_NV` の GPU stall という WebGL ドライバのメッセージで、アプリのコード由来ではない）。

## 判断が要る点

- ランダムのアイコンが 28px 実寸でも深さ優先と明確に見分けられるかは、拡大画像だけでは判定しきれない（境界線上）。必要なら 28px そのままの範囲もあわせて見る作業を追加してよい。
