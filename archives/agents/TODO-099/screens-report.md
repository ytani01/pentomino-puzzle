# TODO-099 screens 報告

## 1. tests.html
- 456 件すべて通過。コンソールエラーは favicon.ico の 404 のみ（無害）。

## 2. タイトル・記録画面（見出し「ボード」「色」）
問題なし。見出しとボタンの重なり・画面からのはみ出し・行内でのボタン列の
食い違いは無い。measured values（内部解像度の座標。画面 CSS 幅は原点を
game.scale で割った比率）:

- タイトル 390x844（縦。内部解像度 640 幅）
  - 見出し「ボード」 right=186、ボード先頭ボタン left=285（重なり無し）
  - 見出し「色」 right=55、色先頭ボタン left=202（重なり無し）
  - 色ボタン最終 right=595 < 内部幅 640（はみ出し無し）
- タイトル 844x390（横。内部解像度 960 幅）
  - 見出し「ボード」 right=346、ボード先頭ボタン left=445
  - 見出し「色」 right=215、色先頭ボタン left=362
  - 色ボタン最終 right=755 < 内部幅 960
- 記録 844x390: 見出し right=346、先頭ボタン left=445、最終ボタン right=672 < 960
- 記録 390x844: 見出し right=186、先頭ボタン left=285、最終ボタン right=512 < 640

境界線上の判断（実害は未確認）: ボードの行（選択肢 2 個）と色の行（選択肢 3 個）は
それぞれ中央寄せで組むため、行ごとに見出し・ボタンの左端がそろっていない
（例: タイトル縦画面で「ボード」x=114、「色」x=31）。これは
`createChoiceRow` の中央寄せの仕組みによるもので、TODO-099 の変更
（labelWidth 48→96）より前から同じ組み方であり、今回の差分が原因かは未確認。

## 3. 本編（縦画面 390x844 のみ）
- はみ出しメッセージ「そこはボードからはみ出す」: `messageText`
  displayWidth=288、left=176・right=464、内部幅 640 に収まり 1 行
  （改行なし）。
- 下端の操作の説明（3 行、「ボードから外す」を含む）: 各行とも画面幅に
  収まっており、折り返しは発生していない（スクリーンショットで確認）。

## 撮った画像（絶対パス）
- /home/ytani/tmp/playwright-mcp/title-390x844.png
- /home/ytani/tmp/playwright-mcp/title-844x390.png
- /home/ytani/tmp/playwright-mcp/records-390x844.png
- /home/ytani/tmp/playwright-mcp/records-844x390.png
- /home/ytani/tmp/playwright-mcp/game-390x844-overflow-message2.png
  （撮影時点で `messageMs` タイマーが切れて文字は空。数値は evaluate で確認済み）

## 再確認（遊び方の枠の折り返し。src/scenes/title.js の手動改行後）

### タイトル 390x844（縦）
`getWrappedText()` の行一覧（7 行、期待どおり）:
```
12 種のピースをボードに
すき間なく敷き詰めるパズル。

ドラッグ … 置く / 動かす
タップ … 次の向きへ（回転と裏返しを順に巡る）
ボードから外す … ボードの外で離す / 
トレイの方へ振る
```
- テキストの上端・下端: top=516, bottom=708（内部解像度座標）
- 枠（howTo パネル）の上端・下端: top=531, bottom=717（`stackTops()` を
  `src/ui.js` から import して再計算）
- テキスト上端(516) が枠上端(531) より 15px 上に出ている。ただし
  スクリーンショット（`title-390x844-recheck.png`）では崩れて見えず、
  文字が枠の外へはみ出して他要素と重なっている様子もない
  （境界線上の判断・実害は未確認）。

### タイトル 844x390（横）
`getWrappedText()` の行一覧（5 行、期待どおり。1 行目は折り返さず 1 行のまま）:
```
12 種のピースをボードにすき間なく敷き詰めるパズル。

ドラッグ … 置く / 動かす
タップ … 次の向きへ（回転と裏返しを順に巡る）
ボードから外す … ボードの外で離す / トレイの方へ振る
```
- テキスト top=188.6, bottom=324.6
- 枠 top=180.4, bottom=366.4（同じ方法で再計算）
- テキストは枠の内側に収まっている（重なり・はみ出し無し）。

### 本編下端の操作の説明（縦画面 390x844 のみ）
`getWrappedText()` の行一覧（3 行、折り返し無し）:
```
ドラッグ … 置く / 動かす
タップ … 次の向きへ（回転と裏返しを順に巡る）
ボードから外す … ボードの外で離す / トレイの方へ振る
```
- テキスト top=1027, bottom=1087、内部解像度の画面高さ 1136 に収まっている。

### 撮り直した画像（絶対パス）
- /home/ytani/tmp/playwright-mcp/title-390x844-recheck.png
- /home/ytani/tmp/playwright-mcp/title-844x390-recheck.png

## 再々確認（縦画面 390x844 のみ。howTo の枠を 186→222 に広げた後）

枠（howTo パネル）の実物は、`Phaser.GameObjects.Graphics.prototype.fillRoundedRect`
を一時的に差し替えて呼び出し引数を記録し、シーンを作り直して読み出した
（`createPanel` が最初に呼ぶ `fillRoundedRect` が howTo の枠）。

- 枠: x=14, y=501, width=612, height=222 → 上端=501, 下端=723
- 遊び方の文字（`getWrappedText()`。7 行、変更無し）:
  ```
  12 種のピースをボードに
  すき間なく敷き詰めるパズル。

  ドラッグ … 置く / 動かす
  タップ … 次の向きへ（回転と裏返しを順に巡る）
  ボードから外す … ボードの外で離す / 
  トレイの方へ振る
  ```
  上端=516, 下端=708
- 余白: 上 516-501=15px、下 723-708=15px。**枠の内側に収まった**
  （前回は上下にはみ出していたが解消）。

下の行との重なりの有無（`getBounds()` の top/bottom、内部解像度座標。
画面高さ 1136）:

| 行 | top | bottom |
|---|---|---|
| howTo 枠 | 501 | 723 |
| ボード（見出し・ボタン行） | 753 | 791 |
| 色（見出し・ボタン行） | 823 | 861 |
| 記録なし | 881 | 907 |
| はじめる・つづきから・記録 | 940 | 982 |
| Space / Enter でも始められる | 995 | 1015 |
| デモ | 1056 | 1087 |

すべて隙間を挟んで並び、重なりは無い。最下段のデモ（bottom=1087）も
画面高さ 1136 に収まっている。

### 撮り直した画像
- /home/ytani/tmp/playwright-mcp/title-390x844-recheck2.png
