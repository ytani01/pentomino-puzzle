# TODO-071 screens への依頼（配置の 2 案を撮る）

## 対象

未コミットの差分。記録画面（`src/scenes/records.js`）にチェック・全部選ぶ・ゴミ箱を足し、配置を 2 案（`config.js` の仮の値で切り替え）作った。
案の切り替え方と、記録を入れる方法は `archives/agents/TODO-071/implementer-report.md` の「撮る担当向け」にある。

## 撮るもの

`python3 -m http.server 8765` を立て、Playwright MCP で撮る。記録は 8×8 に 12 件ほど（ヒント・おまかせの印が付いた回を 2〜3 件混ぜる）入れる。
案 A・B それぞれについて、次の 2 つの画面サイズで、チェックを 2 件入れた状態（ゴミ箱が押せる状態）を 1 枚ずつ撮る（計 4 枚）:
- 844×390（スマホ横）
- 390×844（スマホ縦）
画像は `~/tmp/playwright-mcp/todo071-A-844x390.png` のような名前で、案とサイズごとに別ファイル（1 枚にまとめない）。

## 見るもの

- はみ出し・重なり・欠け・読めない文字（チェックボックスと日時・印の行、全部選ぶ、ゴミ箱・ホーム・前へ次へ）
- implementer が「詰まり気味」と書いた箇所（報告の「依頼との違い」）が、ほかの部品とくっついて見えるか
- コンソールエラーの数

## 見なくてよいもの

どちらの案がよいか（利用者が選ぶ）、完成形の絵の正しさ、ほかの画面、操作の動き（別の担当が確かめる）。

## 報告

`archives/agents/TODO-071/screens-report.md` に画像のパスと、見たものごとの結果。問題なしは 1 行。コードは直さない。返事は 5 行以内。
