# TODO-071 verifier への依頼

## 対象

未コミットの差分（`git diff`）。仕様は `TODO.md` の TODO-071 の節と、利用者が決めたこと（`reviewer-brief.md` の冒頭の一覧）に加えて:
その盤の記録を全部消したら達成度とおまかせの番号も消す／見ていた回が残れば消したあとも選んだまま。
報告: `implementer-report.md`・`implementer2-report.md`・`reviewer-report.md`。

## 確かめること（Playwright。npx の置き場から借りる。`tools/capture.mjs` と同じ。新しいコンテキスト。844×390）

記録は `localStorage` に 8×8 を 10 件ほど入れる（入れ方は `implementer-report.md` の「撮る担当向け」）。各手順は 1 回ずつでよい。

1. `tests.html` の全件（件数・落ちた件数・コンソールエラー）
2. 何もチェックしていないとき、ゴミ箱を押しても確認が出ない
3. 3 行目を表示して、1 行目と 2 行目にチェックしてゴミ箱→確認に「2 件」→消す。残った件数が 8、表示中の回が前の 3 行目の回のまま
4. 2 ページ目へ送ってチェックが残るか（1 ページ目で 1 件チェック→次へ→前へで残っている）。盤を 6×10 に切り替えて戻すとチェックが消える
5. 「全部選ぶ」→ゴミ箱→消す。一覧が空、達成度が 0 解、`localStorage` の found・auto のキーがその盤の分だけ空。6×10 の記録は残る
6. チェックを押しても右の完成形が変わらない／行を押してもチェックが変わらない
7. 下段のアイコンにマウスを載せて説明が出て、画面の下で切れない（ゴミ箱 1 つで代表させる。画像を撮って `~/tmp/playwright-mcp/todo071-tooltip.png` に置き、見る）
8. `docs/UsersGuide.md`・`docs/developer.md` の記録の説明が今の動きと合うか。`docs/images/records.png` の吹き出しの番号と UsersGuide の番号が合うか

## 見なくてよいもの

配置の良し悪し（利用者が選んだ）、テストの強さ（implementer・reviewer が壊して確かめた）、全解データ。

## 報告

`archives/agents/TODO-071/verifier-report.md`。一致したものは 1 行、食い違いだけ詳しく。測った値（件数・キーの中身）を必ず載せる。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
