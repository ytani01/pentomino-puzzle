# TODO-071 screens 報告

## 案 B が撮れなかった件

案の切り替え（`src/config.js` の `RECORDS_LAYOUT`）はソースの編集が要るが、
screens 担当は「リポジトリの中は 1 文字も書き換えない」制約のため、
`config.js` を直接書き換えて反映させる手段が取れなかった（実行時に
`RECORDS_LAYOUT` を差し替える口が無く、`records.js` はモジュール読み込み時に
`LAYOUTS_BY_VARIANT[RECORDS_LAYOUT]` を確定させている）。
案 A（現在の既定値）のみ撮影した。案 B が要るなら、呼び出し側で
`RECORDS_LAYOUT = 'B'` に書き換えてから再度依頼してほしい。

## 撮った画像（案 A、8×8、チェック2件でゴミ箱が押せる状態）

- `/home/ytani/tmp/playwright-mcp/todo071-A-844x390.png`（844×390 スマホ横）
- `/home/ytani/tmp/playwright-mcp/todo071-A-390x844.png`（390×844 スマホ縦）

## 見た結果

- はみ出し・重なり・欠け・読めない文字: 844×390・390×844 とも見当たらず。
  チェックボックス・日時・ヒント/おまかせの印の行、全部選ぶ、
  ゴミ箱・ホーム・前へ次へは、いずれも枠内に収まり重なっていない。
- implementer が「詰まり気味」と書いた箇所: 両サイズとも周辺の部品と
  くっついて見える箇所は無かった。
- コンソールエラー: 0 件（警告も 0 件）。Phaser 起動時のバナーログのみ。

## 追記: 案 B（RECORDS_LAYOUT='B'、main が切り替え済み。新しいコンテキストで再取得）

`/home/ytani/tmp/playwright-mcp/todo071-B-844x390.png`（844×390 スマホ横）
`/home/ytani/tmp/playwright-mcp/todo071-B-390x844.png`（390×844 スマホ縦）

- はみ出し・欠け: 両サイズとも見当たらず。全部選ぶ・ゴミ箱・ホーム・前へ次へは
  枠内に収まっている。
- **重なり（問題あり）**: 844×390（横画面）で、チェック＋選択中（水色枠）の
  行だけ `00:12おまかせ・ヒント` のように、経過時間の直後に「おまかせ・ヒント」
  の印がすき間無くくっついて表示される。ほかの行（未選択、縦画面の同じ行）は
  `00:16　　ヒント` のように右詰めですき間が空いており、この行だけ詰まっている。
  implementer の言う「詰まり気味」の箇所と一致する可能性あり。
- コンソールエラー: 両サイズとも 0 件（警告も 0 件）。

### 補足（案 A の再確認について）

案 A の撮影時、ヒント・おまかせの印のフィールド名を `hint`/`auto` と
誤って仕込んでおり（正しくは `h`/`a`）、印が付いた行を確認できていなかった。
案 A で印付きの行の見え方（特に上と同じ詰まりが起きるか）を確かめるなら、
`h`/`a` で履歴を仕込み直しての再撮影が要る。
