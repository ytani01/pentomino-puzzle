# TODO-071 reviewer への依頼

## 対象

`git diff`（未コミット）。仕様は `TODO.md` の TODO-071 の節と、利用者が決めたこと:
チェックは各行にいつも出す／「全部選ぶ」はその盤の記録すべて／ゴミ箱はチェック 1 件以上で押せ、画面内で件数を確かめてから消す／
チェックはページ送りで残し、盤の切り替えで消す／「この回を消す」「全部消す」はゴミ箱にまとめる／配置は案 B（1 ページ 8 行）。
報告: `implementer-report.md`（Sonnet 5。途中で上限に達した）・`implementer2-report.md`（Opus 5.5。`capture.mjs` の吹き出し）。

## 見てほしいこと

- まとめて消す計算（`storage.js`／`logic.js`）: 消す対象の決め方（添字か、日時などの鍵か）、同じ日時の履歴が 2 件あるとき、
  見つけた解・最短時間・達成度との関係（1 件消すときの今までの扱いと揃っているか）。保存形式を変えていないか
- 消したあとの表示中の回・ページ・チェックの状態（全部消した、表示中の回を消した、最後のページが空になった）
- チェックと行の押し分け（チェックを押して完成形が変わらないか、行を押してチェックが変わらないか）。盤の切り替えでチェックが消えるか
- ゴミ箱が押せない状態のときの見た目と、押しても何も起きないこと。確認の枠の件数
- 前の implementer が途中で止まったので、書きかけ・使われない変数や関数・古い部品の参照（`removeButton`・`clearButton` など）が残っていないか。
  `rg -n "removeButton|clearButton|この回を消す|全部消す|RECORDS_LAYOUT|LAYOUTS_BY_VARIANT" src/ tools/ docs/ tests.html`
- 規約（`confirm()` を使わない、数値と色は `config.js`、状態はシーンのプロパティ）。足したテストが、計算を壊すと落ちるか（Node で確かめる）

## 見なくてよいもの

見た目の良し悪し（配置は利用者が選んだ）、全解データ。

## 報告

`archives/agents/TODO-071/reviewer-report.md` に、指摘を重い順に（場所・問題・どうすればよいか）。問題の無い観点は 1 行ずつ。
コードは直さない。境界線上の判断は「実害は未確認」と添えて報告だけ。返事は 5 行以内。
