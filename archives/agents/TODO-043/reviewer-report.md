# TODO-043 reviewer の報告

対象: `git diff src/`（`src/logic.js`・`src/scenes/demo.js`）。設計は brief どおりか、規約、壊れる場合を見た。

## 要修正

### 1. `src/config.js:621-625` DEMO の JSDoc の所要時間が今の動きと食い違う

- 問題: 「最初の解までの置くは中央値で 1〜1.5 万（TODO-040 の実測）… 速いで 1〜2 時間、最速（60fps）でも 5〜10 分かかる」が残っている。
- 根拠（実測）: `hasSolution` を渡した `solveSteps` で、最初の solved までの place の数を
  mulberry32 のシード 1,2,3,4,5,42,2026 で数えた。
  - 8×8: 40, 31, 53, 59, 69, 45, 45
  - 6×10: 54, 30, 63, 49, 38, 59, 33
  - 1 万のオーダーではなく数十手。速い（200ms 間隔）なら 20〜30 秒前後になる
    （置くと外すで 1 手ずつなので、手数は place の 1〜2 倍）。
  - スクリプト: scratchpad の `first.mjs`（`tools/window-shim.mjs` を先に import）
- 差分に `config.js` は入っていないが、brief の「要れば `config.js`」と、依頼の観点 4（今の動きと食い違う記述）に当たる。
  「利用者と決めた（時間がかかってもよい）」の部分と速さの数値そのものを変えるかは管理者の判断。

## 検討

### 2. `src/scenes/demo.js:71-75` 別の盤で入り直したとき、古い読み込みの結果が新しいシーンに入る

- 問題: Phaser はシーンのインスタンスを使い回すので、`this.scene.isActive()` は「前回の `create()` から来た promise」でも真になる。
  盤を変えて入り直した場合、前の盤の `solutions` と今の `this.spec` で `solveSteps` を作り、`this.steps` を差し替える。
- 起こる筋（静的に読んだもの）: 6×10 のデモを開く（初回で読み込みに時間がかかる）→ 読み込み中にタイトルへ →
  8×8 に変えてデモ → 8×8 が先に届いて running → 遅れて 6×10 が届き、`this.spec`（8×8）×6×10 の解の表で
  generator を作り直す。判定は長さの違う文字列を突き合わせるので意味の無い値になり、盤には前の generator が
  置いたピースが残ったまま、試した手も 0 に戻らない。
- 同じ盤で入り直した場合は、2 本の continuation が同じ microtask の区切りで走るので `update()` を挟まず、実害は無い見込み（未確認）。
- `game.js:99-100` の既存の作りと同じ形（brief の指定どおり）で、本編にも同じ筋がある（`this.solutions` に別の盤の表が入る）。
- ローカルでは読み込みが一瞬なので起こしにくい。GitHub Pages の初回で遅い回線のときだけ。**実害は未確認**（再現していない）。

### 3. `src/scenes/demo.js:111-112` 「解なしに変わった瞬間だけ鳴らす」の条件が常に真

- 問題: デモでは解なしの place の次は必ず同じピースの remove で、remove が `hintState = 'ok'` に戻す。
  そのため `this.hintState !== 'dead'` は必ず真で、`animate` の速さでは**解なしの place のたびに毎回** `audio.invalid()` が鳴る。
- 根拠（実測）: 上の 14 通りで「解なしの place が続いた回数」は 0。implementer の報告でも、3 件目の solved までに解なしの place が 8×8 で 116〜152 件ある。
- brief の設計（本編と同じ、変わった瞬間に鳴らす）どおりではあるが、コメントの「だけ」は何も絞っていない。
  実際の鳴り方（試行のたびに drop と invalid が重なる）が利用者の想定どおりかは、耳で確かめていない。**実害は未確認**。

## 好みの範囲

### 4. `src/scenes/demo.js:194-195` 文言と色の対応を `game.js:1079-1080`（`runHint()`）と別に持っている

- 本編と同じ文字・色にする設計なので、片方だけ変わると食い違う。対で保守するものが 2 か所に分かれている。
  2 か所だけで、切り出すほどではないとも言える。

## 問題なし（1 行ずつ）

- 観点 1: 判定を yield の前に動かしても、判定から探索再開までの間に `grid`・`unused` を触るのは呼ぶ側だけで、呼ぶ側は `cells` を読むだけ。既定の引数で手順は変わらない（implementer の `cmp` 一致 6 通りと合う）。
- 観点 2: loading 中は `update()` が止まる。速さ（`speed` と `waited` を変えるだけ）・次の解（`state !== 'solved'` で弾く。ボタンも無効）・音・タイトルへ、はどれも `steps` を触らないので null でも落ちない。
- 観点 2: 読み込み中に離れた場合は `isActive()` が偽で generator を作らない。デモは `scene.restart` を呼ばない。`create()` で状態を全部作り直すので、入り直しで前の状態は残らない（2. の筋を除く）。
- 観点 3: remove のあとの盤は、`search()` に潜った盤（空の盤か、`ok` が真だった place のあと）なので「解ける」で正しい。solved → 次の解を探すの直後は、12 個目の remove が来て「解ける」になる（その前の 1 フレームは空のまま）。
- 観点 3: `hintState` の名前を本編と共有しているが、本編で読むのは `runHint()`・`toggleHint()` だけで、デモでは `refreshHud()` を上書きしているので通らない。
- 観点 4: `rg -n "regionsFitPieces|5 の倍数|枝刈り" src` で出るもののうち、`logic.js:270,297` は求解一般の話、`config.js:629` の「枝刈りで捨てる手」は今も「置いてすぐ外す」があるので合っている。食い違うのは 1. の所要時間だけ。
- JSDoc・コメントは「なぜ」を書いている（外から判定を受ける理由、yield の前に置く理由、右端に寄せる理由）。
- 規約: `setTimeout` なし、色は `TEXT_COLORS`、状態はシーンのプロパティ、`logic.js` に Phaser/DOM なし、`storage.js` を呼ばない。行長は既存と同程度（最長 100 文字）。
- 範囲: 指示に無い変更は無い。
- テスト: `solveSteps` の `canContinue` と `ok` のテストは別担当が足す前提なので見ていない。

## 作り込みすぎ

- `src/scenes/demo.js:112`: delete: `&& this.hintState !== 'dead'` は常に真（3. の実測）。`if (animate && !value.ok) audio.invalid();` で同じ動き（検討。3. を直すなら同時に）。
- `src/scenes/demo.js:110-113`: shrink: 一時変数 `hintState` は `this.hintState = value.ok ? 'ok' : 'dead';` の 1 行に畳める（好みの範囲）。

net: -2 lines possible.

## 修正の見直し

対象: `git diff src/config.js src/scenes/demo.js`。指摘 1〜3 の直し方と、足した条件で正しい読み込みを捨てる筋が無いかだけ見た。

- 1（config.js の JSDoc）: 直っている。「30〜70 ほど」は測った値（8×8 で 31〜69、6×10 で 30〜63）と合う。
  - 好みの範囲: 「速いでも 30 秒前後」は少し長めに書いている。最初の solved までに `next()` を呼ぶ回数（置く・外す・solved）を
    同じシード 7 通りで数えると 8×8 で 51〜127、6×10 で 49〜115 だった。速い（200ms、フレームの端数で 1 手 200〜217ms）で 10〜28 秒になる。
    「30 秒以内」のほうが測った値に近い。
- 2（読み込みの受け取り）: 前の指摘の筋は塞がっている。正しい読み込みを捨てる筋は見つからなかった（Phaser の実行時の順序は静的に読んだだけで、実測はしていない）。
  - 初回: `state` は `'loading'` で `spec.key` も一致するので通る。キャッシュ済みで microtask ですぐ届く場合も、`create()` を抜けた時点で RUNNING になっているので、
    `isActive()` は `game.js` の既存の作りと同じ扱いで通る。
  - 同じ盤で入り直し: 前回の分が先に届くと、それで始まる（同じ盤の正しい表なので問題ない）。後から届いた今回の分は `state !== 'loading'` で捨てられるが、もう始まっているので失うものは無い。
  - 別の盤で入り直し: 前の盤の分が先に届けば `spec.key` が合わずに捨てられ、今回の分が `'loading'` のまま届いて始まる。今回の分が先なら、それで始まり、前の盤の分は両方の条件で捨てられる。
  - 読み込み中に離れた: `isActive()` が偽で捨てられる。入り直した `create()` が新しく読み込みを呼ぶので、届かないままにはならない。
  - `solutions.spec` は `buildSolutions()` が入れていて、`spec.key` は `config.js` の盤の定義にある。
  - 残るもの: `game.js:99-100` には同じ筋が残っている（範囲外で触っていないのは依頼どおり）。実害は未確認。
  - 好みの範囲: `src/scenes/demo.js:74` の行は 109 文字で、`src/` のほかの行（最長は `game.js:575` の 101 文字）より長い。行長の決まりは無い。
- 3（音のコメント）: 直っている。コメントは今の動き（解なしの手を置くたびに鳴る）と合っている。
  前に「作り込みすぎ」に挙げた、常に真の `&& this.hintState !== 'dead'` はそのまま残っている。コメントで意味は説明されているので、残すかどうかは好みの範囲。
