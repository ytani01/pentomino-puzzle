# TODO-082 reviewer の報告

対象: `git diff`（`src/logic.js`・`docs/UsersGuide.md`・`docs/developer.md`）。
要修正 0 件、検討 3 件、好みの範囲 1 件。

## 実測（node、Math.random、`hasSolution` を canContinue に渡してデモと同じ条件、各盤 20 回）

`src/logic.js` の写しに計測を足して走らせた
（scratchpad の `logic-inst.mjs` / `m.mjs`。src は触っていない）。

| 盤 | 置いた回数 中央値 / 最大 | singlePieceCell が当たった手 / 外れた手 | 当たったうち、控えを除かなければ 2 種類以上入るマス | 空の盤での抽選 / うち当たり |
|---|---|---|---|---|
| 8×8 | 166 / 716 | 1215 / 4004 | 450（37%） | 100 / 0 |
| 6×10 | 227 / 692 | 1281 / 4082 | 558（44%） | 54 / 0 |

参考: HEAD の `logic.js` で同じ条件だと 8×8 は 129 / 1534、6×10 は 157 / 932。
20 回だけでばらつきが大きいので、比べるのは measure 担当の結果にする。

## 検討

### 1. 当たりの 4 割は「控え（`failed`）を除いたから 1 種類に見える」マス

- 場所: `src/logic.js:862`（`singlePieceCell(choices, random)` に渡す `choices` は `failed` を除いた手）、
  JSDoc `src/logic.js:541-547`・`:698-700`、`docs/UsersGuide.md:174-176`、`docs/developer.md:253`
- 何が問題か: 上の表のとおり、当たった手の 37%（8×8）・44%（6×10）は、
  控えを除かなければ 2 種類以上のピースが入るマス。文書と JSDoc は
  「覆えるピースが 1 種類しか無い（残っていない）空きマス」「入るピースが
  1 つしか無いと分かれば」と書いており、控えを除いた結果だとは書いていない。
- なぜ問題か: 挙動は壊れていない（下の「問題なし」の 2 件目）。
  ただし「人は入るのが 1 つしか無いと分かる」という説明と、実際の当たりの
  半分近くが食い違う。「別のピースはここで試してだめだった」と読めば
  人の振る舞いとしても通るので、このままにするか、文書と JSDoc に
  「控えに入れた手は除いて数える」と足すかは管理者の判断。実害は未確認。

### 2. `sumTightCoverage()` のテストの意味が薄まる（tests 担当向け）

- 場所: `tests.html:2346-2380` 付近の `sumTightCoverage()` と、それを使う
  「狭い所を覆える手のあるピースを選びやすい抽選になっている」テスト（8×8・6×10）
- 何が問題か: このテストは今も通っているが、`total` に数える place は
  `isForcedFillStep()` で forced だけを除いている。singlePieceCell で選んだ手
  （上の実測で全体の 2 割ほど）は `DEMO.randomTightWeight` と無関係なのに
  両方の側に混ざる。
- tests 担当へ: 既に落ちている「狭い所を…唯一絞れる場面では…」のテスト
  （`tests.html:2291` 付近）を直すときと同じく、`computeChoices()` で作った
  `choices` に `singlePieceCell(choices, () => 0)` を当てて `null` でない場面
  を除くと、両方のテストが「狭い所の規則が効く場面だけ」を数えるようになる。
  `computeChoices()` は控えを除いて再現しているので、上の 1 の「控えのせいで
  1 種類に見える」場面も生成器と同じに判定できる（`singlePieceCell` の
  import も要る。`tests.html:49`）。

### 3. `singlePieceCell()` そのもののテストがまだ無い（tests 担当向け）

- 場所: `tests.html`（差分に無い）。TODO-082 のチェックリスト 6 つ目の予定どおり。
- 壊すと落ちるかを確かめたい分岐（どれも別のコードを通る）:
  - 2 種類目のピースが同じマスを覆うと、そのマスは外れる（`cell.pick = -1`）
  - 同じピースの手が何手覆っても 1 種類のまま（count だけ増える）
  - 1 種類のマスが複数あれば count の一番少ないマスを返す
  - 同数なら `random` で選ぶ（`() => 0` と `() => 0.99` で違うマスが返る）
  - どのマスも 2 種類以上なら `null`、`choices` が空でも `null`
  - 返す `pick` は `choices` での添字（`unused` での添字ではない）
- `solveStepsRandom()` 側は「1 種類しか入らないマスがある場面の place は、
  そのピースで、そのマスを覆う」を上の 2 と同じ再現のやり方で確かめられる
  （同数のときは内部の random で決まるので、候補のマスのどれかを覆う、で見る）。

## 好みの範囲

- `src/logic.js:700-701`: 「どちらも無ければ、まず「狭い所」（残りの手で覆える数が」
  で行が短く切れ、次の行へ続いている。書き足した跡が残るだけで意味は同じ。

## 作り込みすぎ

- `src/logic.js:564-575`: shrink: 一番少ないマスを集めて同数を `random` で選ぶ
  ループは `pickTightCell()`（`:588-600`）と同じ。1 種類のマスだけの
  `Map`（key → count）を作って `pickTightCell()` に渡し、`pick` は `cells.get(key)` で引けば
  約 7 行減る。Map の並びも乱数の使い方も同じなので、シードでの手順は変わらない
  （`pickTightCell()` の JSDoc「`countCellMoves()` の戻り値」は言い回しを広げる）。
  重大度は「好みの範囲」。

net: -7 lines possible.

## 問題なし（1 行ずつ）

- 判定: 2 種類目で `pick = -1`、以後は戻らない。count は全ピースの手の数で、1 種類のマスではそのピースの覆う手の数と同じ。最小の選び方・同数の乱数も TODO の記述どおり。
- 控えで 1 種類に絞られても延々とは続かない: 控えは盤面ごとに増えるだけで、そのマスを覆う手が全部控えに入ればマスは Map から消える。`movesForName` は必ず 1 手以上（当たったマスはそのピースの手から来ている）。20 回×2 盤で全部解まで届いた（最大 716 手）。
- 優先の順: forced → singlePieceCell → 狭い所。重み（touch・近さ）は `movesForName` に対して今までどおり掛かる。置いたあとの判定（`regionsFitPieces`・`hasUncoverableCell`・forced の 2 手外し）は変わっていない。
- 空の盤: 8×8・6×10 とも当たりは 0 回（上の表）。最初の手の偏りは生じない。
- 規約: `setTimeout`・モジュールの `let`・色の直書き・DOM/Phaser の持ち込みなし。JSDoc は「なぜ」を書いている。
- 範囲: 指示に無い変更なし。
- 文書: 優先の順と絞り方は JSDoc・UsersGuide・developer.md と実装で一致（控えの扱いだけ上の 1）。
