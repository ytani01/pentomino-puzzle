# TODO-070 の分担

- implementer（Sonnet 5）: `src/icons.js` に案を 3 組描き、利用者が選んだ C を正式にし、ランダムを 2 回描き直した（顔と「?」→「?」だけ）。`docs/UsersGuide.md`
- screens（Sonnet 5）: 案を撮って並べ、描き損じと HUD のほかのアイコンとの取り違えを見た。仕上げたアイコンと、撮り直した `docs/images/` も確かめた
- main: 比較の画像を案ごとに拡大して利用者に見せ、選んでもらった。`node tools/capture.mjs` で `docs/images/` を撮り直した

挙動は変わらないので reviewer は入れなかった（見込みどおり）。
依頼は `*-brief.md`、報告は `*-report.md`（2 回目以降は同じファイルに追記）。
