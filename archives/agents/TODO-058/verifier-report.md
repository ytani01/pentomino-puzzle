# TODO-058 verifier report

対象: `git diff HEAD -- README.md docs/developer.md`（未コミットの作業ツリー差分）。
コード・文書は直していない。

## 1. 起動手順の再現

`docs/developer.md` の「## 動かす」節のコマンド `python3 -m http.server 8765` を
指示どおり実行しようとしたが、**ポート 8765 が作業開始前から別プロセスに
使われていた**（PID 400235、起動時刻 2026-09-24 11:19:52、このセッションで
起動したものではない）。

- 自分が起動した `python3 -m http.server 8765` は
  `OSError: [Errno 98] Address already in use` で即座に失敗した
  （`/tmp/http8765.log` に記録）。
- そのため `curl` で得た結果（`http://localhost:8765/` → `200`、
  `http://localhost:8765/tests.html` → `200`）は、**このセッションで
  developer.md の手順どおりに起動したサーバへの応答ではなく**、
  既存の別プロセスへの応答である。手順そのものが動くかどうかは
  今回の実行では確認できていない。
- 既存プロセス（PID 400235）は自分が起動したものではないため kill していない。
  依頼の「pgrep で PID を確かめて kill する」は、自分が起動した分（今回は
  起動に失敗して存在しない）については対象が無く、実施のしようがなかった。
- **判断が要る点**: developer.md のコマンド自体（`python3 -m http.server 8765`
  という記述）が誤っているとは言えない。ポート衝突は環境側の事情であり、
  手順の正しさとは別問題。手順が実際に機能するかどうかは、ポートが空いた
  状態で改めて確認する必要がある（今回未確認）。

## 2. リンクと図のパス

一致（食い違い無し）。

- README.md の `docs/images/play.png`、`docs/images/demo.gif`、
  `docs/UsersGuide.md`、`docs/developer.md` はすべて実在する。
- developer.md の目次と本文中のアンカー
  `#動かす` `#ファイル構成` `#構成` `#画面の用語` `#テスト` `#記録の保存`
  `#全解のデータ` `#作り直す` `#サブエージェントの定義` `#github-上の設定`
  は、すべて対応する見出し（`## 動かす` `## ファイル構成` … `### 作り直す`
  など）が存在する。

## 3. 記述の落ちこぼれ（旧 README の主張が新 README／developer.md のどちらかに残っているか）

| 旧 README の主張 | 移った先 | 判定 |
|---|---|---|
| 依存は Phaser 3.90.0（CDN、バージョン固定）。npm は使わない | developer.md「動かす」節 | 残っている |
| 画像・音声ファイルを持たない（`docs/images/` は文書用、ゲームは読まない）。絵は実行時に描き、効果音は Web Audio API で合成 | developer.md「動かす」節 | 残っている |
| マウスでもタッチでも遊べる | 新 README「インストールも登録も要らない。パソコンでもスマホでも、マウスでもタッチでも遊べる」 | 残っている |
| 盤は 8×8（中央 2×2 が穴）／6×10（穴なし）の 2 つ、置けるマスは 60、ペントミノ 12 種、解 8×8=65 通り・6×10=2339 通り | 新 README は「盤は 8×8（中央に穴）と 6×10 の 2 つ。解は 65 通り／2339 通り」と要約、詳細（中央 2×2・置けるマス 60・ピース 12 種の記号 F I L N P T U V W X Y Z）は docs/UsersGuide.md に残る | 残っている（新README は簡略化、詳細は UsersGuide 側） |
| 全解をあらかじめデータとして持つので、おまかせ・ヒント表示は探索せず待たされない | 新 README「詰まったら…どちらも待たされない」に要約、仕組み（`src/data/`）は developer.md「動かす」節 | 残っている |
| デモ（コンピューターが探索してピースを置いたり外したりする） | 新 README「## デモ」節 | 残っている |
| 遊ぶのにも公開するのにも Node.js は要らない。要るのは全解データを作り直すときだけ | developer.md「動かす」節（`tools/capture.mjs` の場合も要ると追記されている） | 残っている |
| `file://` では動かず、ローカルサーバ経由で開く | developer.md「動かす」節 | 残っている |
| ビルド工程は無い | developer.md「動かす」節冒頭 | 残っている |
| UsersGuide.md へのリンク | 新 README「## デモ」節末尾 | 残っている |
| developer.md へのリンク | 新 README「## 開発者向け」節 | 残っている |
| ライセンス MIT | 新 README「## ライセンス」節（変更なし） | 残っている |

すべて残存を確認した。落ちているものは無い。

## 4. 新 README の事実の正しさ（docs/UsersGuide.md との突き合わせ）

一致（食い違い無し）。

- 「ピースをタップすると向きが変わる（回転と裏返し）」→ UsersGuide.md の
  操作表「次の向きへ（回転と裏返しを順に巡る） / ピースをタップ」と一致。
- 「置く場所は多少ずれていても、近くの置けるマスへ吸い付く」→ UsersGuide.md
  に同文言そのままあり。
- 「詰まったらヒント表示で『まだ解けるか』が分かり、おまかせで 1 枚ずつ
  置いてもらえる。どちらも待たされない」→ UsersGuide.md の HUD ボタン表
  （おまかせ／ヒント表示の説明）および「全解のデータを引くだけなので
  待たされない」の記述と一致。
- 「クリアすると、それが何番目の解かが出る。見つけた解と最短時間は記録に
  残り、全解のうちいくつ見つけたかを見返せる」→ UsersGuide.md「記録の画面」
  節（最短時間・見つけた解・達成度の記述）と一致（本文は目視のみ、
  clear.js 等の実装までは今回確認範囲外）。
- 「途中でやめても、次はつづきから再開できる」→ UsersGuide.md「## つづきから」
  節と一致。

## 確かめられなかったこと・判断できないこと

- developer.md「動かす」節のコマンドが実際に機能するかどうかは、ポート
  衝突のため今回未確認（上記 1 参照）。別ポートで確かめ直すか、
  衝突していた既存プロセスの扱いを利用者に確認したうえで再実行するのが
  良いと考えるが、これは提案であり判断はしていない。
- 新 README の「クリアすると、それが何番目の解かが出る」の実装側の挙動
  （clear.js 等）までは踏み込んでいない。依頼の範囲（README と
  UsersGuide.md の記述の突き合わせ）では一致を確認した。

## 1-2. 追記: ポート 8766 でやり直した結果

管理者の指示で、ポートを 8766 に変えて起動手順を再現した（8765 の既存
プロセス PID 400235 には触っていない）。

- `python3 -m http.server 8766` をリポジトリのルートでバックグラウンド起動
  → 正常に起動（8765 と違い衝突なし）。
- `curl --max-time 5` の結果:
  - `http://localhost:8766/` → `200`
  - `http://localhost:8766/tests.html` → `200`
- 応答本文の `<title>` 突き合わせ（別ディレクトリを配信していないことの確認）:
  - `index.html` の `<title>` は `Pentomino Puzzle`。`/` の応答本文にも
    `<title>Pentomino Puzzle</title>` が含まれていた（一致）。
  - `tests.html` の `<title>` は `Pentomino Puzzle — テスト`。
    `/tests.html` の応答本文にも `<title>Pentomino Puzzle — テスト</title>` が
    含まれていた（一致）。
  - リポジトリのルートを配信しており、別のディレクトリを返していないことを確認した。
- 自分が起動したサーバのみ `pgrep -af "http.server 8766"` で PID
  （447514）を確かめて `kill` し、停止を確認した。8765 の既存プロセス
  （PID 400235）は指示どおり触っていない（`pgrep -af "http.server 8765"` で
  起動したままであることを確認）。

これにより、developer.md「動かす」節のコマンドが指示どおりに機能すること
（起動・200 応答・正しいファイルの配信）を確認できた。1 節に書いた
「手順が実際に機能するかは未確認」という懸念は、ポートを変えた実行で解消した。
