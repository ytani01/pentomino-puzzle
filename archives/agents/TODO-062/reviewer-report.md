# TODO-062 reviewer-report

対象: `git diff`（`src/logic.js`・`src/config.js`・`tests.html`・`docs/UsersGuide.md`・`docs/developer.md`）。
実測は Node（`tools/window-shim.mjs` 経由で `src/` を import）で行った。スクリプトは
セッションのスクラッチパッドに置いただけで、リポジトリには残していない。

## 要修正

なし。

## 検討

### 1. `docs/UsersGuide.md:114`: 「盤の上を飛び回らず近くから順に埋まっていく」は実測より強い言い方

- **問題:** 近さの重みは、ピースを一様に選んだ**あと**、そのピースの置き方の中だけで掛かる
  （`src/logic.js:624-634`。`pickOne(choices)` の後）。選んだピースに近くの置き方が無ければ、
  遠くに置くしかない
- **根拠（実測。40 シード × 1000 手、`regionsFitPieces`、抽選の place のうち forced を除く）:**

  | 盤 | power | 平均距離 | 距離 1 | 距離 3 以上 |
  |---|---|---|---|---|
  | 8×8 | 2 | 2.77 | 39.0% | 46.0% |
  | 8×8 | 0 | 3.13 | 29.7% | 56.1% |
  | 6×10 | 2 | 2.49 | 42.6% | 38.0% |
  | 6×10 | 0 | 2.66 | 37.3% | 42.7% |

  距離 3 以上に置いた手のうち約 43%（8×8: 915/2127、6×10: 923/2219。10 シード、
  `failed` の控えは無視して数えた）は、**選んだピースに距離 3 未満の置き方が 1 つも無かった**。
  つまり power の値を上げても、この分は近くならない
- **どうするか:** 文書の言い方を実際に合わせて弱める（「飛び回りにくい」など）か、
  近さをピースの選び方にも効かせるかを管理者が決める。後者は TODO-061（ピースの選び方を
  変える項目）と重なる。power の値の良し悪しは見ていない（依頼どおり）。
  実害（画面でどう見えるか）は未確認

### 2. `tests.html:1715-1741`: 近さのテストが重い（Node で約 37 秒）

- **問題:** 1 盤あたり 40 シード × 1000 手 × 2（power あり・なし）＝ 8 万手を回す。
  既存のランダムのテストは 1 シードで 4000〜8000 手（`tests.html:1309`・`1356`・`1452`）で、
  これの 10 倍以上
- **根拠（実測）:** 同じ回し方（`isForcedFillStep` 込み）で 8×8 が 9.5 秒 + 9.6 秒、
  6×10 が 8.6 秒 + 8.8 秒。`solveStepsRandom()` を回すだけでも 1 盤・1 power あたり 8〜9 秒。
  ブラウザでの所要時間は未確認
- **なぜ要るか:** 効きが弱い（指摘 1。6×10 は平均 2.49 と 2.66）ので、シードを減らすと
  逆転する（tests-report に 1 シードで逆転したとある）
- **どうするか:** 管理者が判断。選択肢は、(a) 1 シードあたりの手数を減らす（最初の数十手だけ
  見る）、(b) テストの中では power を大きい値（例: 8）にして差をはっきりさせ、シードを減らす、
  (c) このまま。(b) でも「距離の項を外すと落ちる」は保てるはず（未確認）

### 3. `tests.html:1352-1355`・`1392`・`1396-1399`・`1404-1405`: forced の 1 手目を除く分岐は、このテストでは一度も効かない

- **問題:** このテストの `canContinue` は `regionsFitPieces`（空きの大きさが 5 の倍数か、
  `src/logic.js:367-369`）。5 マスの穴をちょうど埋めても他の空きの大きさは変わらないので、
  forced な手で解なしになったなら置く前から解なしで、1 手目の remove の `ok` は必ず偽になる。
  `step.ok` の条件だけで既に対象から外れている
- **根拠（実測）:** 40 シード × 4000 手で、forced で解なしの手の 1 手目の remove は
  8×8 で 261 件・6×10 で 325 件あり、`ok:true` は 0 件。tests-report にも
  「打ち切り末尾のガードだけで通った」とある
- **狙いを弱めているか:** 弱めていない（除く remove はもともと `ok:false` なので、確かめる
  場面は減っていない）
- **どうするか:** 消してよい（約 8 行と 4 行のコメント）。残すなら、`regionsFitPieces` では
  効かず、別の `canContinue` に差し替えたときの備えだとコメントに書く

### 4. `tests.html:1716-1733`: テストが `config.js` の `DEMO` を書き換える

- **問題:** `DEMO.randomNearPower` を一時的に書き換え、`finally` で戻している。
  `logic.js` の探索が共有の設定オブジェクトを実行時に読むので、テストから差し替えるには
  これしか無い
- **根拠:** 規約違反ではない（`CLAUDE.md` が禁じるのは `src/` のトップレベルの書き換わる
  `let`）。`finally` で戻しているので他のテストへの漏れは無い（コードを読んで確認）
- **どうするか:** 今のままでよい。`solveStepsRandom()` の引数（例: 4 つ目に `{ nearPower }`）で
  受ける形にすればテストが設定を書き換えずに済むが、呼び出し元（`demo.js`）と
  テストの引数が増えるので、得は小さい

## 問題の無い観点

- 重みの式: `touch / (1 + d) ** power` は `config.js` の JSDoc の説明と一致。置ける手は盤上の
  手と重ならないので `d >= 1` で、0 除算や重み 0 は起きない。`power = 0` で従来と同じ重みになる
- 「直前の手」＝ `stack` の最後: 外したあとは 1 つ前の手になり、外した手の隣へ戻りやすい。
  目的に合う。スタックが空（最初の手・全部外した後）は距離の項を掛けない分岐があり正しい
- TODO-059: 近さは `touchWeight()` に掛けるだけで、`touchingEdges()` の数え方は不変。
  「最初の place」のテストはスタックが空なので影響を受けない
- TODO-060・TODO-066: 抽選の分岐の中だけを変えており、forced の分岐と、置いた後に外す分岐
  （`src/logic.js:641-650`）は変わっていない
- `logic.js` が `DEMO` を import すること: 規約（数値は `config.js` に集約、`logic.js` に
  Phaser・DOM を持ち込まない）に反しない。`config.js` は既に import 済みで、依存は増えない
- 足したテストの強さ: 距離の項を外すと平均が一致して落ちることを tests 担当が確かめている。
  `moveDistance` が常に 0 を返す壊れ方でも `0 < 0` で落ちる（コードを読んで確認）。
  固定シードなので揺れない
- 打ち切り末尾のガード（`tests.html:1410`）: 他のテスト（`1204`・`1281`）と同じ形で、
  `checkedRunEnds` を数える位置もガードの後で正しい
- JSDoc: `moveDistance()`・`solveStepsRandom()`・`DEMO.randomNearPower` とも「なぜ」を書いており、
  今の動きと合う
- `docs/developer.md:237`: 今の動きと合う
- 範囲: 指示どおりの 5 ファイルだけ。`VERSION` の行・全解データには触れていない

## 作り込みすぎ

- `tests.html:1352-1355,1392,1396-1399,1404-1405`: delete: forced の 1 手目を除く分岐
  （`isForcedFirstRemove`・`boardBeforePlace`・`skip`）。`regionsFitPieces` では常に `ok:false`
  なので何も除いていない（上の指摘 3）。これを消すだけで同じ結果になる。重大度は「検討」
- それ以外は無し（`moveDistance` は既存の関数に同じものが無く、`touchWeight` と同じく
  そのまま書くのが最短）

net: -12 lines possible.
