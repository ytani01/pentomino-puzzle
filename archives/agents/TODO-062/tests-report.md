# TODO-062 tests-report

## 直したテスト

- 「外す連なりが ok:true で止まったとき、まだ（控えていない）置ける手が残っていれば、
  次は必ず place になる（8×8/6×10）」（`tests.html`）
  - 実際に落ちた原因を `solveStepsRandom()` を手元で再生して特定した。実装担当の
    見立て（TODO-066 の 2 手外しが原因）とは違い、**この不変条件のチェックに
    「limit で打ち切った末尾」を除くガードが無かった**ことが直接の原因だった
    （他の似たテストには `if (i >= steps.length) break;` の形で入っている）。
    STEPS=4000 の最後の手が、たまたま「まだ置ける手が残っている ok:true の
    remove」に該当し、次の手（本当は place のはず）が配列に無いだけで
    `assert(next && …)` が落ちていた
  - ただし TODO-066 の 2 手外しの 1 手目（forced な解なしを外す 1 手目。
    `if (forced.length > 0 && !ok) { yield undoLast(); if (stack.length > 0) yield undoLast(); }`
    の 1 手目）も、間に place を挟まず続けて 2 手目の remove へ進む作りで
    この不変条件と噛み合わないため、依頼どおり `isForcedFillStep()` で見分けて
    その 1 手だけ対象から除く形に直した（2 手目は通常どおり確かめる）
  - 直した結果、限界打ち切りのガードだけでも今回のシードでは通ったが、
    forced の 1 手目の除外もあわせて入れてある（依頼の狙いを保つため）

## 足したテスト

- `moveDistance()`（3 件）: 隣り合う 2 手で距離 1、同じマスを含む 2 手で距離 0、
  離れた例で手で数えた最短マンハッタン距離と一致
- 近さが効いていることの比較（8×8・6×10 の 2 件）: `DEMO.randomNearPower` を
  一時的に書き換え（`finally` で戻す）、固定シードで `solveStepsRandom()` を
  回し、抽選で選ばれた place（`forced` を除く）の直前の手からの距離の平均を、
  既定値のときと 0 にしたときで比べた
  - 1 シードだけでは 6×10 でたまたま逆転することがあったため、複数シード
    （1〜40）で積み上げた平均で比べる形にした（`touchingEdges` の既存テストと
    同じ考え方）

## 壊すと落ちるか

`fetch` で `src/logic.js` を読み込み、重みの行を `touch`（距離の項を外したもの）
に書き換えた版を Blob URL 経由で一時的に import し、同じ比較を実行して確認した
（ディスク上の `src/` は変更していない）。結果、`randomNearPower` を効かせても
効かせなくても平均距離が完全に一致し（2.6465… = 2.6465…）、近さのテストは
落ちることを確認した。元の `src/logic.js` には手を加えていない。

## 全件の件数

`python3 -m http.server 8765` 経由で `tests.html` を開き、**327 件すべて通った**
（既存 322 件 → 直した 1 件はそのまま数え、新規 5 件を追加）。

## レビュー指摘 2・3 への対応（追記）

### 指摘 3: forced の 1 手目を除く分岐を削除

「外す連なりが ok:true で止まったとき…」のテストから `isForcedFirstRemove`・
`boardBeforePlace`・`skip` まわりを削除した。`regionsFitPieces` では forced な
解なしの 1 手目の remove の `ok` は必ず偽になり、`step.ok` の条件だけで
既に対象から外れているため、この分岐は何も除いていなかった（指摘どおり）。

### 指摘 2: 近さのテストを軽くする

テストの中でだけ `DEMO.randomNearPower` を大きい値（8）に上げ、既定の 2 では
差が小さくシードを減らすと逆転しうる問題を避けた。あわせてシード数を
40→5、1 シードあたりの手数を 1000→300 に減らした。

- Node（`tools/window-shim.mjs` 経由、レビュー担当と同じ実測環境）で計測:
  8×8 が 693.5ms、6×10 が 549.8ms（いずれも 1 盤 1 秒以内）
  - 比較: 8×8 平均距離 2.322（近さあり） vs 3.443（近さなし）、
    6×10 は 2.381 vs 2.859。軽くしても差ははっきり出ている
- 距離の項を外すと落ちるかを、軽くした後の設定（シード 5・300 手・power 8/0）で
  再確認: Blob/ローカルコピー経由で `src/logic.js` の重みの行を `touch` だけに
  した版を一時的に import し、同じ比較をすると平均距離が完全に一致し
  （8×8: 3.4427 = 3.4427、6×10: 2.8588 = 2.8588）、`pass: false` となることを
  確認した。`src/` のファイル自体は変更していない

### 全件・所要時間

`python3 -m http.server 8765` 経由で `tests.html` を開き、**327 件すべて通った**
（件数は変わらず。既存 322 件・直した 1 件・新規 5 件のまま。テストの中身のみ軽量化）。
