# TODO-097 見た目の確認（screens）

対象: デモの HUD、探し方ボタン（左から 6 番目）に出るキャプション
（「人間的」「機械的」）。コードは直していない。

## 撮った画像

すべて `/home/ytani/tmp/playwright-mcp/` 配下（絶対パス）。

| ファイル | 大きさ | 盤 | 状態 |
|---|---|---|---|
| `demo-844x390-human.png` | 844x390（横） | 8x8 | 開いた直後（人間的） |
| `demo-844x390-machine.png` | 844x390（横） | 8x8 | 切り替え後（機械的） |
| `demo-844x390-tooltip.png` | 844x390（横） | 8x8 | 機械的 + ツールチップ表示 |
| `demo-390x844-human.png` | 390x844（縦） | 8x8 | 開いた直後（人間的） |
| `demo-390x844-machine.png` | 390x844（縦） | 8x8 | 切り替え後（機械的） |

## はみ出し・重なりの確認

`strategyButton` の子要素を `getBounds()` で読み出した数値（ボタン左上を原点とした相対座標）。
横・縦のどちらも同じ値（ボタンの大きさは画面サイズに依らないため）。

- ボタンの高さ: `37`
- アイコン（Graphics, `icon.y`）: `-6`
- キャプション（Text）の `top` 相対 Y: `23`、`bottom` 相対 Y: `37`、文字の高さ: `14`
- 「人間的」「機械的」どちらも同じ数値

キャプションの下端（相対 Y = 37）はボタンの高さ（37）と一致し、
枠の外へは出ていない（超えてはいない。ちょうど下端に接している）。
アイコンの `y` は `-6`（ボタン中心より上）で、キャプションの上端（相対 Y = 23、
中心 18.5 から見て下寄り）との間に十分な間隔があり、重なりは無い。
スクリーンショットを目視でも確認し、文字は枠内に収まり、アイコンとも
離れて見える（`demo-844x390-tooltip.png`、`demo-390x844-machine.png` で確認）。

## 文言の確認

- 画面下の説明文（Demo シーンの Text）: `探し方は「機械的」と「人間的」の 2 種類` — 一致
- ツールチップ（`strategyButton` 押下後の `scene.tooltip` 内 Text）: `探し方: 機械的` — 一致

## コンソール

横・縦とも 0 errors。warning は 4 件、いずれも
`GL Driver Message ... GPU stall due to ReadPixels` という WebGL ドライバの
性能情報で、今回の変更とは無関係（本文はログに記録済み、上記に転記）。

## 判断が要る点

なし。数値・文言・見た目のいずれも問題は見つからなかった。
