# TODO-067 reviewer 報告

対象: `git diff`（`src/logic.js`・`tests.html`・`docs/UsersGuide.md`・`docs/developer.md`）。

## 要修正

1. `src/logic.js:524-527`（`solveStepsRandom()` の JSDoc）
   - 問題: 「ただし、次の **2 つ**は行き詰まりを待たずに…人も置いた瞬間に明らかな詰みだと
     気づくのは**この 2 つだけ**で」のまま、下の箇条書きが 3 つになった
   - 根拠: 差分で足された箇条書き（531-532 行）と、その直前の本文（524-527 行）を読んだ
   - どうする: 「次の 3 つ」「この 3 つだけ」にする（数を書かない言い方でもよい）

## 検討

2. `src/logic.js:656-658`（作り込みすぎ・shrink）
   - 問題: 置き済みの名前を `PIECES.map((p) => p.name).filter((name) => !unused.includes(name))`
     で作っているが、置き済みの手はそのまま `stack` にある（`place` で push、`undoLast` で pop）
   - どうする: `forcedPlacements(board, stack.map((m) => m.name)).length > 0` で同じ。3 行が 1 行になる

3. `tests.html:1599-1653`（足した 2 件。作り込みすぎ・重複）
   - `置いた直後に置き済みのピースと同じ形の…（TODO-067）`（1629 行〜）は、
     書き換えた `置ける手が尽きて 1 手ずつ外す並び…`（1188 行〜、既定の判定）と主張が同じ
     （`placedFormRegion` なら次が同じ名前の remove、かつ 1 回以上起きる）。
     しかも 1451 行のテストが同じ主張を「置ける手が尽きた remove ではない」と絞ったうえで
     確かめている。同じ分岐を 3 か所で確かめていることになる。消しても分岐の検出力は落ちない
   - `残りのピースと同じ形の 5 マスの空きそのものは、置き済みのピースの形とは一致しない`
     （1599 行〜）は 12 種の形が互いに違うという性質の確認で、`solveStepsRandom()` の
     分岐を消しても落ちない（tests-report の「分岐を無効にして落ちたのは 8 件」の内訳からも外れる）。
     `forcedPlacements()` の性質なら、盤を手で作った小さなテストで足り、4000 手の探索を回す必要は無い
   - どうする: 1629 行のテストは削る。1599 行のテストは削るか、手で作った盤 1 枚に置き換える

4. `tests.html:1192`（手数 3000 → 8000）
   - 問題: 重い。探索だけで 1 盤・1 判定あたり +1.0〜1.4 秒、このテスト 4 件で計 +4.7 秒
     （node で `solveStepsRandom()` を回して実測。3000 手: 664/764/481/945 ms、
     8000 手: 1736/1973/1474/2372 ms。8×8 既定/常に偽、6×10 既定/常に偽の順）。
     テスト内の判定の分はさらに乗る（未計測）
   - 理由は tests-report のとおり `sawForcedDoubleUndo` が 3000 手で出なくなったため
     （新しい分岐で乱数の進み方が変わった）。狙いが弱まったわけではない
   - どうする: 二重 remove は `makeFailAfterForcedFill()` を使う専用のテスト（1530 行〜）が
     確実に起こして確かめているので、このテストの `sawForcedDoubleUndo` の assert を外して
     3000 手に戻すのが軽い。残すなら現状のまま（判断は管理者）

5. `tests.html` のテスト名・コメントに TODO-060 だけの言い方が残っている
   - 1188 行・1264 行のテスト名「（小さな空きで即座に外れる 1 手は除く。…）」:
     今は置き済みのピースの形の空きの 1 手も除いている
   - 1186-1187 行・1267-1269 行のコメントも小さな空きだけを挙げている
   - 1462 行のコメント「…小さな空きによる即座の remove と区別できない」: このテストが
     今区別しているのは TODO-067 の即座の remove と、置ける手が尽きた remove
   - どうする: 「その場で外れる 1 手は除く」のように、理由を並べない言い方にする

6. `src/logic.js:338`（`forcedPlacements()` の JSDoc）
   - 問題: 「残りのピース `names`」とあるが、今回は置き済みの名前を渡して呼んでいる
   - どうする: 「ピース `names`」にするか、置き済みを渡す使い方（TODO-067）を 1 行足す

7. 同じ形の空きが 2 つあるとき（実害は未確認・境界線上の判断）
   - `forcedPlacements()` は同じ名前を 1 回しか返さない（JSDoc にも「同じピースが 2 つの空きに
     当たる盤面は解なし」とある）。残りのピースの形の空きが 2 つできた手は TODO-067 でも
     TODO-060 でも外れず、TODO-066 で片方を埋めた直後に、もう片方が「置き済みの形の空き」
     になって TODO-067 で 1 手だけ外れる。TODO.md の「残りの形なら TODO-066 で埋め、
     置き済みの形ならこの項目で外す」の 2 分けには収まらない場面がある
   - 実際に起きるか、見た目に困るかは確かめていない。仕様の範囲外なので報告だけ

## 問題の無かった観点

- 分岐の置き場所: TODO-066 の二重 remove（`forced && !ok`）より後ろの `else if` なので、
  二重 remove を先に済ませる順は保たれている。TODO-060 とはどちらも 1 手外すだけで、順は結果に効かない
- 条件: `forcedPlacements(board, 置き済み名)` は形だけを見るので、仕様（置き済みの形の閉じた 5 マス）どおり。
  外した手は `undoLast()` で `failed` に入る（TODO.md の決めごとどおり）
- TODO-062 の近さの重み: 触っていない
- 規約（`setTimeout`・トップレベルの `let`・色の直書き）: 該当なし
- `docs/UsersGuide.md`・`docs/developer.md`: 実装と合っている。「2 つ目はその前に置いた手もまとめて外す」の数え方も正しい。
  `rg -n "合う.*無い|合わない" src/ docs/ tests.html` で古い言い方（合うピースが無い 5 マスは待つ）は残っていない
- 反転した 2 件（1451 行〜）: 元の「即座に外さない」は TODO-067 の仕様と両立しないので、反転は正しい。
  `hasUnfailedMove()` で「置ける手が尽きた remove」を除く絞り込みが残っているので、分岐を消すと落ちる
  （tests-report の壊して落ちる確認と合う。こちらでは壊して回していない）
- 範囲: 指示に無い変更は無い

## 作り込みすぎ

- `src/logic.js:656-658`: shrink: 置き済みの名前を `PIECES` と `unused` から作り直している。`stack.map((m) => m.name)`、1 行（上の 2）
- `tests.html:1629-1653`: delete: 1188 行・1451 行のテストと同じ主張。消すだけ（上の 3）
- `tests.html:1599-1627`: delete: 分岐を消しても落ちない、形の性質の確認を 4000 手の探索で回している（上の 3）

net: 約 -55 行
