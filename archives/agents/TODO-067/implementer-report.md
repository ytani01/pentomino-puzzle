# TODO-067 implementer 報告

## 変更したファイル

- `src/logic.js:526-527` JSDoc の箇条書きに「置き済みのピースと同じ形の 5 マスの空き」の項目を追加
- `src/logic.js:656-662`（`solveStepsRandom()` 内、TODO-060 の小さな空きの `else if` の後ろ）
  新しい `else if` を追加。置いたあとの盤で、盤に置き済みのピース名（`PIECES` のうち
  `unused` に無いもの）を `forcedPlacements(board, 置き済み名)` に渡し、1 件以上返れば
  `yield undoLast()` で直前の 1 手を外す。判定は形だけを見る `forcedPlacements()` を
  そのまま流用（TODO-066 用の呼び出しとは別に、置き済み名を渡す呼び出しを追加）
- `docs/UsersGuide.md:116-120` ランダムの説明に、置き済みのピースと同じ形の空きの場合を追記
- `docs/developer.md:239` 表の説明に同じ内容を追記（TODO-067 を参照に追加）

## 検証

- `node tools/gen-solutions.mjs --check`: 通過（終了コード 0）
- `tests.html`（`python3 -m http.server 8765` 経由、Playwright で自動取得）:
  319 件通過 / 8 件失敗。失敗した 8 件は次の観点の既存テストで、いずれも
  「この分岐が無かった前提」のもの（ブリーフの指示どおり原因の見立ては書かず、
  どのテストかだけ記す）。
  1. 置ける手が尽きて 1 手ずつ外す並びで、途中の remove が ok:false になる確認
     （既定 8×8／常に偽 8×8／既定 6×10／常に偽 6×10 の 4 件）
  2. 常に偽の判定で remove の連なりの終わりに盤のピースが残っていない確認（8×8／6×10 の 2 件）
  3. 「合うピース無しの 5 マスの空きは即座に外さない」確認（8×8 の X／6×10 の T、2 件）
- 実測: 固定シード（mulberry32、seed 12345）で `regionsFitPieces` を渡し、
  `solveStepsRandom()` を 8×8・6×10 それぞれ 5000 手回した。一時的にこの分岐へ
  カウンタを仕込んで数え、測定後に削除した（コミットには残っていない）。
  - 8×8: 5000 手中 151 回この分岐で外した
  - 6×10: 5000 手中 90 回この分岐で外した

## 判断が要る点・気づいたこと

- `tests.html` の 8 件の失敗はテスト側の想定（この分岐が無い前提）が古くなったために
  起きているとみられるが、原因の切り分けはしていない（範囲外・別担当のため）
