# TODO-067 tests への依頼

## 背景

`solveStepsRandom()` に、置いた直後に「置き済みのピースと同じ形の閉じた 5 マスの空き」ができたら
その手をその場で 1 手外す分岐を足した（`git diff src/logic.js`。仕様は `TODO.md` の TODO-067 の節）。
実装担当の報告（落ちた既存テスト 8 件の一覧）: `archives/agents/TODO-067/implementer-report.md`。

## やること

1. **落ちた既存 8 件を直す**。新しい即座の外し（仕様どおり）を前提に入れていないためと見込まれる。
   TODO-060・066 の即座の外しを見分けるフラグの分け方に揃え、この分岐用の見分けを足す。各テストの元の狙いは弱めない。
   直した理由をテストごとに 1 行で報告する
2. **足す**:
   - 置いた直後に置き済みのピースの形の 5 マスの空きができた place の次は、必ずその手の remove（1 手だけ。
     次の手は remove が続かないか、続いても別の理由で説明がつく）。固定シードで数千手回して確かめ、確かめた件数が 0 でないこと
   - 残りのピースの形の 5 マスの空き（TODO-066 で埋める側）では、この分岐で外さないこと
3. 壊すと落ちるか: 新しい分岐（`else if (forcedPlacements(board, 置き済み…)`）を消したとき、2 のテストが落ちることを確かめて戻す
4. 足すテストは 1 盤 1 秒以内に収める（TODO-062 の反省）

## 保つもの

- `src/` は触らない。既存テストの狙いを弱めない。既存の書き方（`group`・`mulberry32`）に合わせる

## 完了条件

`python3 -m http.server 8765` 経由で `tests.html` の全件が通る（件数を報告）。

## 報告

`archives/agents/TODO-067/tests-report.md`。返事は 5 行以内。
