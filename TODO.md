# TODO

**残っている項目: TODO-008,TODO-012,TODO-013,TODO-015,TODO-016,TODO-017,TODO-19**
これまでに 12 件を決着させた。新しく足すときは「完了済み」の上に節を作る。
**番号は `TODO-020` から。**

## 着手順(案)

着手する順は **015 → 017 → 013 → 012 → 008 → 016** を勧める。理由:

- 015（ピースの色）を先にするのは、**008 の記録画面が完成形を縮小して
  描く**ため。色の組が決まっていないと、記録した時点の色で描くのか
  今の設定で描くのかを後から作り直すことになる。013 で決める「詰みを
  盤に印で出すか」も、単色のガラスの上に乗るかどうかで見せ方が変わる。
  他とは独立していて短く、区切りをつけやすい
- 013（解の有無の表示）は HUD にボタンが 1 つ増えるので、メニューバーが
  2 段になる 011 の後にする。009 で測った「6×10 の詰みの盤面は求解に 4.2 秒」が
  そのまま待たせ方を決める材料になる。「このモードを使ったか」を記録に
  残すかどうかが 008 に渡るので、008 より先
- 012（対称な解の重複除去）は 008 で記録の重複を省くかどうかの前提になる。
  純関数だけなので画面の作業とは独立に進められる。検討の結果をそのまま
  008 の設計に持ち込めるよう、008 の直前に置く
- 008（クリア記録）は、009 で記録の保存先を盤ごとに分けたので、
  履歴も盤ごとに持つかどうかから決めることになる
- 016（既に出した解を避けるヒント）は、008 の記録と 012 の代表形が
  そろっていないと「被っている」を判定できないので最後

---

## TODO-008. クリアした結果を記録し、あとから見返せるようにする

- [ ] 履歴の保存形式を決め、`src/storage.js` に足す
- [ ] 既存の最短時間（盤ごとの `BOARDS[key].storageKey`）との関係を決める
- [ ] タイトルから開く「記録」画面を新しいシーンとして作る
- [ ] `tests.html` に、保存して読み出すと同じものが返るテストを足す

1 件に持たせるのは「日時・所要時間・盤のサイズ・ヒント使用の有無・
完成した盤面の配置」。盤面は 60 マスのピース名の並びとして持つ
（`logic.js` の盤の表現をそのまま文字列にする）。件数の上限を決めて、
古いものから捨てる。記録画面では一覧と、選んだ 1 件の完成形を縮小して出す。

最短時間を履歴から導くか、今のキーを残して両方持つかは着手時に決める。

進め方: 3 ファイル以上にまたがり、実装・テスト・画面がまとまって要るので、
**サブエージェント（Agent Team）を編成する**。分担は着手前に示す。

---

## TODO-012. 回転・反転で同じになる解を、重複と見なす仕組みを検討する

- [ ] 盤の対称性（8×8 は 90 度回転を含む 8 通り、6×10 は 4 通り）を確かめる
- [ ] 完成した盤面から、対称なもののうち 1 つを選ぶ関数を `logic.js` に足す
- [ ] `tests.html` に、対称な盤面が同じ代表形に落ちるテストを足す
- [ ] この仕組みを画面のどこで使うかを決める（使わないなら理由を残す）

完成形を回転・反転すると同じ形になる解があり、そのままでは別の解として
数えてしまう。よく使われる手は **X ピースの位置で代表形を決める**やり方
（X を盤の一方の側だけに置いた解を代表とする）だが、8×8 の中央 2×2 穴の盤で
何通りに絞れるかは実際に数えて確かめる。

これは**検討が主**の項目で、まず「対称な盤面を同じものと見なす関数」を
作るところまで。使い道は 008（記録）で同じ解を重ねて残さないこと、
あるいは解の総数を数えることだが、そこまでやるかは検討の結果で決める。

進め方: `logic.js` と `tests.html` だけだが、数え上げの筋を立てる所が本体
なので、**サブエージェントは編成しない**。モデル Opus、effort high。

---

## TODO-013. 置くたびに解の有無を教えるモードを足す

- [ ] HUD に切り替えのボタンを足す（既定は切）
- [ ] 置く・外すのたびに `solver.js` の `solve()` を呼び、結果を表示する
- [ ] 手が止まるほど遅くないかを実測し、遅ければ待たせ方を決める
- [ ] 上限（`SOLVER_LIMIT`）で打ち切ったときの言い方を決める
- [ ] `tests.html` に、詰みの盤面を詰みと判定するテストを足す

`solver.js` はヒントで既に使っており、解が無いのか上限で打ち切ったのかを
`reason` で返す。それをそのまま「この先も解ける／もう解けない」の表示に使う。

懸念: `config.js` に書いてある実測では、1 個だけ置いた詰みの盤面が最悪で
448,217 回。毎手これを回すと画面が固まりかねない。**同期のまま回して
実測する**のが先で、遅ければ「少し遅れて出す」「上限を下げる」などを決める。

決めること: 詰みをどう見せるか（メッセージだけか、盤に印を出すか）と、
このモードを使ったことをクリア記録に残すか（008 のヒント使用の有無と同じ扱い）。

進め方: `game.js`・`config.js`・`tests.html` にまたがるが量は多くないので、
**サブエージェントは編成しない**。モデル Opus、effort medium。

---

## TODO-015. ピースの色を選べるようにし、ガラスっぽい見た目を足す

- [ ] 色の組を `config.js` に定義する（「ガラス（単色）」と「12 色」）
- [ ] ガラスっぽいマスの描き方を `boot.js` で試し、見え方を決める
- [ ] 選んだ組をテクスチャ名と外周の色に反映する
- [ ] タイトル画面に選ぶところを足し、`storage.js` に覚えさせる
- [ ] スマホの小さい画面で、隣り合うピースの境目が分かるかを撮って確かめる

TODO-007 でピースに外周の線を付けたので、**塊の見分けに色を使わなくても
済むようになった**。そこで、12 個とも同じ色（既定はこちら）と、
今までどおりピースごとに色を変える組を、タイトルで選べるようにする。

ガラスっぽさは画像を持ち込まずに出す（アセットを持たない方針）。
今のマスは「上と左を明るく、下と右を暗く」の帯 2 本だけ（`boot.js` の
`makeTile`）なので、ここに半透明・内側の明るい縁・斜めの光の筋あたりを
足して試す。**盤のマス・穴・ゴースト（`COLORS.boardCell` / `hole` /
`ghost`）と紛れないこと**が条件。

決めること:

- テクスチャ名（`TEX.piece(name, cell)`）に色の組をどう入れるか。単色なら
  12 種で 1 枚に減らせるが、`game.js` は名前ごとに引いている
- 外周の色（`game.js` の `darken(piece.color, OUTLINE.darken)`）を単色のとき
  どうするか。同じ色どうしが接するので、**外周だけが境目になる**
- 選んだ組の保存先。盤ごとの記録（`BOARDS[key].storageKey`）とは別に持つ
- ヒントの点滅や、置けない位置の見せ方が色に頼っていないかの確認

進め方: `config.js`・`boot.js`・`game.js`・`title.js`・`storage.js` に
またがるが、**見た目を撮って直す往復が本体**で分担しづらいので、
**サブエージェントは編成しない**。モデル Opus、effort medium。

---

## TODO-016. ヒントは、既にクリアした解と別の解へ導く

- [ ] 既にクリアした解を `solver.js` へ渡す形を決める（008 の記録から作る）
- [ ] `solve()` に「この解は避ける」を足す（探索順を混ぜる／見つけた解を捨てる）
- [ ] 避けきれないとき（そこまでの配置が既出の解にしか繋がらない）の扱いを決める
- [ ] ヒントを何度も押したときに毎回同じ手にならないかを確かめる
- [ ] `tests.html` に、既出の解を渡すと別の解を返すテストを足す

同じ盤を何度も解くと、ヒントが毎回同じ解へ導く。既にクリアした解は
避けて、まだ出していない解へ導きたい。

前提が 2 つある。**008（クリア記録に完成した盤面を残す）と、012（回転・
反転で同じ解を同じと見なす代表形）が先。** 記録が無いうち（初回や、
localStorage が使えないとき）は今までどおりに出す。

`solver.js` の `solve()` は今「1 解見つけたら止める」作りで、探索順も
固定（`PIECES` の並び、空きマスの若い順）。避けるには、見つけた解が既出
なら捨てて探索を続けるか、向きとピースの試す順を乱数で混ぜるかになる。
**上限（`SOLVER_LIMIT`）の中で終わるか**を実測してから決める。

ヒントは 1 手ぶんしか返さない（`hintPlacement`）ので、「その 1 手が既出の
解にしか繋がらない」場合がある。盤上に既に置いてあるピースの並びが
既出の解の一部なら、そもそも避けようがない。そのときは普通にヒントを
出したうえで、何か伝えるかどうかを決める。

進め方: `solver.js`・`game.js`・`tests.html` にまたがり、探索の筋を
立て直すところが本体なので、**サブエージェントは編成しない**。
モデル Opus、effort high。

---

## TODO-017. ヒントは、毎回違うものを出すようにする。

[一手戻す] → [ヒント] を繰り返したときに、毎回違うヒントが出るようにする。

---


## TODO-019. 操作法改良

現在、ピースの回転と反転を別の操作にしているが、
クリック(タップ)で
回転..-->反転-->回転...
というように、一つの操作でも出来るようにしたい。
従来のダブルタップで裏返しは、そのまま残す。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-018** ピース内の正方形の区切りをもっと細く薄くする](archives/todo/TODO-018.%20ピース内の正方形の区切りをもっと細く薄くする)
- [**TODO-009.** 盤のサイズに 6×10 を選べるようにする](archives/todo/TODO-009.%20盤のサイズに%206×10%20を選べるようにする.md)
- [**TODO-014.** 操作の説明が「長押しで裏返す」のまま残っている](archives/todo/TODO-014.%20操作の説明が「長押しで裏返す」のまま残っている.md)
- [**TODO-011.** スマホの縦画面では盤とトレイを縦に並べる](archives/todo/TODO-011.%20スマホの縦画面では盤とトレイを縦に並べる.md)
- [**TODO-010.** 反転を長押しからダブルタップに変える](archives/todo/TODO-010.%20反転を長押しからダブルタップに変える.md)
- [**TODO-007.** ピースを 1 個の塊として見分けられるようにする](archives/todo/TODO-007.%20ピースを%201%20個の塊として見分けられるようにする.md)
- [**TODO-006.** スマホでピースをつかみやすくし、トレイを詰める](archives/todo/TODO-006.%20スマホでピースをつかみやすくし、トレイを詰める.md)
- [**TODO-005.** スマホ横画面ではみ出ないようにする](archives/todo/TODO-005.%20スマホ横画面ではみ出ないようにする.md)
- [**TODO-004.** プレイ中の画面にもバージョン番号を表示する](archives/todo/TODO-004.%20プレイ中の画面にもバージョン番号を表示する.md)
- [**TODO-003.** プレイ中にタイトル画面へ戻れるようにする](archives/todo/TODO-003.%20プレイ中にタイトル画面へ戻れるようにする.md)
- [**TODO-002.** GitHub Pages で公開し、タグのバージョンを画面に出す](archives/todo/TODO-002.%20GitHub%20Pages%20で公開し、タグのバージョンを画面に出す.md)
- [**TODO-001.** ペントミノパズルを遊べる形まで作る](archives/todo/TODO-001.%20ペントミノパズルを遊べる形まで作る.md)
