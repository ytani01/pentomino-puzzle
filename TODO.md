# TODO

**残っている項目: TODO-081。** これまでに 80 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-082` から。**

---

## TODO-081. デモのランダムが、解の無い盤面で延々と手を重ねないようにする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | main（実装）+ tests（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ measure（Sonnet 5 / medium） |

- [ ] `solveStepsRandom()` で、解の無い盤面（`canContinue` が偽）で続けた手を数える。置いた手も、置いてすぐ外した手も 1 手と数える
- [ ] 数が `DEMO.randomDeadLimit`（既定 8）に達したら、`canContinue` が真の盤面まで 1 手ずつ外す（外した手は今と同じく `failed` に控える）
- [ ] JSDoc と、ランダムの動きを書いた文書（`docs/UsersGuide.md` など）を合わせる
- [ ] `tests.html` に「解の無い盤面で続く手が上限を超えない」テストを足し、壊すと落ちることも確かめる
- [ ] 直す前と同じ条件（Node、各盤 20 回）で測り直し、置いた回数と、解の無い盤面で置いた回数が減ったことを確かめる

**背景。** ランダムの探し方は、置いて解が無くなっても、その場では外さない
（人の試行錯誤に見せるため。TODO-059）。気づくのは置ける手が尽きたとき
だけで、広い空きが残る盤ではなかなか尽きない。その間も、すぐ外す 3 つの
場合（TODO-060・066〜068・077）に当たる手を 1 つずつ試しては外し続ける。
直す前の実測（Node、各 20 回の中央値）:

| 盤 | 置いた回数 | 解を無くした手 | 解の無い盤面で置いた回数 | 続いた長さ（最長） |
|---|---|---|---|---|
| 8×8 | 1480 | 52 | 1400 | 127 |
| 6×10 | 704 | 39 | 638 | 89 |

**決めたこと。** 案「解の無い盤面で N 手続いたら戻す」を採る（置いてすぐ
外した手も数える）。N は 8 手で、`src/config.js` の `DEMO` に置く。
「詰まり」の回数（TODO-063）に、この戻りを数えるかどうかは、実装のときに
決めて JSDoc に書く。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
