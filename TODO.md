# TODO

**残っている項目: TODO-082。** これまでに 81 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-083` から。**

---

## TODO-082. デモのランダムで、1 種類のピースしか入らない空きマスがあれば、そのピースを先に置く

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | main（実装）+ reviewer（Opus 5.5 / high）+ tests（Sonnet 5 / medium）+ measure（Haiku 4.5） |

- [ ] `solveStepsRandom()` で、置ける手（`choices`）のうち、覆えるピースが 1 種類しか無い空きマスを探す
- [ ] あれば、そのピースを、そのマスを覆う置き方から今と同じ重み（端・隣・近さ）で抽選して置く。該当するマスが複数あれば、覆える置き方が一番少ないマスを選ぶ
- [ ] 優先する順は、5 マスの穴（`forcedPlacements()`。TODO-066）→ 今回の場合 → 今の抽選（狭い所の重み。TODO-061）
- [ ] 置いたあとの判定（5 の倍数でない空き・覆えない空きマスならすぐ外す。TODO-060・068・077）は今のまま
- [ ] JSDoc・`docs/UsersGuide.md`・`docs/developer.md` を合わせる
- [ ] `tests.html` に「1 種類しか入らないマスがあれば、そのピースでそのマスを覆う手が選ばれる」テストを足し、壊すと落ちることを確かめる
- [ ] TODO-081 の測定スクリプト（`archives/agents/TODO-081/measure-dead.mjs`）で測り直し、置いた回数を前と比べる

**背景。** 今は、閉じた 5 マスの空きに残りのピースが合うときだけ先に置く
（TODO-066）。閉じていない空きでも、あるマスを覆えるピースが 1 種類しか
無いことは人ならすぐ分かる。今は「狭い所」を覆えるピースを重み 10 で
選ばれやすくするだけ（TODO-061）で、別のピースを置いて遠回りすることがある。
利用者の指示（2026-09-25）:「5 マスの穴でなくても、ある空白を埋めるには
トレイの 1 種類のピースしか無いことを判断して、そこに入れてから、空きの
面積が 5 の倍数かどうかを判断する」。

**分担の見込み。** TODO-081 の振り返りに従い、main が実装したら全件を
走らせてから reviewer、指摘まで含めて tests に 1 回で頼む。
measure は既存のスクリプトを走らせるだけなので Haiku。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
