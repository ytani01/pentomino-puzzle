# TODO

**残っている項目: TODO-094〜096。** これまでに 93 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-097` から。**

---

## TODO-094. プレー画面の操作概要・ガラスの下地・確認とホームの動きを直す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | implementer（Opus 5.5 / medium）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] 画面の下に操作方法の概要（タイトルの操作 3 行と同じ内容）を常に出す。入らなければ 1 行に詰める
- [ ] 8×8・6×10 の両方で、盤の空きマスの下地を半透明＋斜めの反射でガラス面に見せる
- [ ] 「やり直し」の前に確認のモーダルを出す（`createButton` / `createPanel` で作る）。盤が空なら確認なしですぐやり直す
- [ ] 盤に何も置いていないときにホームを押したら、つづきからのデータを保存せず（今あるデータはそのまま）すぐタイトルへ戻る
- [ ] バグ: スマホで「やり直し」を押した直後、HUD のボタンの説明（ツールチップ）が出ない。原因を切り分けてから直す

（挙動が変わるので reviewer を入れる。確認のモーダルが入るとやり直しの経路が変わるため、ツールチップのバグは同じ項目で扱う）

---

## TODO-095. 画面の縦横の変化に追従する

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | implementer（Opus 5.5 / medium）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] 画面の縦横が変わったら、今のシーンを状態ごと作り直して新しい向きの配置にする（全画面）
- [ ] プレー中・デモ中は盤の状態を保つ。ドラッグ中のピースは元の位置へ戻す
- [ ] クリア表示・確認のモーダルを出していたら、作り直したあとにも出し直す

背景: 今は起動時に縦横を決め、`SCREEN` / `LAYOUTS` やシーンの配置をモジュールの定数で持っている（`src/config.js`・`src/scenes/title.js` など）。
これを向きごとに引けるようにする必要があり、変更が大きい。
TODO-092〜094 で配置が変わるので、その**あと**に着手する。

---

## TODO-096. 公開した直後のリロードで新しい版が出るようにする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（実装）+ verifier（Sonnet 5 / medium） |

- [ ] `.github/workflows/pages.yml` で、公開するコピーの `index.html` の `src/main.js` と、`src/` の相対 import すべてに `?v=<タグ名>` を付ける（手元のコードは変えない）
- [ ] 付け漏れがあればジョブが失敗するようにする（`VERSION` の置換と同じ考え方）
- [ ] `docs/developer.md` の公開の節に、キャッシュと版の付け方を書く

背景: GitHub Pages は `cache-control: max-age=600` を返す（2026-09-26 に `curl -I` で確認）。
版ごとに URL が変わらないので、公開から 10 分はリロードしても古い JS が使われ、
スマホではキャッシュを消す手段も限られる。動的 import（`src/solutions.js` の全解のデータ）も対象。
確認は、ワークフローの置換と同じ処理を手元で走らせ、出てきたコピーをローカルサーバで開いて
すべてのモジュールに `?v=` が付いて読み込まれることを見る（verifier）。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
