# TODO

**残っている項目: TODO-097。** これまでに 96 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-098` から。**

---

## TODO-097. デモの探し方を「機械的」「人間的」と呼び、アイコンの下に名前を出す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | main（実装）+ screens（Sonnet 5 / low） |

- [ ] ツールチップを `探し方: 機械的` / `探し方: 人間的` にする（`src/scenes/demo.js` の `STRATEGIES`）
- [ ] デモの画面下の説明文を新しい呼び方にする
- [ ] 探し方のボタンで、アイコンの下に「機械的」「人間的」を小さく出す。
      文字の大きさと色は `src/config.js` に置く
- [ ] `docs/UsersGuide.md` の呼び方を直す
- [ ] screens で縦・横の 2 画面を撮り、文字がボタンからはみ出していないか、
      アイコンと重なっていないかを見る

「深さ優先」はアルゴリズムの用語で、一般のプレーヤーに通じにくい。深さ優先は
機械的に、ランダムは人間っぽく動くように作ってあるので（TODO-059・TODO-065）、
その違いを呼び方に出す。字数を揃えて「機械的」「人間的」にする（利用者が決めた）。
アイコンの絵は変えない。下の文字は小さくて読みにくくてもよい（利用者が決めた）。

変えないもの: URL の `?demo=random|depth`、内部の名前（`strategy`、`random*` の設定）、
コメントの中のアルゴリズムの説明。分岐は変わらないので reviewer は入れない。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
