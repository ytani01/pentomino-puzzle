# TODO

**残っている項目: TODO-053。** これまでに 52 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-054` から。**

---

## TODO-053. トレイのピースを詰めて、指を動かす距離を短くする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | main（設計）+ implementer（Opus 5.5 / medium）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low）+ verifier（Sonnet 5 / medium） |

- [ ] 着手前に、縦・横画面 × 8×8・6×10 で、トレイのピースの中心から盤までの距離を測る（比べるため）
- [ ] スロットの大きさをピースの長い辺に合わせる（`I` 5 マス、`L`・`N`・`Y` 4 マス、他 3 マス）
- [ ] スロットを枠いっぱいに等分せず、盤の側へ寄せて詰める。`TRAY_SLOT_PAD` も減らす
- [ ] 並べ方は `config.js` の純粋な計算で出し、`tests.html` で確かめる
- [ ] `game.js` のスロットの位置と当たり判定の 2 か所を、その結果を読むように直す
- [ ] reviewer：並べ方の計算、回したときにはみ出さないか、デモ・記録の画面への影響
- [ ] screens：縦・横画面 × 8×8・6×10 を撮り、はみ出し・重なりが無いか
- [ ] verifier：ドラッグで掴めるか、盤までの距離が縮んだかを実測

利用者が 2026-09-24 に、トレイのピースを詰めて指を動かす距離を短くしたいと望んだ。
間隔が狭くなっても掴みやすさはそれほど落ちない、とのこと。

今はスロットがトレイの枠を列・段で等分した大きさ（`game.js` の 2 か所）で、枠は盤の
残りを全部使うため、画面に余裕があるほどピースが離れる。どの向きでも `I` の 5 マスが
入るよう、全スロットが 5 マス角になっている。回しても長い辺は変わらないので、
スロットをピースごとの長い辺に合わせればはみ出さない（案 B を選んだ）。
マスの上限 `TRAY_CELL_MAX` = 20 は変えない。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
