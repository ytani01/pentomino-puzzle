# TODO

**残っている項目: TODO-099。** これまでに 98 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-100` から。**

---

## TODO-099. 「盤」の呼び名を「ボード」に変える

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | implementer（Sonnet 5 / medium）+ reviewer（Sonnet 5 / high）+ screens（Sonnet 5 / low） |

- [ ] 画面の文言の「盤」を「ボード」にする（選択肢の見出し、操作の説明、メッセージなど）
- [ ] `README.md`・`docs/*.md`・`CLAUDE.md` の「盤」を「ボード」にする（`docs/developer.md` の「画面の用語」を含む）
- [ ] ソースのコメント、`tests.html` のテスト名、`tools/` の「盤」を「ボード」にする
- [ ] reviewer が置き換え漏れと不自然な言い回しを確かめる
- [ ] screens が `tests.html` を走らせ、文言が変わった画面ではみ出しが無いかを確かめる

背景（利用者と決めたこと）:

- 「盤」は野暮ったいので呼び名を変える。候補（フレーム、枠、エリア など）を
  比べ、識別子の `board` と揃う「ボード」にした
- 8×8・6×10 の種類も「ボード」と呼ぶ（選択肢の見出しも「ボード」）
- **「盤面」（ピースを置いた状態）は変えない。** 「ボード面」は不自然なため
- **識別子（`board` / `boardPanel` / `BOARDS` / `boardKey` など）と保存データは変えない**
- パネル（panel）の呼び名はそのまま
- 対象は `rg -n '盤' --glob '!archives/**' --glob '!src/data/**'` で拾う。
  「盤面」を含む行は残し、それ以外を置き換える。`archives/` は過去の記録なので触らない

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
