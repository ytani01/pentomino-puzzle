# TODO

**残っている項目: TODO-087、TODO-088、TODO-089。** これまでに 88 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-092` から。**

---

## TODO-087. タイトル画面の改良（動く盤・盤の選択・色の選択）

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（実装）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] 「PENTOMINO PUZZLE」の下に、README の GIF のような動く盤を出す
- [ ] 盤の選択ボタンを高くし、8×8 と 6×10 の違いが見て分かるようにする
- [ ] 色の選択ボタンを、ピース F・W・X の絵で色の組が分かるようにし、高さも上げる

動く盤は GIF を読まず、Canvas の上でデモの探索を小さく動かす（画像アセットを
足さない規約を保つ。利用者と決めた）。見せる盤は選んでいる盤に合わせ、盤を選び直すと
切り替わる。タップしても何もしない（飾り）。探索は `logic.js` の `solveStepsRandom()`
などを使い回し、タイマーは `scene.time` で回す。盤と色のボタンの図は、
今ある `src/icons.js` の図（TODO-046）を大きくするか描き直す。

---

## TODO-088. タイトル画面で、記録があるのに「記録なし」と出る

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（調査・実装）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium） |

- [ ] 原因を確かめる
- [ ] 最短時間に、ヒント表示を使った回も含める（`shouldRecordBest()`）。おまかせの回は今までどおり含めない
- [ ] 履歴に残っている自力とヒントの回から、最短時間を出し直す（今「記録なし」と出ている盤にも時間が出るように）
- [ ] 最短時間の説明がある文書（UsersGuide など）と `tests.html` を合わせる

`title.js` の `refreshBoard()` は `loadBest()`（最短時間）だけを見て「記録なし」を出す。
最短時間はヒント・おまかせを使わなかった回だけ残す（TODO-020・TODO-024）ので、
ヒントを使ったクリアしか無いとき、履歴はあるのに「記録なし」になる、というのが見立て。
着手時に確かめる。見立てどおりなら上のとおり直す（利用者と決めた。TODO-020 の決めごとを
「おまかせの回だけ除く」に変える）。見立てと違えば、そこで相談する。

---

## TODO-089. タイトル以外の画面の最上段に、タイトル行を出す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（実装）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] 本編・記録・デモの最上段に「PENTOMINO PUZZLE」の行を出す
- [ ] タイトル行をタップするとタイトルへ戻る。本編では HUD の「タイトルへ」と同じ
  `confirmToTitle()` を通す。HUD の「タイトルへ」ボタンは残す（利用者と決めた）
- [ ] デモでは、タイトル行の下に探し方が 2 種類（深さ優先・ランダム）あることを
  目立たない一文で出す

クリア表示は本編の上に重ねるので、本編のタイトル行がそのまま見える想定。
スマホ横画面で盤・トレイが縦に詰まらないかは画面を撮って確かめる。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
