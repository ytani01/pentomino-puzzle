# TODO

**残っている項目: TODO-001。** これまでに 0 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-002` から。**

---

## TODO-001. ペントミノパズルを遊べる形まで作る

- [ ] `index.html` と `src/main.js`（Phaser の起動、シーンの登録）
- [ ] `src/config.js`（盤面・12 種のピース・色・レイアウトの定数）
- [ ] `src/logic.js`（向きの生成、配置判定、盤面の更新。純関数）
- [ ] `src/solver.js`（ヒント用の求解。純関数）
- [ ] `src/audio.js`（効果音の合成）と `src/storage.js`（最短時間の保存）
- [ ] `src/scenes/boot.js`（マス目テクスチャの生成）と `title.js` / `clear.js`
- [ ] `src/scenes/game.js`（本編。ドラッグ配置・回転・反転・Undo・ヒント）
- [ ] `tests.html`（`logic.js` / `solver.js` / `config.js` のテスト）
- [ ] ブラウザで通しで遊んで確かめる

8×8 の中央 2×2 を穴にした 60 マスへ、ペントミノ 12 種をすべて敷き詰める。
解は 65 通りあり、1 画面に収まる大きさなのでこの盤面にした。

**決めたこと**（作り始める前の相談で確定した）:

| 項目 | 決定 |
|---|---|
| 土台 | ビルド工程なし。Phaser 3.90.0 を CDN から読み込み、自前コードは ES Modules |
| 盤面 | 8×8 の中央 2×2 を穴 |
| 操作 | ドラッグで配置。掴んでいないときのタップで右 90° 回転、長押しで反転 |
| 補助機能 | Undo / 経過時間とクリア記録 / 置けない場所の警告 / ヒント（1 ピース置く） |
| 効果音 | 入れる（Web Audio で合成。アセットファイルは持たない） |

### 設計

**`src/logic.js`**（Phaser にも DOM にも依存しない純関数）

- ピースは名前（F I L N P T U V W X Y Z）とセル座標の配列で `config.js` に持つ
- `normalize(cells)` — 左上を原点へ寄せ、座標を並べ替える。向きの同一判定に使う
- `rotateCw(cells)` / `flip(cells)`
- `orientations(cells)` — 回転 4 通り × 反転 2 通りを作り、正規化してから重複を除く。
  X は 1 通り、I・T・U・V・W・Z は 4 通り、F・L・N・P・Y は 8 通りになる
  （この通り数を `tests.html` で検査する）
- `boardCells(spec)` — 8×8 から中央 2×2 を抜いた 60 マスを作る
- `canPlace(board, cells, row, col)` — 盤外か、重なりか、置けるかを返す。
  **置けない理由を返す**（赤い警告の出し分けに使う）
- `place` / `remove` — 盤面を書き換えず、作り直して返す（Undo の履歴が壊れないように）
- `isSolved(board)` — 空きマスが無いか
- `formatTime(ms)` — 表示用

**`src/solver.js`**（ヒント用の求解。同じく純関数）

- 未使用のピースと現在の空きマスから、深さ優先で 1 解を探す。Dancing Links は
  持ち込まず、次の 2 つの枝刈りで足りる見込み
  - 空きマスのうち**一番若い位置から必ず埋める**（同じ配置を何度も試さない）
  - 空き領域を連結成分に分け、**5 で割り切れないものがあれば切る**
- ヒントは「解ける置き方を 1 つ求め、その最初の 1 ピースを置く」。すでに詰んでいる
  ときは「この形からは完成できない」と伝える（どこを外すべきかまでは示さない）
- 探索が長引いたときのために**試行回数の上限**を設け、超えたら「見つからなかった」
  として打ち切る（画面が固まらないようにする）
- `tests.html` で「空の盤面から解が 1 つ求まる」「その解が盤面をちょうど埋める」
  「詰みの盤面では解なしを返す」を検査する

**画面（`src/scenes/game.js`）**

- 内部解像度は固定（`Scale.FIT` で拡大縮小）。**盤を左、未使用ピースのトレイを右**に置く
- ドラッグ中は掴んだマスを指の位置に合わせ、置ける場所では薄く影を出す。
  離したときに置けなければ赤く光らせて元の場所へ戻す
- 掴んでいない状態でピースをタップ＝右 90° 回転、長押し＝反転。
  盤上のピースをドラッグして外す／動かすこともできる
- HUD に経過時間、残りピース数、Undo・ヒント・やり直し・ミュートのボタン
- Undo は「置く／外す／動かす」の履歴を配列で持ち、1 手ずつ戻す
  （ヒントで置いた手も 1 手として戻せる）
- 全マスが埋まったら `Clear` シーンへ。`storage.js` の最短時間を更新する

**確認のしかた**: `python3 -m http.server 8765` で `tests.html` が全部通ること。
本体をブラウザで開き、コンソールにエラーが出ないこと、ドラッグ配置・回転・反転・
Undo・ヒントがそれぞれ動くこと、ヒントを押し続けるだけで完成まで行けること。

**まだ決めていないこと**:

- ヒントの求解の打ち切り上限をどのくらいにするか。実測してから決める
  （画面が固まらないことが条件）

**モデルと effort**: Opus / effort high。ファイル数は多いが 1 つ 1 つは小さく、
盤面の座標系と `config.js` の定数を全ファイルが共有するので、分担せず通しで書く。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

（まだ無い）
