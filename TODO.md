# TODO

**残っている項目: TODO-002。** これまでに 1 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-003` から。**

---

## TODO-002. GitHub Pages で公開し、タグのバージョンを画面に出す

- [ ] `src/config.js` に `VERSION` を足す（既定は `'dev'`）
- [ ] タイトル画面の隅に `VERSION` を表示する
- [ ] `.github/workflows/pages.yml` を置き、タグを押したときに公開する
- [ ] 公開の手順（タグの打ち方、Pages の設定）を `CLAUDE.md` に書く

いま公開の仕組みが無い。GitHub Pages で公開することにしたので、
その工程でタグ名をページへ埋め込む。

ビルド工程を持たないので、タグの値をソースへ自動で入れる方法が他に無い
（手で書き換えると、書き換え忘れが起きる）。実行時に GitHub API を叩く案は、
ネットワーク必須・レート制限ありで、ローカルで開いたときに意味を成さないので採らない。

決めること・気にすること:

- **置換の壊れやすさ。** `sed` の文字列一致で `src/config.js` を書き換えるため、
  書き方を変えると静かに `dev` のまま公開される。壊れたと分かる形にできないか
- ローカルで `python3 -m http.server` で開くと `dev` 表示になる（本番と見た目が違う）。
  これは許容してよいか
- Pages 側の設定（Settings → Pages → Source: GitHub Actions）は手で行う。
  リポジトリを作り直したときに忘れやすいので、手順を残す
- 表示の位置と大きさ。タイトル画面の右下、小さく薄く、を想定

規模は小さい（ファイル 4 つ、判断が要るのは置換のやり方だけ）。
サブエージェントは編成しない。**Sonnet / effort: medium** で進める。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-001.** ペントミノパズルを遊べる形まで作る](archives/todo/TODO-001.%20ペントミノパズルを遊べる形まで作る.md)
