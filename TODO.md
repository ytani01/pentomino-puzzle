# TODO

**残っている項目: TODO-046。** これまでに 45 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-047` から。**

---

## TODO-046. タイトルの盤・色の選択肢を、文字から図にする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（実装）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] `createChoiceRow`（`src/ui.js`）が選択肢ごとに絵を描けるようにする
- [ ] 盤: ボタンの中へ盤の形を小さなマス目で描く（8×8 は中央 2×2 を穴、6×10 は横長）
- [ ] 色: 盤のマス目テクスチャを縮めて 2×1 の小片を 3 つ並べる（12 色・蛍光は色違い、ガラスは単色）
- [ ] 文字の代わりに、ツールチップで盤の名前と但し書き・色の組の名前を出す
- [ ] reviewer にレビューさせる
- [ ] screens に横・縦画面で撮らせ、はみ出し・選択中の見え方・ツールチップを確かめる

利用者の依頼（2026-09-24）は「タイトルの文字をアイコンなどにしたい。細かいことは任せる」。
範囲は盤と色の 2 行だけ。`はじめる`・`つづきから`・`記録`・`デモ` と、
行頭の見出し「盤」「色」は文字のまま残す（利用者が選んだ）。

- 画像アセットは足さない。盤は Graphics API で描き、色は `boot.js` が焼いた
  マス目テクスチャ（`cell-<組>-<ピース>-<大きさ>`）を縮めて使う
- reviewer の定義は Sonnet だが、挙動の変わる項目のレビューなので Opus 5.5 に上書きする

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
