# TODO

**残っている項目: TODO-044、TODO-045。** これまでに 43 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-046` から。**

---

## TODO-044. ヒント表示が入のとき、残りのピースと同じ形の空きを自動で埋める

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | implementer（Opus 5.5 / medium）+ tests（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] `logic.js` に、周りから切り離された 5 マスの空きのうち、残りのピースの
      どれかの向きと同じ形のものを探して、置き方（ピース・向き・位置）を返す関数を足す
- [ ] 本編で、ヒント表示が入・解ける・上の関数が置き方を返す、のときに
      そのピースを盤へ置く。おまかせと同じくトレイから滑らせて音を鳴らす
- [ ] 自動で埋めた手は、直前の手（利用者が置いた手、またはヒント表示を入にした時点）と
      まとめて「一手戻す」1 回で戻る
- [ ] tests に上の関数のテストを足させる（壊すと落ちるかも確かめる）
- [ ] 文書（`README.md`・`docs/developer.md`）のヒント表示の説明を合わせる

背景（立てるときに決めたこと）:

- 一手戻すの単位は直前の手とまとめる。自動の手だけ戻すと、ヒント表示が入のままなら
  戻した直後にまた埋まり、戻せなくなるため
- 本編だけ。デモ（TODO-043）には入れない
- 見せ方はおまかせと同じ動き（滑らせて音を鳴らす）
- 解ける盤面で、切り離された 5 マスの空きが残りのピースと同じ形なら、解はその空きを
  そのピースで埋めるしかない（12 種の形はどれも違う）。だから埋めたあとも解ける。
  埋めても他の空きの形は変わらないので、該当する空きが複数あれば一度に埋めればよい
- ヒント表示を入にした時点で該当する空きがあれば、そこで埋める。最後の 1 個を
  埋めたらそのままクリアになる
- 記録の印は今のまま（ヒント表示を入にした時点で「ヒント」が付くので、新しい印は足さない）

分担: 空きの形を探す関数と、本編の置き方・一手戻すとのつなぎは implementer
（Opus 5.5 に上書き）。関数のテストは tests に分ける。盤の変化の流れが変わるので
reviewer（Opus 5.5 に上書き）を入れる。滑らせる見た目は screens。
着手は TODO-043 の決着のあと（`game.js` の継承をデモが使っているため）。

---

## TODO-045. ヒント表示の「解ける／解なし」を色付きの札にして目立たせる

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | implementer（Sonnet 5 / medium）+ screens（Sonnet 5 / low） |

- [ ] 「解ける」を緑地、「解なし」を赤地の角丸の札に白文字で出す。文字は今（`FONT.small`）より大きくする
- [ ] 札を描く処理は `src/ui.js` に 1 つだけ置き、本編（`game.js`）とデモ（`demo.js`）の両方から使う
- [ ] 緑の色と札の寸法は `config.js` に置く
- [ ] 4 通りの配置（8×8/6×10 × 横/縦）で、HUD の 1 段目の他の文字と重ならず、枠からはみ出さない
- [ ] 文書（`docs/developer.md` の画面の用語など）に見た目の説明があれば合わせる

背景（立てるときに決めたこと）:

- 今は HUD の 1 段目に小さい文字（解ける＝暗い色、解なし＝赤）で出していて目立たない
- 見せ方は HUD の中の札だけにする。盤の枠の色は変えない
- 本編とデモの両方を同じ見た目にする（デモは TODO-043 で同じ文字を出している）
- 文言（解ける／解なし）と音の鳴らし方は変えない

分担: 描き方を変えるだけで分岐は変わらないので reviewer は入れない。実装は
implementer（`ui.js`・`config.js`・`game.js`・`demo.js` にまたがるため）。見た目の確認は screens。
着手は TODO-043 の決着のあと（デモの HUD に同じ文字を足したばかりのため）。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
