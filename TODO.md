# TODO

**残っている項目: TODO-098。** これまでに 97 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-099` から。**

---

## TODO-098. HUD のボタンを高くし、探し方の名前を大きくする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | main（実装）+ screens（Sonnet 5 / low） |

- [ ] `src/config.js` の `HUD_BUTTON_HEIGHT` を 56 → 64 にする（本編とデモの両方）
- [ ] `ICON.captionSize` を 12 → 16 にする。`captionShift` はアイコンと名前が
      離れるように合わせる
- [ ] コメントに書いた数値（`ICON` の「ボタンの高さ（56）」、`HINT_BADGE` の
      「`HUD_ROW` = 68」）を直す
- [ ] screens で本編とデモを縦・横の 2 画面で撮り、名前がボタンからはみ出して
      いないか、アイコンと重ならないか、HUD が盤・トレイと重ならないかを見る

TODO-097 で探し方のボタンに名前を出したが、小さすぎた。ボタンを高くして名前を
大きくする。範囲は本編とデモの両方（`HUD_BUTTON_HEIGHT` は共有で、本編とデモの
盤・トレイの位置を揃えているため）、高さ 64・文字 16 と利用者が決めた。
HUD が 8 高くなるぶん、盤とトレイは少し小さくなる。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
