# TODO

**残っている項目: TODO-052。** これまでに 51 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-053` から。**

---

## TODO-052. デモで解を見つけたら 10 秒待って次の解を自動で探す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | main（実装・文書）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium） |

- [ ] `src/config.js` の `DEMO` に待ち時間（10000 ms）を足す
- [ ] `src/scenes/demo.js` の `update()` で、`solved` の間も経過時間を数え、
      待ち時間を過ぎたら `searchNext()` と同じく探索を再開する
- [ ] `done`（出し切った）の間も同じく数え、待ち時間を過ぎたら `startSearch()` で空の盤から探し直す
- [ ] README の「デモ」と `docs/developer.md` の「次の解を探す」の説明を直す
- [ ] reviewer：状態の移り方（`loading`・`running`・`solved`・`done`）と、
      速さ・探し方の切り替え、タイトルへ戻る操作との組み合わせを見る
- [ ] verifier：Playwright で、解を見つけてから約 10 秒後に探索が再開すること、
      待っている間に「次の解を探す」を押すとすぐ再開すること、
      `done` から 10 秒後に空の盤から探し直すことを実測する

利用者が 2026-09-24 に「ホームボタン（タイトルへ）を押すまで止まらない」形を望んだ。
決めたこと:

- 「次の解を探す」ボタンは残し、押せば待たずに次へ進む
- すべて探し終えたら、10 秒後に空の盤から探し直す（見つけた解の数も 0 に戻る）
- 残り秒数は画面に出さない
- 待ち時間の数え方は、今の `update()` の `waited` に合わせる（`delayedCall` は使わない）

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
