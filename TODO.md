# TODO

**残っている項目: TODO-086。** これまでに 85 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-087` から。**

---

## TODO-086. 色の組の既定をネオンにし、README の GIF もネオンで撮り直す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | main（実装）+ reviewer（Opus 5.5 / high）+ screens（Sonnet 5 / low） |

- [ ] `src/config.js` の `DEFAULT_PALETTE_KEY` を `'neon'` にし、「ガラスを既定にする」と書いた JSDoc を直す
- [ ] `docs/UsersGuide.md` に既定がネオンであることを書く
- [ ] `tools/capture.mjs` の GIF をネオンで撮り直す（他のキャプチャは色を指定して撮っているので変えない）
- [ ] 画面で確かめる: 色を保存していない状態でタイトル・本編・デモがネオン、保存した色があればそちらが優先。GIF のフレームを抜いて見る

**背景。** 利用者の依頼（2026-09-26）。デモの既定の色をネオンにしたい。場面を聞いたところ、
デモだけでなく本編の既定もネオンにすると決めた。既に色を選んで保存している人は、
その色のまま（`loadPalette()` は保存した値を先に見る）。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
