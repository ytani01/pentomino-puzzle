# TODO

**残っている項目: TODO-064・071〜073。** これまでに 69 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-074` から。**

着手する順番の案（TODO-064・071〜073）: 071 → 072 → 073（064 は保留。利用者が 2026-09-25 に決めた）

- 064 を再開するときは、TODO-065 の回している時間（`DEMO.randomTurnStepMs`）が待ちに足されていることを踏まえる

---

## TODO-064. デモのランダムで、考える時間を盤面に合わせて変える

**保留**（利用者が 2026-09-25 に決めた）。

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | implementer（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium） |

- [ ] `demo.js` の `pickWaitScale()`: 置ける手が少ない盤面や、行き詰まって戻った直後は長めに待つ
- [ ] 倍率は `config.js` に置く（値は仮。画面で見て利用者が決める）
- [ ] `docs/UsersGuide.md`・`docs/developer.md` のランダムの説明を直す

今の揺らぎは ±50% と、外したあと 2 倍だけで、盤面の難しさは関係しない。
置ける手の数は generator の中にしか無いので、`place` に載せて運ぶ
（`logic.js` も少し触る）。探索の順番は変えない。
確認は TODO-059 の振り返りのとおり、画面の時間ではなく `pickWaitScale()` の
値を `evaluate` で集めて見る（headless ではフレームが重く測れない）。

---

## TODO-071. 記録画面で、チェックした回をまとめて消せるようにする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | implementer（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium）+ screens（Sonnet 5 / low） |

- [ ] 「タイトルへ」をホームのアイコンにする（本編・デモの HUD と同じ `ICONS.title`）
- [ ] 消すボタンをゴミ箱のアイコンにする（`src/icons.js` に足す）
- [ ] 一覧の各行の左端にいつもチェックボックスを出す。行を押すと今までどおり完成形を出し、チェックを押すと選ぶ
- [ ] 一覧の上に「全部選ぶ」のチェックを置き、「全部消す」と「この回を消す」はゴミ箱 1 つにまとめる
- [ ] ゴミ箱はチェックが 1 件以上のときだけ押せる。押したら今までどおり画面内で確認してから消す（何件消すかを出す）
- [ ] ボタンの配置と一覧の見せ方（前へ・次へ、1 ページの行数など）は、2 案ほどをスマホ横で撮って利用者に選んでもらう
- [ ] `tests.html`: まとめて消す計算を `src/storage.js` などに置くならテストを足す
- [ ] `docs/UsersGuide.md` の記録の説明と、`docs/images/records.png`（`tools/capture.mjs` で撮り直す）

今は「この回を消す」（選んでいる 1 件）と「全部消す」の 2 つで、何件かだけ消すことができない（利用者の指摘）。
操作の決めごとは利用者が決めた（チェックは常に出す、全部消すは「全部選ぶ」に置き換える、何も選んでいないときは押せない）。
確認で決めること（着手時、案を撮ってから）: ボタンの配置と一覧の見せ方。
盤を切り替えたとき・ページをまたいだときにチェックを残すかは、着手時に案と一緒に聞く。

---

## TODO-072. 本編で、完成したあとも続けて別の解を作れるようにする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | implementer（Sonnet 5 / medium）+ tests（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium）+ screens（Sonnet 5 / low） |

- [ ] 完成したら時計を止め、本編の上にクリア表示を重ねて出す（今は別の画面 `ClearScene` へ移る）。「続ける」を押すと時計を動かして続けて遊べる
- [ ] 完成した解が新しいか、もう記録にあるかを HUD に出す（何番の解かも）
- [ ] 完成した解（最初の完成も、続けて作った解も）が記録に無ければ、その時点の経過時間で履歴を 1 件足し、見つけた解にも加える
- [ ] 記録にある解なら履歴は足さない。成績がよければその解の履歴を上書きし（日時・経過時間・印）、よくなければ HUD に「記録済み」と出すだけ
- [ ] 成績の比べ方: ヒント・おまかせの印が無い方を優先し、印が同じかよいときだけ経過時間の短い方をよいとする
- [ ] 今の履歴に同じ解が複数あるときは、成績が一番よい 1 件にまとめる（履歴は 1 つの解に 1 件になる）
- [ ] 完成したあとも、遊びかけの盤面を今までどおり保存する。そのプレーで完成させた解の一覧も一緒に保存する
- [ ] `tests.html`: 履歴の足し方・書き換え方を `src/storage.js` などの計算に置くならテストを足す
- [ ] `docs/UsersGuide.md`（クリア・記録・つづきから）・`docs/developer.md`（シーンの移り方・保存）

完成したあと、一部のピースを入れ替えると別の解を作れることがある（利用者の指摘）。
操作と記録の決めごとは利用者が決めた（上のとおり。立てたあとで「記録にある解は、成績がよければ上書き」に改めた）。
着手時に調べること: 今の `ClearScene` の役目（記録の更新はここでだけ行う）を本編の上の表示へどう移すか。
最短時間（`loadBest()`・`shouldRecordBest()`）と、1 つの解に 1 件になった履歴との関係。記録画面の達成度・履歴の件数の出し方が変わらないか。
経過時間は最初からの累計のまま比べる。

---

## TODO-073. 記録画面から、選んだ回の完成形の続きを遊べるようにする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort medium | implementer（Sonnet 5 / medium）+ reviewer（Opus 5.5 / high）+ verifier（Sonnet 5 / medium）+ screens（Sonnet 5 / low） |

- [ ] 記録画面に「続ける」のボタンを置き、選んでいる回の完成形を盤に並べた状態で本編を始める
- [ ] 時計はその回の経過時間から、ヒント・おまかせの印はその回のものを引き継ぐ
- [ ] 遊びかけの盤面があるときは、画面内で「遊びかけの盤面が消える」と確かめてから置き換える。無ければ確かめない
- [ ] 続きで作った解の記録は TODO-072 の決めごとに従う（記録に無ければ足す、あれば成績がよければ上書き）
- [ ] `docs/UsersGuide.md`（記録・つづきから）・`docs/developer.md`（シーンの移り方）

TODO-072 で完成したあとも続けて遊べるようにするのに合わせ、前に完成させた回からも続けられるようにする（利用者の依頼）。
時計・印・遊びかけの扱いは利用者が決めた（上のとおり）。時計を 0 から始めないのは、数手入れ替えるだけで短い時間の記録が作れてしまうため。
TODO-072（記録の上書き・完成後に続ける）と TODO-071（記録画面のボタンの配置）のあとにする。
ボタンの置き場所は TODO-071 で決める配置に合わせる。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
