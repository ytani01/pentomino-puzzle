# TODO

**残っている項目: TODO-006。** これまでに 5 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-007` から。**

---

## TODO-006. スマホでピースをつかみやすくし、トレイを詰める

- [x] 決めること（下の「決めたこと」）
- [ ] トレイのスロット全体を当たり判定にする
- [ ] トレイを詰めて小さくし、空いた幅を盤に回す
- [ ] ドラッグ中のピースを指より上にずらす
- [ ] スマホ横画面（568x320 ほか）で撮って確かめる

（モデル: Opus / effort: medium。`config.js` のレイアウト値と `game.js` の
当たり判定にまたがり、詰め具合は画面を撮って目で決めることになるため）

### きっかけ

スマホで遊ぶとトレイのピースをつかみにくい。内部解像度 960x640 を
`Scale.FIT` で縮めるので、568x320 では倍率が 0.5 になる。
`tray.cell = 22`（`config.js`）は画面上で **11 CSS px** にしかならず、
指のタップ目標の目安 44px を大きく下回る。

当たり判定はピースの形そのままで、`game.js:115-121` が 5 枚のタイル
1 枚ずつに `setInteractive()` している。`L` や `U` の凹んだところは
反応しない。一方トレイの 1 スロットは 142x125 内部px（**71x62 CSS px**）
あり、大半が遊んでいる。

### やること

**スロット全体を当たり判定にする。** 透明な矩形をスロットに 1 枚置いて
`pointerdown` を受ける。11px 幅の細い形を狙う必要がなくなる。
盤に置いたピースは今までどおりタイル単位でよい（盤のマスは
56 内部px = 28 CSS px あり、事情が違う）。

**トレイを詰めて小さくする。** 空いた幅を盤に回せば、盤のマスも大きくなる。

**ドラッグ中のピースを指より上にずらす。** 今は掴んだ点が指の真下に来るので
（`game.js:350-351` の `offsetX` / `offsetY`）、ピースも、その下の盤も指で隠れる。
`startDrag()` の `offsetY` にずらし幅を足せば上へ逃げる。

### 決めたこと

**スロットの下限 — `tray.cell` を 22 から 20 に下げる。**
トレイの中でも回転できるので、`I` を横向きにすると `5 × tray.cell` の幅が
要る。22 なら 110px で、今のスロット 142x125 はこれに合わせた寸法。
20 に下げれば下限が 100px になり、スロット幅を 110 前後まで縮められる。

**空いた幅は盤に回す。** 盤は高さが頭打ちなので（`board.y = 84`、
メッセージが `y = 612`）、マスは 64 が上限。そこまで上げる。

| | 今 | 変更後 |
|---|---|---|
| `board` | `x: 24, y: 84, cell: 56` | `cell: 64` |
| `boardPanel` | `468x468` | `532x532`（下端 74+532=606 < 612） |
| `tray` | `x: 500, width: 426, cell: 22` | `x: 560, width: 366, cell: 20` |

盤のマスは 28 → 32 CSS px（568x320）になる。トレイのピースの絵は
小さくなるが、スロット全体が当たり判定になるのでつかみにくくはならない。

**3 列 4 行のままにする。** 12 種がちょうど収まる。

**ずれたときの判定は `container`（見えているピースの位置）に揃える。**
`dropSpot()` は今「盤の上か」を `pointer` で見て、置く升目を `container` で
求めている（`game.js:376-386`）。ずらしたままだと盤の下端で「ピースは盤の
上に見えているのに指は盤の外」が起きるので、「盤の上か」も `container`
基準に変える。

**マウスではずらさない。** PC では指で隠れない。`pointer.wasTouch` で
タッチのときだけずらす。

**ずらし幅は盤のマス 1 個ぶん**（変更後の 64 内部px = 568x320 で 32 CSS px）。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-005.** スマホ横画面ではみ出ないようにする](archives/todo/TODO-005.%20スマホ横画面ではみ出ないようにする.md)
- [**TODO-004.** プレイ中の画面にもバージョン番号を表示する](archives/todo/TODO-004.%20プレイ中の画面にもバージョン番号を表示する.md)
- [**TODO-003.** プレイ中にタイトル画面へ戻れるようにする](archives/todo/TODO-003.%20プレイ中にタイトル画面へ戻れるようにする.md)
- [**TODO-002.** GitHub Pages で公開し、タグのバージョンを画面に出す](archives/todo/TODO-002.%20GitHub%20Pages%20で公開し、タグのバージョンを画面に出す.md)
- [**TODO-001.** ペントミノパズルを遊べる形まで作る](archives/todo/TODO-001.%20ペントミノパズルを遊べる形まで作る.md)
