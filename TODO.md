# TODO

**残っている項目: TODO-040。** これまでに 39 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。
**新規項目の番号は `TODO-041` から。**

---

## TODO-040. コンピューターが試行錯誤して解を見つけるデモを足す

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort 記載なし | measure + implementer + tests + reviewer + screens + docs |

- [ ] 着手前に、ランダムな順で最初の解までに何手かかるかを 8×8・6×10 で実測する（速さの段階を決める材料）
- [ ] `src/logic.js` に 1 手ずつ進める探索を足す（置く・外す・見つけたを 1 件ずつ返す。乱数は外から渡し、テストでは固定する。枝刈りは `tools/enumerate.mjs` と同じ）
- [ ] 1 フレームに進める手数を速さに応じて変え、画面を止めない
- [ ] 盤とピースの描画を本編（`game.js`）から使い回せるかを着手時に見て、デモ用のシーンを別に作るかを決める
- [ ] タイトルにボタンを足す（横画面の縦が足りないので、`はじめる`・`つづきから` と同じ行に並べる案から当たる）
- [ ] `tests.html` に探索のテストを足す
- [ ] README と `docs/developer.md` を直す
- [ ] reviewer（先）、screens（後）で確かめる

背景（利用者と決めたこと、2026-09-23）:

- 入口はタイトルの「デモ」ボタン。選んでいる盤と色の組で、空の盤から解く
- 速さは「ゆっくり／速い／最速」を切り替えられる
- 解を見つけたら止まり、完成形と試した手数を見せ、「次の解を探す」「タイトルへ」を選ばせる
- 試す順は毎回ランダム
- 記録・遊びかけ・見つけた解には何も残さない
- 実行時の求解は TODO-022 で `src/` から外した（6×10 で画面が 2.8 秒止まったため）。
  デモでは探索を 1 手ずつに区切り、フレームごとに少しずつ進めて止めない

完了条件: タイトルからデモを始められ、選んだ速さで試行錯誤しながら解に至る。
最速でも画面が止まらない。次の解を探せる。タイトルへ戻ると探索が止まる。記録に何も増えない。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
