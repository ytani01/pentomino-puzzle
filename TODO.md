# TODO

**残っている項目: TODO-016、TODO-022。**
これまでに 20 件を決着させた。新しく足すときは「完了済み」の上に節を作る。
**番号は `TODO-023` から。**

---

## TODO-016. ヒントは、既にクリアした解と別の解へ導く

- [ ] 既にクリアした解を `solver.js` へ渡す形を決める（008 の記録から作る）
- [ ] `solve()` に「この解は避ける」を足す（探索順を混ぜる／見つけた解を捨てる）
- [ ] 避けきれないとき（そこまでの配置が既出の解にしか繋がらない）の扱いを決める
- [ ] ヒントを何度も押したときに毎回同じ手にならないかを確かめる
- [ ] `tests.html` に、既出の解を渡すと別の解を返すテストを足す

同じ盤を何度も解くと、ヒントが毎回同じ解へ導く。既にクリアした解は
避けて、まだ出していない解へ導きたい。

前提の **008（クリア記録に完成した盤面を残す）は済んでいる**。1 件の `cells`
が `logic.js` の `boardKey()` の出力なので、そのまま盤面へ戻せる。
記録が無いうち（初回や、
localStorage が使えないとき）は今までどおりに出す。回転・反転で同じ解を
同じと見なす代表形は 012 で済んでいる（`logic.js` の `canonicalBoard()`）。

020 の副作用に注意する。**ヒントを使ったクリアは記録に残らない**ので、
「既出」になるのは自力で解いた解だけになる。ヒントに導かれて解いた解は
記録されず、次にヒントを押したときの候補に残り続ける。ヒントを頼りに
遊び続けるかぎり同じ解へ導かれかねないので、着手時に、この項目のための
別の持ち方（記録とは分けて「ヒントで出した解」を覚える）が要るかを決める。

`solver.js` の `solve()` は今「1 解見つけたら止める」作りで、探索順も
固定（`PIECES` の並び、空きマスの若い順）。避けるには、見つけた解が既出
なら捨てて探索を続けるか、向きとピースの試す順を乱数で混ぜるかになる。

**後者は 017 で入れた。** `solve()` の `shuffle` オプションで、ピースと向きの
試す順を混ぜられる。上限の中で終わることも 017 で実測済み（最悪 106 万回、
上限は 800 万）。この項目で足すのは「既出かどうかの判定」のほうになる。

ヒントは 1 手ぶんしか返さない（`hintPlacement`）ので、「その 1 手が既出の
解にしか繋がらない」場合がある。盤上に既に置いてあるピースの並びが
既出の解の一部なら、そもそも避けようがない。そのときは普通にヒントを
出したうえで、何か伝えるかどうかを決める。

進め方: `solver.js`・`game.js`・`tests.html` にまたがり、探索の筋を
立て直すところが本体なので、**サブエージェントは編成しない**。
モデル Opus、effort high。

---

## TODO-022. 解を全てデータとして持つ

- [ ] 全解を列挙するスクリプトを置く（開発時に手で走らせる。依存は増やさない）
- [ ] 生成したデータを `src/` の下へ置く
- [ ] 読み込みの入り口を作る（盤を選んだときだけ動的 import する）
- [ ] 代表形から回転・反転を展開して、盤面と照合できる形にする
- [ ] ヒントと「解の有無」を、探索からデータの照合に置き換える
- [ ] 要らなくなった上限と「？？？」を消す
- [ ] `solver.js` と、そのテストを生成スクリプト側へ移す
- [ ] `pages.yml` に、生成し直してデータと突き合わせる検査を足す
- [ ] `tests.html` に、件数と「どれも本当に解になっているか」のテストを足す
- [ ] 履歴を代表形の番号で持つ（保存済みの記録は番号へ読み替える）
- [ ] クリア画面に「正解の ○○ 番」を出す
- [ ] 達成度（全 2339 解のうち何解を見つけたか）をこの項目でやるか決める

解を全て持てれば、いろいろな場面で使える。

- ヒントの元データにできる（探索を回さず、条件に合う解を選ぶだけで済む）
- 保存履歴は解の番号だけ持てばよくなる
- 「全 2339 解のうち何解を見つけたか」を出せる

### 調べた結果（2026-08-15 実測）

現実的だと分かった。**採用する。**

| 盤 | 全解 | 代表形（回転・反転をまとめた数） | 列挙にかかる時間 |
|---|---|---|---|
| 8×8（中央 2×2 穴） | 520 | 65 | 4.5 秒 |
| 6×10 | 9356 | 2339 | 144 秒 |

代表形の数は、文献にある 65 / 2339 と一致した。列挙には `solver.js` と同じ
枝刈り（空きマスの若い順、空き領域が 5 の倍数か）をそのまま使った。

**実行時に列挙するのは無理。** 6×10 は Node で 144 秒かかる。ブラウザは
さらに遅く、初回の起動が数分止まる。事前に生成してコミットするしかない。
Node で走らせるスクリプトを 1 つ置くだけなので、ライブラリは増えない。

**逆に、作るのは 1 回だけなので、生成にかかる時間は問題にしない。**
144 秒という数字が意味を持つのは「実行時にはできない」の裏付けとしてだけで、
**生成を速くする工夫は要らない**（Dancing Links を持ち込む、探索順を練る、
といったことをしない）。`solver.js` の枝刈りをそのまま流用した素朴な作りで
足りる。上の実測もそれで測った。

なお、この 144 秒は上限でもある。ペントミノが敷き詰められる長方形は
6×10（2339）・5×12（1010）・4×15（368）・3×20（2）で、**解が一番多い
6×10 を既に測っている**。盤を足しても生成時間がこれより大きく延びることはない。

**圧縮の工夫は要らない。** 代表形を 60 文字の文字列で持つと 8×8 が 4.1 KB、
6×10 が 139.3 KB。1 マス 4bit に詰めれば 68.5 KB まで半分になるが、
**gzip をかけると 38.9 KB 対 38.0 KB でほとんど差が消える**。
GitHub Pages が実際に gzip で返すことは公開中の `src/config.js` で確かめた
（25 KB が 9.8 KB で届く。Brotli は要求しても返らなかった）。
読みにくい形式にしてまで縮める理由が無いので、`boardKey()` の出力と
同じ 60 文字の文字列をそのまま並べて持つ。

**照合は速い。** 6 ピース置いた盤面と矛盾しない解を代表形 2339 件から
線形に探して 0.02 ms。索引を作らずに毎回なめて構わない。

### 遊んでいる間の速さ（ここが一番大きい）

1 ピースだけ置いた盤面を全通り試して測った最悪の時間（Node。ブラウザは
これより遅い）。**求解は同期で回すので、この時間は画面が止まる時間**。

| | 今（探索） | 全解データ |
|---|---|---|
| ヒント（6×10 の詰み盤面） | **2795 ms** | 0.068 ms |
| ヒント（8×8 の詰み盤面） | 329 ms | 0.007 ms |
| 解の有無モード（置くたび） | 273〜280 ms | 0.068 ms |
| 起動時 | 0 | 20 ms（6×10 の展開） |
| メモリ | 0 | 1.07 MB（展開後 9356 件） |

`config.js` に「8×8 の最悪は 0.5 秒」と書いてあるが、6×10 では 2.8 秒だった。

**打ち切りの曖昧さも消える。** 今は上限に達すると「解なし」と言い切れず
「？？？」と出しており、6×10 の 1 手目だけで 93 通りが当てはまる。
データがあれば必ず言い切れる。

**コードは減る。** 要らなくなるもの: `SOLVER_LIMIT`、`SOLVE_CHECK_LIMIT`、
`HINT_RETRIES`、`solve()` の `'limit'`、`checkState` の `unknown`（「？？？」）、
`shuffle` オプション、`config.js` にある上限の根拠を書いた長いコメント。

### 拡張性で失うもの（**受け入れると決めた**）

次の 4 つは、検討したうえで**問題にしない**と判断した。

1. **盤を足すたびにデータの生成が要る**（今は `BOARDS` に足せば `solver.js`
   がそのまま動く）
2. **動的な盤が作れない**（穴の位置を毎回変える、遊ぶ人が盤を描く、など）
3. **ピースを増やす方向が閉じる**（ヘキソミノなどは解の数が桁違いで持てない）
4. データとコードがずれると、間違ったヒントを静かに出す

4 だけは仕掛けで防ぐ。**公開のときに検査する**——
`.github/workflows/pages.yml` で生成スクリプトを走らせ、コミットしてある
データと突き合わせて、違っていればジョブを失敗させる（`VERSION` の
プレースホルダを検査しているのと同じやり方）。6×10 の生成に 144 秒かかるが、
タグを押したときだけなので構わない。`tests.html` 側では、生成し直さずに
済む軽いほう——件数（65 / 2339）と、各行が本当に解になっているか
（12 種が 1 個ずつ、形が正しいか）——を確かめる。

したがって **`solver.js` は遊ぶ側では使わなくなる**。捨てるのではなく
生成スクリプト側へ移す（枝刈りはそのまま要る）。`tests.html` にある
`solver.js` のテストも一緒に移す。

### 決めたこと

- **持つのは代表形**（65 件 / 2339 件）。回転・反転した見た目も解になるので、
  読み込んだあとに `boardSymmetries()` で展開してから盤面と突き合わせる。
  展開後は 520 件 / 9356 件で、文字列にして 1 MB 弱。メモリ上は問題ない
- **置き場所は `src/` の下。** `.github/workflows/pages.yml` は公開するものを
  `index.html` と `src/` だけと**列挙している**ので、`data/` のような新しい
  ディレクトリへ置くとローカルでは動くのに公開先で 404 になる。
  `src/` の外へ置くならワークフローも直す
- **6×10 のデータは、その盤を選んだときだけ読む**（動的 import）。
  タイトル画面と 8×8 では 139 KB を読まない
- 生成スクリプトは公開の対象外でよい（`tools/` など）
- **履歴は代表形の番号で持つ。** 60 文字の `cells` を 1 件ずつ持つのをやめる。
  番号は代表形を `boardKey()` の昇順に並べた順（1 から数える）。**並べ方を
  文字列順に固定する**のは、データを作り直したときに番号がずれないため
  （代表形は `canonicalBoard()` で一意に決まるので、並べれば毎回同じ順になる）。
  保存済みの履歴は `canonicalCellsKey()`（TODO-021 で入れた）を通せば番号へ
  読み替えられるので、**捨てずに済む**
- **クリアしたときに「この解は正解の ○○ 番と同じです」と出す。**
  代表形の番号なので、回転・反転して置いても同じ番号になる
- **ヒントは、条件に合う解から無作為に 1 つ選ぶ。** どのピースを置くかは
  今までどおり「一番若い空きマスを覆う 1 手」でよい（選んだ解が毎回違えば、
  そこから決まる手も自然に変わる）。TODO-017 の `shuffle` と `HINT_RETRIES`
  は、これで置き換わる

TODO-016 は `solver.js` を改造する前提で書いてあるので、この項目を先に
やるとその前提が変わる。**016 の中身の書き直しは、016 に着手するときに行う**
（この項目では扱わない）。

進め方: 生成スクリプト・データ・読み込み・テストにまたがるが、筋は上で
立っているので、**サブエージェントは編成しない**。モデル Opus、effort medium。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

- [**TODO-021.** クリアした結果を記録する際には重複を避ける](archives/todo/TODO-021.%20クリアした結果を記録する際には重複を避ける.md)
- [**TODO-020.** ヒント使用時は記録に残さない](archives/todo/TODO-020.%20ヒント使用時は記録に残さない.md)
- [**TODO-008.** クリアした結果を記録し、あとから見返せるようにする](archives/todo/TODO-008.%20クリアした結果を記録し、あとから見返せるようにする.md)
- [**TODO-012.** 回転・反転で同じになる解を、重複と見なす仕組みを検討する](archives/todo/TODO-012.%20回転・反転で同じになる解を、重複と見なす仕組みを検討する.md)
- [**TODO-013.** 置くたびに解の有無を教えるモードを足す](archives/todo/TODO-013.%20置くたびに解の有無を教えるモードを足す.md)
- [**TODO-019.** タップだけで、すべての向きを巡れるようにする](archives/todo/TODO-019.%20タップだけで、すべての向きを巡れるようにする.md)
- [**TODO-017.** ヒントは、毎回違うものを出すようにする](archives/todo/TODO-017.%20ヒントは、毎回違うものを出すようにする.md)
- [**TODO-015.** ピースの色を選べるようにし、ガラスっぽい見た目を足す](archives/todo/TODO-015.%20ピースの色を選べるようにし、ガラスっぽい見た目を足す.md)
- [**TODO-018.** ピース内の正方形の区切りをもっと細く薄くする](archives/todo/TODO-018.%20ピース内の正方形の区切りをもっと細く薄くする.md)
- [**TODO-009.** 盤のサイズに 6×10 を選べるようにする](archives/todo/TODO-009.%20盤のサイズに%206×10%20を選べるようにする.md)
- [**TODO-014.** 操作の説明が「長押しで裏返す」のまま残っている](archives/todo/TODO-014.%20操作の説明が「長押しで裏返す」のまま残っている.md)
- [**TODO-011.** スマホの縦画面では盤とトレイを縦に並べる](archives/todo/TODO-011.%20スマホの縦画面では盤とトレイを縦に並べる.md)
- [**TODO-010.** 反転を長押しからダブルタップに変える](archives/todo/TODO-010.%20反転を長押しからダブルタップに変える.md)
- [**TODO-007.** ピースを 1 個の塊として見分けられるようにする](archives/todo/TODO-007.%20ピースを%201%20個の塊として見分けられるようにする.md)
- [**TODO-006.** スマホでピースをつかみやすくし、トレイを詰める](archives/todo/TODO-006.%20スマホでピースをつかみやすくし、トレイを詰める.md)
- [**TODO-005.** スマホ横画面ではみ出ないようにする](archives/todo/TODO-005.%20スマホ横画面ではみ出ないようにする.md)
- [**TODO-004.** プレイ中の画面にもバージョン番号を表示する](archives/todo/TODO-004.%20プレイ中の画面にもバージョン番号を表示する.md)
- [**TODO-003.** プレイ中にタイトル画面へ戻れるようにする](archives/todo/TODO-003.%20プレイ中にタイトル画面へ戻れるようにする.md)
- [**TODO-002.** GitHub Pages で公開し、タグのバージョンを画面に出す](archives/todo/TODO-002.%20GitHub%20Pages%20で公開し、タグのバージョンを画面に出す.md)
- [**TODO-001.** ペントミノパズルを遊べる形まで作る](archives/todo/TODO-001.%20ペントミノパズルを遊べる形まで作る.md)
