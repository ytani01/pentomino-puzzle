# TODO

**残っている項目: TODO-001。** これまでに 0 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-002` から。**

---

## TODO-001. ペントミノパズルを遊べる形まで作る

- [ ] `index.html` と `src/main.js`（Phaser の起動、シーンの登録）
- [ ] `src/config.js`（盤面・12 種のピース・色・レイアウトの定数）
- [ ] `src/logic.js`（向きの生成、配置判定、盤面の更新。純関数）
- [ ] `src/solver.js`（ヒント用の求解。純関数）
- [ ] `src/audio.js`（効果音の合成）と `src/storage.js`（最短時間の保存）
- [ ] `src/scenes/boot.js`（マス目テクスチャの生成）と `title.js` / `clear.js`
- [ ] `src/scenes/game.js`（本編。ドラッグ配置・回転・反転・Undo・ヒント）
- [ ] `tests.html`（`logic.js` / `solver.js` / `config.js` のテスト）
- [ ] ブラウザで通しで遊んで確かめる

8×8 の中央 2×2 を穴にした 60 マスへ、ペントミノ 12 種をすべて敷き詰める。
解は 65 通りあり、1 画面に収まる大きさなのでこの盤面にした。

**決めたこと**（作り始める前の相談で確定した）:

| 項目 | 決定 |
|---|---|
| 土台 | ビルド工程なし。Phaser 3.90.0 を CDN から読み込み、自前コードは ES Modules |
| 盤面 | 8×8 の中央 2×2 を穴 |
| 操作 | ドラッグで配置。掴んでいないときのタップで右 90° 回転、長押しで反転 |
| 補助機能 | Undo / 経過時間とクリア記録 / 置けない場所の警告 / ヒント（1 ピース置く） |
| 効果音 | 入れる（Web Audio で合成。アセットファイルは持たない） |

**まだ決めていないこと**:

- ヒントの求解に上限を設けたとき、どのくらいで打ち切るのが妥当か。
  実測してから決める（画面が固まらないことが条件）

**モデルと effort**: Opus / effort high。ファイル数は多いが 1 つ 1 つは小さく、
盤面の座標系と `config.js` の定数を全ファイルが共有するので、分担せず通しで書く。

---

## 完了済み

1 項目 1 ファイル。`archives/todo/` にある（新しい順）。
**やらないと決めたものの理由もそこにある。** 蒸し返す前に読むこと。

（まだ無い）
